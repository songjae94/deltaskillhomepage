# 델타스킬 홈페이지 (deltaskill.co.kr)

수학 학습 AI '델타스킬'의 홈페이지. GitHub Pages로 배포되며 **main에 push하면 곧 배포**된다.
운영자: 송재형 (형). 이 리포는 프론트엔드 전용 — 정적 단일 파일 페이지들이다.

## 최종 목표 (2026-08-18 운영자 — 판단이 갈리면 이 문장으로 돌아온다)

**"인간이 수학을 이해하는 방식을 가장 잘 아는 기업."**

- 문항은 도구다. 자산은 **사람이 어디서 막히고 무엇으로 넘어가는가**의 기록이다 —
  델타맵 좌표·단계·관문·진단질의·처방이 본체고, 문제은행은 측정기다.
- 그래서 남의 문항을 모으는 회사와는 길이 갈린다: 문항 원문은 저작권에 묶이지만
  **이해의 지도는 우리 저작물**이고, 그 지도가 촘촘해지면 문항은 생성으로 대체된다(지시 7).
- 우선순위가 갈릴 때의 잣대: **그 작업이 '이해의 지도'를 더 정밀하게 만드는가.**

## 이름 — 여기서만 정한다 (2026-08-13)

**"스킬맵"이라는 말은 쓰지 않는다.** 그 한 단어가 서로 다른 두 층을 가리켜 계속 혼선이 났다.
층마다 이름을 하나씩만 쓴다.

| 부르는 이름 | 무엇 | 어디 |
|---|---|---|
| **델타맵** | 문항을 **어떻게 푸는가** — 단계(`steps`)·도구코드(`uses`)·좌표 | `deltamap.json` (32회차 1,088문항) |
| **도구코드** | 정규 어휘 78종 — 원장이 참조하는 유일한 코드 집합 | 원장 DB `tool_codes` |
| **원장** | 문항별 델타 좌표·관문·진단질의·계보 | 원장 DB `ledger_items` (608) |
| **교재 스킬** | 교재 기준 스킬 체계 — **아직 없다** | 재료가 생기면 `textbook_skills.json` 로 만든다 |

- ⚠️ **`skills.json` 은 삭제됐다**(2026-08-13). 2026-08-12에 델타맵으로 개명하면서 빈 `{}` 만
  남겨 뒀는데, 아무도 읽지 않으면서 이름만 겹쳐 혼선을 키웠다. 교재 스킬이 생기면
  **`skills` 라는 모호한 이름을 다시 쓰지 않는다.**
- ⚠️ **개명 이전 문서·주석은 `skills.json` 을 "문항 데이터"라는 지금과 정반대 뜻으로 쓴다.**
  `_dev/백엔드-인수인계-0812.md` · `_dev/파일럿2-정리.md` · ops 리포의 `backend/*.ts` 주석이 그렇다.
  옛 글에서 `skills.json` 을 보면 **델타맵으로 읽으면 된다.**

## 구조

| 경로 | 역할 |
|---|---|
| `index.html` | 랜딩 페이지 |
| `pilot/index.html` | **AI 오답노트 파일럿** (핵심 앱 — 답 입력·채점·오답 진단·사진 첨부) |
| `my/index.html` | 내 오답노트 (로그인·회원가입·결과 조회) |
| `cuts.json` | **회차별 등급컷** — `exam_cuts` 테이블에서 뽑은 것. `my`가 읽는다 |
| `deltamap.json` | **델타맵** — 회차별 문항의 단계·도구코드·스킬 후보 (아래 규격) |
| `rx.json` | **연습 처방** — 문항→도구코드→원형 기출 사다리. 원장 DB에서 추출로만 갱신. `my`가 읽는다 |
| `admin/` `report/` `partners/` | 관리자·리포트·파트너 페이지 |
| `privacy/` `terms/` | 법적 고지 (**동의 문구·개인정보 문구는 임의 수정 금지**) |

## 코드 관례

- 페이지는 **단일 HTML 파일 SPA** — CSS·JS 전부 인라인. 빌드 없음.
- 상태 객체: pilot은 `S`, my는 `SG`. 화면 전환은 `S.screen` + `render()`.
- localStorage 키: `ds_phone`(전화번호), `ds_students`(프로필), `ds_tk`(토큰), `ds_tk_ph`(**토큰이 어느 번호의 것인지** — 불일치하면 토큰 폐기), `ds_subs`(데모 기록), `ds_subid`(회차→submission_id 매핑).
- 백엔드: Supabase Edge Function (`CONFIG.api`). 엔드포인트: `/enroll` `/login` `/revoke` `/submit` `/submit-update` `/history` `/submission` `/profile` `/photo` `/photo-upload`. 백엔드 코드는 이 리포에 없음(Supabase에 직접 배포, 버전관리 안 됨).
- ⚠️ 전역 `input{width:100%}`가 있어 체크박스 등에는 개별 오버라이드 필요 (과거 버그 원인).
- ⚠️ **토큰을 저장하는 모든 곳은 `ds_tk_ph`도 함께 저장·삭제할 것.** 빠뜨리면 파일럿 저장이 `need_pin`으로 막힌다 (실제 발생했던 버그).

## 디자인 체계 (2026-08-11 확정)

**색은 세 계열뿐이다. 새 색을 추가하지 말 것.**

| 계열 | 값 | 쓰임 |
|---|---|---|
| 파랑 | `--brand #5aa2ff` / `--brandInk` / `--brandSoft` | 브랜드·액션·강조 |
| 코랄 | `#f0806c` (밝은 화면 `#c14933`) | 오답·주의 — **이 하나뿐** |
| 초록 | `--ok #5fd39a` | 정답·완료 |

- **코랄은 한 가지만 쓴다.** 틀림/찍어서 맞음은 색이 아니라 **채움 vs 테두리**로 구분한다
  (`.gn.x`=채움, `.gn.g`=2px 테두리·배경 없음). 예전엔 빨강+주황 2색이었고 서로 13°밖에 안 떨어져 뭉쳐 보였다.
- **파랑은 밝기로 위계를 만든다.** 주 동작만 꽉 찬 `--brand`, 카드 안 단계 버튼은 `--brandSoft` 배경 + `--brandInk` 글자.
- **밝은 화면(qlight)에서 `#5aa2ff`를 글자·테두리로 쓰지 말 것** — 흰 배경 대비 2.61로 기준 미달.
  반드시 `--brandInk`(qlight에서 `#1c4fa0`)를 쓴다. 변수는 테마에 따라 자동으로 바뀐다.
- 새 색을 넣기 전에 **대비 4.5 이상**인지 확인한다 (밝은 화면은 `#fff`, 어두운 화면은 `#05060f` 기준).

**테마 전환** — `quiz`·`note` 화면만 밝고(`body.qlight`) 나머지는 어둡다.
`render()`가 `body.qlight` + `documentElement.style.colorScheme` + `theme-color` 메타를 **함께** 바꾼다.
⚠️ 루트 `color-scheme`을 안 바꾸면 모바일 브라우저(삼성 인터넷 등) 다크 모드가 밝은 화면을 강제로 어둡게 칠해 색이 뭉개진다 (실제 발생).

## deltamap.json 규격 (델타맵)

```
{ "회차코드": { "트랙": { "문항번호": {
    "steps": [...], "uses": [...], "anchors": [...],
    "cost": "○강|○중|✗", "cand": true } } } }
```

문항을 **어떻게 푸는가**(단계·도구코드·좌표)를 담는다. 원장 DB에서 나오고,
세 페이지(`pilot`·`my`·`admin`)가 전부 이 파일 하나를 읽는다. 층 구분은 맨 위 **이름** 절 참고.

- **`cand: true` = 스킬 후보.** 원장의 `COST`가 `○강`인 문항 — 알맞은 도구를 쓰면 풀이가 크게 줄어드는 문항이라 **교재 스킬로 뽑을 1순위**다. `my`·`admin`의 "요구 스킬" 칸에 배지로 뜬다. (전체 608문항 중 ○강 423 · ○중 125 · 미부여 48 · ✗ 12)
- `uses` 항목은 문자열이거나 `{skill, step}` 객체다. **객체를 그대로 `esc()`하면 `[object Object]`가 찍힌다** — `useName()`을 거칠 것 (admin에서 실제로 전 회차가 그렇게 깨져 있었다, 2026-08-12 수정).
- **`dq` = 진단질의** (2026-08-19 추가, 대성 4회차 64문항) — `{q, ch:[3], a:"A|B|C", why}`.
  원장 `diagnostic` 텍스트를 구조화 파싱해 추출한 것. 파일럿 오답 카드가 자가확인 UI로 띄운다.
  ⚠️ **원장 형식은 정답이 항상 (A)다** — 화면에서 문항번호 기반 고정 순열로 섞고,
  `why` 의 원본 라벨(B는/C는)도 표시 기호(①②③)로 함께 번역한다. 데이터는 원장 순서 그대로 둔다.
  ⚠️ 파싱 때 선택지 속 수학 표기 `n(A)` 가 보기 마커로 오인된다 — 공백 뒤 마커만 경계로 삼는다.
- ⚠️ **회차코드가 앱과 맵에서 어긋나면 해설이 통째로 안 뜬다** — 수능이 앱은 `SN26`, 맵은 `SN26_11`이라 한 번도 주입되지 않았다(2026-08-12 별칭 추가로 복구, 둘 다 보존). 회차를 추가할 때 `EXAMS[].code`와 맵 키가 같은지 먼저 확인할 것.
- 파일럿은 `steps`만 읽는다. `steps`가 빈 배열이면 "어디까지 갔나요?" 단계 UI를 조용히 건너뛴다 — 부분 등재가 안전하다.

- 트랙: `공통` / `확통` / `미적분` — **기하는 등재 금지**. 2026-08-12 저녁에 기하 104문항(13회차)을 실제로 제거해 이제 잔존 0이다 (분석 제외 원칙). 기관용 `admin-api`도 같은 규칙을 `EXCLUDED_TRACKS`로 강제한다.
- 회차코드 예: `MP27_06`(평가원), `HP26_03`(학평), `OMEGA_5`, `SV27_01`(서바), `JQ27_02`(지QUEL).
- **내용(steps 등)은 수정하지 말 것** — 분석·검수는 Cowork(분석 세션)에서 하고, 완성본 교체 또는 원장 DB에서 추출로만 갱신한다. 이 리포에서 steps를 지어내지 않는다.

## 오답노트 두 갈래 (2026-08-11)

시험을 고르면 인트로에서 **두 카드 중 하나를 눌러** 시작한다 (하단 버튼 없음).

| 갈래 | `S.mode` | 화면 | 내용 |
|---|---|---|---|
| 채점부터 하기 | `"grade"` | `quiz` | 30문항 답 입력 → 자동 채점 → 틀린 문항 원인 |
| 채점은 했어요 · 오답만 체크 | `"note"` | `note` | 답 입력 없음. 틀린 번호만 탭 → 점수 자동 계산 |

- `S.mark[문항] = "x"`(틀림) / `"g"`(찍어서 맞음). 점수 = 100 − 틀린 문항 배점 합.
- 번호 탭 → 카드 하나만 펼침(`S.open`) → 상태·원인 입력 → [입력 완료]로 접힘. 번호나 접힌 줄을 다시 탭하면 수정.
- **저장할 때 맞은 문항까지 30개 전부 보낸다** (`my_answer:""`, `result:"o"`). 안 그러면 마이페이지 영역별 정답률·리포트가 깨진다.
- 찍어서 맞음은 `result:"o"` + `confidence:"찍음"`으로 저장 — **백엔드 스키마 변경 없이** 표현된다.
- 수정 진입 시 `my_answer` 유무로 어느 화면을 열지 자동 판별한다.
- 오답 원인은 5개: 개념 모름 · 계산 실수 · 접근 안 떠오름 · 시간 부족 · 아는데 실수
  ("안 배움"은 "개념 모름"과 중복이라 제거. 단 기존 기록용으로 `REASONFIX` 매핑은 남겨 둠).

## 백엔드 구조 (2026-08-12 확인)

- Edge Function `api`(**v8**, 파일명 `deltaskill_api_index_v8.ts`)는 **supabase-js(PostgREST REST) + service_role 키**로 DB에 접근 — Postgres 직결(5432/6543) 아님. 클라이언트는 모듈 스코프 전역 1회 생성이라 연결 고갈 위험 없음. 롤백본은 `api_v7_rollback.eszip`(운영자 PC로 옮겨 둘 것).
- **소유 판정은 student_id가 아니라 전화번호다.** `my_history`·`my_submission`·`/submit-update`가 모두 `students.phone` 으로 매칭한다 — 기기 변경·재가입으로 student_id가 갈라져도 이력이 보인다. 반대로 `review_items`(복습 원장)는 `student_id` 로만 묶이므로 계정이 갈라지면 **복습 카드만 쪼개진다.**
- 인증: 자체 토큰의 SHA-256 해시를 RPC `auth_verify`로 검증. `auth_enroll`·`auth_login`(레이트리밋 포함)·`auth_revoke_all`·`my_history`·`my_submission`·`my_photo_ok` RPC 사용.
### 같은 번호로 계정이 갈라진 사고 (2026-08-12 해결)

> 전체 기록(병합 되돌리는 법 · 프론트 수정 내역 · 검증 · 인증 조사): **`_dev/계정병합-인증-0812.md`**

마이페이지에서 [수정하기]를 눌렀는데 "먼저 로그인해 주세요"가 뜨고, 다시 입력했더니 **같은 번호로 학생이 하나 더 생기던** 문제. 원인이 앞뒤로 두 겹이었다.

| 층 | 무엇이 잘못됐나 |
|---|---|
| 프론트 | `/my/` 로그인이 `ds_students`(로컬 프로필)를 저장하지 않았다 → 파일럿이 프로필을 못 찾아 **가입 화면**을 다시 띄웠다 |
| 백엔드 | `/enroll`이 `students` 행을 **먼저 INSERT** 한 뒤 `auth_enroll`을 불렀다. 이미 등록된 번호면 `already_enrolled`(409)로 막히는데 **방금 만든 행은 롤백되지 않았다** → 빈 유령 계정이 쌓임 |

- 피해: 7개 번호 11행(빈 행 8 · 데이터 있는 행 3). 제출 이력은 `my_history`가 전화번호로 매칭해 계속 보였지만 **복습 카드(`review_items` 12장)는 student_id 기준이라 갈라져 있었다.**
- 병합: `student_auth`가 가리키는 행을 정본으로 삼아 `submissions`·`review_items`·`auth_tokens`를 옮기고 중복 행에 `merged_into`를 표시했다(**삭제하지 않는다** — FK·감사 보존). 충돌 0건. 원본은 `students_merge_log`에 `to_jsonb` 통째로 남겼다.
- 재발 방지 **세 겹**:
  1. DB — `students_phone_active_uniq` 부분 UNIQUE 인덱스 `on students(phone) where merged_into is null`. 병합된 행은 예외라 인덱스가 유지된다.
  2. 백엔드 v8 — `/enroll`이 살아 있는 같은 번호 행을 먼저 찾아 **재사용**하고, `auth_enroll`이 실패하면 이번에 만든 행을 **보상 삭제**한다. `already_enrolled`(409)는 그대로 유지 — 프론트의 `/login` 폴백이 이 문자열에 걸려 있다.
  3. 프론트 — `/my/`가 로그인·가입·재진입 때 `ds_students`를 저장하고, 파일럿은 로컬 프로필이 없어도 토큰이 있으면 `/profile`에서 받아 복구한다(부팅·[시작하기]·`?edit=`·`?profile=` 네 경로 전부).
- ⚠️ **토큰만 있고 프로필이 없다고 가입 화면을 띄우지 말 것.** 그게 이 사고의 방아쇠였다. 진짜 신규(토큰 없음)만 가입으로 보낸다.

- **`review_items` 테이블(간격반복 복습 원장)이 `responses.id`를 FK(NO ACTION)로 참조** — responses를 함부로 DELETE하면 막히거나 복습 이력이 끊긴다. `/submit-update`가 UPSERT인 이유.
- `responses(submission_id, item_no)` unique index `responses_sub_item_uniq` 존재 (UPSERT 전제조건).
- 프로젝트: `deltaskill-pilot`(ref cvlrphtupjmaefahodgz, 서울 ap-northeast-2). 조직은 **Pro 플랜**(2026-08-12 업그레이드) — 일일 자동 백업 7일 보관(매일 KST 0시 33분경), PITR은 미사용. 같은 조직의 `deltaskill` 프로젝트는 옛것(일시정지 상태 — resume하면 과금되니 그대로 둘 것).

## 기관용(파일럿2) 데이터 — 2026-08-12 확인

**`admin-api`(Edge Function, v4)가 이미 기관 백엔드의 절반이다.** `admins(role, academy)` + `admin_sessions`(토큰 해시·12시간 만료) + `admin_login`·`rl_hit` RPC + **서버측 학원 범위 강제**(`role='academy'`면 `students.academy` 밖을 못 봄). 관리자 계정은 현재 **1개, `role='owner'`**(운영자 본인).

| 테이블 | 내용 | 읽는 액션 |
|---|---|---|
| `official_scores` | **운영자가 강사로서 보유한 학원 성적 명단.** 623행(OMEGA_5·6·7·8 I반) + JEONDAE_7 218행(일산) + JEONDAE_8 332행(신촌) + **신촌 9회차 1,825행(2026-08-19 — 성적일람표에서 수학 응시자만: DSP-27-M05 202·JEONDAE_7 210·OMEGA_1/3~8 1,413. `class`='신촌 NN반')**. 지점은 `class` 접두로 가른다(I반/일산/신촌). ✅ **같은 시험은 지점 합산이 더 정확하다**(형 확인 2026-08-19) — JEONDAE_7 은 일산+신촌 428명 합산으로 평균 66.1. ⚠️ 신촌 오메가 5~8 = I반과 같은 시험(정답 238문항 대조 0 불일치로 확정 — 두 해 회차번호 겹침 함정 검사함). 등급: DSP-27-M05 만 공식 수능예상 컷으로 채움, 오메가·전대실모는 null(I반 학원추정컷을 신촌에 적용 금지). 신촌 9회차는 `student_no` 없음(일람표에 학번 칸 없음) | `cohort` |
| `item_stats` | 문항별 `correct_rate` + `c1`~`c5`(**선택지별 선택률 — 정답 포함**). **모집단 3종을 `source` 로 가른다: NULL=운영자 학원 I반 · `'SINCHON'`=신촌메가 · `'KCSAT-ML'`=전국 공식.** OMEGA_5~9 확통·미적분 298행(NULL) + KCSAT-ML 322행(수능 2014~2025, 기하 제외) + **SINCHON 660행(2026-08-19 완성 — 메대프 4·5월, 전대실모 7·8월, 오메가 2~8회 × 각 60)**. ✅ **admin-api v6 배포(2026-08-19)** — `items` 액션이 source 를 가른다(미지정=NULL 우선 · `source:"SINCHON"` 지정 · 응답에 source/sources 명시). ⚠️ 이 적재를 위해 **item_stats PK 를 source 포함 유니크(NULLS NOT DISTINCT)로 변경** — 옛 PK (exam,subject,item_no) 가 두 모집단의 공존을 막고 있었다(409 실측, 마이그레이션 2026-08-19_item_stats_pk_source포함.sql). 섞어 밴드를 매기지 말 것 | `items` |
| `review_tokens`·`notify_log` | 미사용(0행) | — |

**admin-api v5 (2026-08-12 배포) — 추가된 액션 2개, 둘 다 `role='owner'` 전용(403으로 차단):**

- **`cohort`** `{exam_code?, prev_exam_code?, classes?[]}` → 회차·반 목록, 반별 집계(인원·평균·SD·평균등급·선택과목 분포), 반×회차 추이, 알림(직전 회차 대비 **−10점 이상 급락** / 미응시). 회차 생략 시 최신 회차.
- **`items`** `{exam_code, subject?}` → 문항별 정답률·정답·선택지 분포. **단답형은 `objective:false`** 로 표시하고 오답 분포를 만들지 않는다(c1~c5가 전부 0이라 만들면 거짓이 된다). 객관식은 `top_wrong`(최다 오답 선택지)을 준다.
- **기하 제외**: 상수 `EXCLUDED_TRACKS`로 두 액션 모두에서 걸러낸다. `subject:"기하"`를 직접 요청하면 `excluded_track`(400). 영향은 미미 — OMEGA 4회차 통틀어 16명분(I2·I4·I8·I11), I2 평균이 72.6→72.7로 움직이는 정도.
- 롤백은 v4 재배포. 기존 액션(`login`·`logout`·`students`·`student`)은 미변경.

- **원본은 회차마다 엑셀 2개**: 성적(`DB` 시트 + 반별 시트 + 평균) · 문항분석(과목별 시트, 7회는 `DB` 통합). 원본에는 **학번·탐구1/2·국어유형(화작/언매)·수험번호**가 더 있고 그중 학번만 적재했다.
- **적재 규칙: 수학 응시자만.** 원본 248/245/244/241명 → DB 184/147/137/155행으로 정확히 일치(총 623).
- **조인 키는 `student_no`.** 이름도 원본에서 수기로 구분돼(`강건우1`·`김유찬1`) 실제로는 유일하지만, 그 규칙에 기대지 않는다.
- ⚠️ `official_scores`는 **`role='owner'` 전용**으로 다룰 것. 학원 계정(`role='academy'`)에 열면 동의 범위 밖 개인정보 제공이 된다.
- **등급컷 확장 (2026-08-13)** — 대성 더프리미엄 공식 등급컷 12회차를 받아 적재했다(48행).
  `exam_cuts` 에 **`subject`·`cut6~8`** 을 더하고 키를 `(exam_code, subject, basis)` 로 넓혔다.
  기존 5회차는 `subject=''`(과목 무관)로 그대로 산다. `grade_of()` 는 **옛 시그니처를 유지**한 채
  과목·기준을 선택 인자로 받는다(호환 확인함).
  - 담긴 것: 2025년 시행 8회차 + 2026년 시행 4회차 · **확통·미적분**(기하 제외) ·
    **1~8등급** · 기준 둘 — `실제응시`(더프리미엄 응시집단) / `수능예상`(실수능 분포 환산).
  - ⚠️ **선택과목별로 컷이 다르다.** 같은 85점이 `DSP-27-M03` 에선 2등급, `DSP-27-M07` 에선 3등급이다.
    회차당 컷 한 벌로 다루면 틀린다.
  - ✅ **프론트 반영 완료 (2026-08-19)** — `cuts.json` 이 과목별 컷(`subj:{확통,미적분}`)을 담고
    `estGrade(score,subj,code)`·`examCuts(code,subj)` 가 과목 컷을 고른다. 옛 모양(cuts 배열)
    호환 유지. **기준은 `수능예상`만 내보낸다**(실제응시를 섞으면 같은 점수가 두 등급을 가진다).
    cuts.json 은 `deltaskill-ops/scripts/gen_cuts_json.py` 로만 재생성한다(손 수정 금지).
    ⚠️ DB `grade_of` 는 basis 를 안 주면 두 기준 중 아무 행이나 잡는다(order by 가 subject 만
    본다) — 호출할 때 **basis 를 명시**할 것. 또 grade_of 가 2인자·4인자 두 벌이라 PostgREST
    rpc 는 `p_subject` 를 안 주면 300 Multiple Choices 가 난다.

### 회차코드 체계 — `{SRC}-{학년도2}-{KEY}` (2026-08-13 확정)

> ✅ **분류체계 재정리 (형 승인 2026-08-27)** — 두 갈래 두 층위: **기출(정부)=시험 제도 코드**
> (SNG·PYG·EDU·EBS) / **사설=발행 주체 코드 주체당 1개**, 시리즈는 KEY 앞 글자.
> 메가 통합이 본체: OMG·OMB(·black·the27) → **MEG-{YY}-OG/OB/OK/OT{NN}**, 전대실모 J{MM}.
> 옛 코드는 안 지운다 — DB `exam_aliases` 치환표(30행)가 잇고 **신규 등재부터 새 체계**.
> JR↔JRO·MEGA↔MEG 폴더 철자 통일은 라인 배치 완료 후. 상세: `deltaskill-ops/docs/코드체계-재정리안-0827.md`.
> ✅ 지QUEL(JQL)=메가 QUEL 계열 판정(형 확인 0827 '퀄은 메가뿐'+R2 파일명 전수 메가 표기) → MEG 포함, 신규 등재 금지.
> ⚠️ OMJ 2행은 MEG-26-J09와 겹침 의심 — 치환표에서 제외해 둠(원본 대조 후 결정).

1,326개 PDF 를 넣기 직전에 세웠다. **도구코드가 138종으로 갈라졌던 사고를 회차코드에서
반복하지 않기 위해**, SRC 를 DB 표 `src_codes` 에 등록하고 `exams.src` 에 **FK** 를 걸었다.

- **YY 는 학년도**(시행년+1). `2026년 3월` → 27 · `2027학년도` → 27.
  ⚠️ 이걸 섞으면 한 해가 통째로 어긋난다 — 이 프로젝트에서 가장 자주 난 사고다.
- **KEY**: 시험은 시행키(`M03`·`M06`·`M09`·`M11`·`R05`), 교재는 교재키(ASCII).
- **`MO` 로 시작하는 파일 = 대성학원 모의고사 원본이다** (운영자 확정 — 다시 묻지 말 것).
  드라이브 `연월별사설 모의고사/{연도}/` 안의 `MO{YY}…{MM}` 꼴: 끝 두 자리가 시행월.
  예: `MO263…03` = 2026년 3월 시행 = **DSP-27-M03**. 파일명이 다 가려져 있어도 MO 면 대성이다.
- SRC: `SNG`수능 · `PYG`평가원 · `EDU`교육청 · `EBS`교재 / `DSP`대성 더프리미엄 ·
  `SDA`시대인재 · `OMG`·`OMB`OMEGA · `MEG`메가 · `JQL`지퀄 · `PRV`개인 제작(강사)
- **한 발행처가 여러 시리즈를 낸다** — 시대인재는 서바이벌·숏컷 등. SRC 는 발행 주체
  하나로 두고 **시리즈는 KEY 앞 2글자**로 가른다(`SV01`·`SC01`). 새 시리즈는 `src_codes.note` 에 먼저 적는다.

**⚠️ 회차물은 시행월을 쓰지 않는다 — 회차번호로 만든다** (2026-08-14 운영자 확인).
오메가·서바이벌은 **시행일이 정해져 있지 않고 학원마다 다른 날 친다.** 파일에 `2026년 10월` 이
적혀 있어도 그건 그 학원이 친 날일 뿐 시험의 성질이 아니다.
- **월을 코드에 넣으면 같은 5회차가 학원마다 다른 회차코드로 갈라진다** — 도구코드가 138종으로
  갈라졌던 사고와 같은 모양이다. 그래서 월을 **의도적으로 버린다.**
- 있는 것은 **"몇 학년도 몇 회차"** 둘뿐이고 코드도 그 둘로만 만든다: `OMG-27-R05` · `SDA-27-SV01`.
  OMEGA 는 시리즈 구분이 SRC 층(OMG/OMB)에 이미 있어 `R` 하나면 된다.
- ⚠️ **월조차 없을 수 있다.** 연도와 회차만 있는 이름이 정상이다 — 월이 없다고 판별 실패로 보지 말 것.
- ⚠️ **맨 연도(`2027_서바이벌_01`)는 학년도로 읽는다.** 사설 회차물은 대비 학년도로 이름 붙여 파는
  관례다. **시행년으로 이름 붙이는 발행처가 있으면 그만큼이 한 해 어긋난다** — 보고서의
  `연도근거=맨연도→학년도` 를 `--apply` 전에 눈으로 확인할 것.
- ⚠️ 회차는 **근거가 있을 때만** 읽는다(`N회`·`제N회`, 또는 이름 **맨 끝**의 숫자). 아무 숫자나
  주우면 `2016EBS7030A형` 이 7030회가 된다. 규칙 검사는 `ingest_local.py --selftest`(12건).
- **월과 회차가 함께 있으면 회차가 이긴다.** 회차는 시험의 성질이고 월은 그 학원이 친 날이다.
  **회차 표기가 없을 때만** 월로 만든다(`OMG-27-M10`).
- **연도는 폴더명에서 빌린다** — 오메가는 연도를 폴더로 구분해 뒀다(운영자 확인).
  `2027 오메가/OMEGA_5회.pdf` 처럼 파일명에 연도가 없는 것이 정상이다.
  ⚠️ **폴더에서 회차는 빌리지 않는다** — 한 폴더에 여러 회차가 들어 있어 폴더로 회차를 읽으면
  그 폴더가 통째로 한 회차로 뭉개진다.
- ⚠️ **파일명이 일련번호뿐이면 근거로 치지 않는다**(`201103가18`·`2016EBS7030A형`).
  근거로 세우면 인쇄 제목과 어긋나 **멀쩡한 문서가 통째로 검토필요로 막힌다** → 그럴 땐 제목만 본다.

**⚠️ 기출 폴더는 `고N` + 시행월이면 출처를 정한다** (2026-08-14 실측). `--gichul` 은 사람이
"이 폴더는 기출"이라고 보증한 것이다. 그 안에서 이름이 학년과 월을 함께 밝히면 그걸로 정한다.
- 이걸 안 하면 `2003년-06월-문제및해설고3-수리(가형)` 꼴 **339개**(EBS 267 + 년도별 72)가
  전부 코드미상이 된다 — 이름에 '전국연합학력평가'라는 낱말이 없어서다. **스캔본이면 인쇄
  제목도 못 읽어 영원히 막힌다.**
- ⚠️ **학년이 결정적이다** — 고3만 6·9월이 평가원이고 고1·고2는 6·9월도 학력평가다.
  학년이 없으면 이 길을 쓰지 않는다(개인 제작이 섞인다).
- ⚠️ **회차는 괄호 안에 있을 수 있다**(`OMEGA BLUE 수학_문제지(2회)`). 꼬리표 `(문제)`·`(해설)`
  을 떼기 **전에** '회' 형을 먼저 본다 — 먼저 떼면 회차까지 날아간다. 반대로 안 떼면
  `2027_서바이벌_01(문제)` 의 01 이 '이름 맨 끝'에 안 걸린다(서바 8개가 그래서 막혔다).
- **OMEGA 는 이름의 `BLUE` 로 OMG/OMB 를 가른다.** 예전엔 통째로 사람에게 넘겼는데
  **표기가 있는 것까지 넘길 이유가 없다** — 표기가 없을 때만 넘긴다.
- **파일명도 출처의 근거다** — 본문만 보면 스캔본에서 아무것도 못 얻는다.

**⚠️ 연도 미상은 코드미상이 아니다 — 출처를 남긴다** (2026-08-14 운영자 지시).
출처를 알아냈는데 연도만 모르는 것을 '아무것도 모름'과 한 칸에 넣으면 **알아낸 것을 버리는 셈**이다.
- 학년도 자리에 `XX` 를 넣어 보고서 `연도미상` 칸에 모은다: `OMB-XX-R02` · `DSP-XX-M10`.
  사람은 **연도만** 채우면 끝난다.
- ⚠️ **`XX` 코드는 등재하지 않는다.** 다른 해의 같은 회차와 한 코드로 겹쳐 조용히 덮어쓴다.
  확정은 `--map` 에서 한다.
- ⚠️ `yy_month()` 는 연도를 못 찾아도 **월은 계속 읽는다.** 예전엔 `(None, None)` 을 돌려주며
  읽어낸 월까지 버렸다.

**⚠️ 폴더가 연도를 알려 주는 곳이 많다 — 다만 하나일 때만 빌린다** (2026-08-14).
`D:\수학 NEW\4. 모의고사 모음\연도별 사설포함 모의고사` 는 `2021년 모의고사`…`2025` 로 **연도별 폴더**다.
- 연도를 **형태 가리지 않고 세어 정확히 하나일 때만** 빌린다. 형태별로 세면
  `[년도별] … (2003-2024년)` 에서 '년' 붙은 게 2024 하나뿐이라 통과해 **22년치가 통째로 2024년으로 뭉개진다**(실측).
- ⚠️ **형제 폴더끼리 표기가 어긋난다** — `2024년 모의고사` 옆에 맨숫자 `2025` 가 있다.
  앞은 시행년(→25학년도), 뒤는 학년도(→25학년도)로 읽혀 **같은 25가 다른 해를 가리킨다.** 사람이 확인할 것.
- ⚠️ **`사설포함` 폴더에 `--gichul` 을 주지 말 것.** 주면 `고N`+월 규칙이 **개인 강사 모의고사를
  교육청 기출로 등재한다.** 안 줘도 문서에 출처 표기가 있는 기출은 정상으로 잡힌다.
- ⚠️ **`.hwp` 는 자동으로 무시된다**(PDF 만 훑는다) — 한글본은 오타 위험으로 이미 접은 갈래다.

**⚠️⚠️ 오메가는 두 해 분량이 있고 회차 번호가 겹친다** (2026-08-17, GPT F 판정으로 발각).
인쇄된 제목을 읽어 보니 같은 폴더에 **2026학년도와 2027학년도가 섞여 있었다.**

| 파일 이름 꼴 | 인쇄된 제목 | 코드 |
|---|---|---|
| `OMEGA_모의고사_수학_NN회_문제지(검토본).pdf` | **2026학년도** OMEGA 모의고사 N회 | `OMG-26-R{NN}` |
| `OMEGA_NN회_수학_문제지_unlocked.pdf` | **2027학년도** OMEGA 모의고사 N회 | `OMG-27-R{NN}` |

- **5·6·7·9·10 회가 양쪽에 다 있다.** 이름 규칙만으로는 **절대 못 가른다** — 내가 만든
  `map_ledger.json` 이 둘 다 `OMG-26` 으로 뭉개 **5건이 한 해 어긋나 있었다.**
- ⚠️ **가르는 유일한 근거는 문서 안의 인쇄된 제목이다.** 파일명·폴더명은 이 경우 무력하다.
- ⚠️ **OMEGA blue 는 2026학년도**로 확인됐다(원장 `OMB-26` 과 일치). blue 와 일반이 **다른 해**다.
- **저작권자는 둘 다 메가스터디교육(주)** 다. SRC 를 발행 주체로 두는 원칙과 어긋나지만
  원장이 이미 `OMG`·`OMB` 를 쓰므로 **바꾸지 않는다** — 기록만 남긴다.
- 피해: 등재된 오메가 21행이 거의 전부 `kind=answer`(해설지)라 **분할이 안 돼 문항 오염은 없다.**
- ⚠️ **`item_sources` 에 원본 파일명 칸이 없어 어느 행이 어느 파일에서 왔는지 DB 만으로는 몰랐다.**
  `source_name` 을 추가한다(`db/migrations/2026-08-17_…`). 다음엔 되짚을 수 있어야 한다.
- 확정표: `db/map_확정.json`(194개) — **F 판정이 내 추정을 이긴다.** 만드는 도구는 `scripts/f_to_map.py`.

**취합 후 일괄 정리** — 판정은 초안이고 확정은 모아서 한 번에 한다: `deltaskill-ops/docs/회차코드-일괄정리-0814.md`.
갈라짐·겹침·중복·빈칸은 **여러 파일을 모아 놓아야만 보인다.** 가르는 자는 이름이 아니라 **정답표 해시**다.
⚠️ 최종 산출물은 `--map` 확정표다 — 그래야 나중에 판정 로직을 고쳐도 확정된 대응이 안 흔들린다.

**자동 판정** (`ingest_local.py`) — 사람이 `--src` 를 안 줘도 되는 경우가 많다:
① 문서 전문(앞16쪽+뒤8쪽)에서 **저작권·발행처 표기**로 출처 ② 문항 1번 쪽 상단의
**인쇄된 제목**에서 학년도·시행월 ③ 파일명에서도 뽑아 ②와 **대조 — 어긋나면 중단**.

⚠️ **여기서 실제로 잡은 함정 넷** (전부 조용히 틀린 회차로 등재될 뻔했다):
1. **사설이 수능 제목을 흉내 낸다.** 대성 제목이 `2027학년도대학수학능력시험3월모의고사문제지` 다.
   "대학수학능력시험"이 있어도 **시행월이 11월이 아니면 수능이 아니다.**
2. **출처를 모르면 코드를 만들지 않는다.** 예전엔 시행월만 보고 `3월 → EDU(교육청)` 로 단정했다.
   그러면 **개인 강사 모의고사가 교육청 기출로 등재된다.** 개인 제작은 제목이 제각각이라
   이 오분류가 대량으로 났을 것이다. 기출은 `전국연합학력평가`·`한국교육과정평가원` 같은
   **적극적 근거**가 있을 때만 인정한다.
3. **저작권 표기 위치를 가정하지 않는다.** 대성은 쪽 하단이지만 모두가 그렇지 않다 → 전문을 훑는다.
4. **표기가 갈리면 집계가 갈린다.** 대성 제목이 `대성 THE PREMIUM` / `더프리미엄` /
   `대성 더프리미엄` 세 갈래였다 → `대성 더프리미엄 {학년도}학년도 {월}월 고3` 로 통일.

⚠️ 아직 사람 손이 필요한 곳: **OMEGA**(OMG/OMB 를 이름으로 못 가름) · **스캔본**(텍스트 없음) ·
**개인 제작**(제목 제각각) — 전부 `--src` 또는 `--map` 으로 준다.

⚠️ **대성 더프리미엄은 응시 규모가 교육청 다음으로 크다**(운영자 확인). 등급컷·정답률
표본으로 값지다 — 사설 중에서 우선 순위를 높게 둔다.

### ⚠️ 데이터 모집단이 셋으로 갈린다 — 섞으면 안 된다 (2026-08-13)

같은 회차라도 어느 집단의 숫자인지가 다르다. 이걸 섞으면 "정답률 X%면 Y등급" 같은 **틀린 추론**이 나온다.

| 데이터 | 모집단 | 어디 |
|---|---|---|
| **대성 등급컷** | **대성 더프리미엄 전국 응시자** | `exam_cuts` basis=`실제응시`/`수능예상` |
| **문항 정답률·변별도** | **한 학원 학생** (운영자가 강사로 있는 곳) | `item_stats` |
| **성적 명단** | **같은 한 학원 학생** | `official_scores` |
| 옛 등급컷 5회차 | 같은 한 학원 응시자 추정 | `exam_cuts` basis=`학원응시자상대추정` |

- ⚠️ **문항 정답률은 전국 정답률이 아니다.** 그 학원 표본이라 전체 평균과 다를 수 있다(운영자 확인).
- ✅ 반대로 **둘을 대조하면 그 학원의 전국 대비 위치를 잴 수 있다** — 같은 회차에
  전국 등급컷과 학원 점수 분포가 함께 있는 회차가 생겼기 때문이다. 이건 새로 열린 길이다.
- ⚠️ 학생 성적 명단은 **개인정보** — 어느 리포에도 넣지 않는다. Supabase 한 곳뿐이다.

- **등급컷 — 회차별 적용 (2026-08-12 해결, 5회차분)**: DB `exam_cuts` + 함수 `grade_of(exam,score)` · `delta_to_next(exam,score)` · 뷰 `cut_coverage`. 여기서 뽑은 `cuts.json`을 `my/`가 읽어 `estGrade(score, subj, code)`가 **회차 컷을 우선** 쓰고, 없으면 기존 `GRADE_CUTS`(전국 모평 기준)로 되돌아간다.
  - 보유: `OMEGA_5·6·7·8` · `JD27_07`(전대실모 7월) 5회차. 나머지 98회차는 아직 컷이 없다.
  - ⚠️ **`basis='학원응시자상대추정'`** — 그 학원 응시자 분포에서 뽑은 값이라 **전국 등급이 아니다.** 파일럿(해당 학원 학생) 진단엔 맞지만 전국 등급으로 말하면 안 된다. `my/` 표에서 그 회차는 `*` 표시 + 각주로 밝힌다.
  - **가진 컷 아래로는 추측하지 않는다** — 5등급컷이 있는 회차만 6등급까지 말하고, 없으면 `null`(화면엔 `-`). DB `grade_of`와 프론트 `estGrade`가 같은 규칙이다.
  - 효과: 같은 85점이 OMEGA_5에선 2등급, OMEGA_6·8에선 3등급으로 갈린다(예전엔 전부 2등급).
- 기하: 문항분석에 3회차 모두 존재하고 실제 선택 학생도 있지만(I2·I4·I11), **제외로 확정**했다(2026-08-12).

## `/pilot2/live2/` — 단과 강사 미팅용 리포트 (2026-08-12)

첫 유료 제안용 데모. **비밀번호를 넣어야 내용이 나온다.**

- **암호화 방식**: 앱 코드와 데이터 전체를 AES-256-GCM으로 봉인해 base64로 심었다. 잠금 해제 시 PBKDF2-SHA256 310,000회로 키를 파생해 WebCrypto가 복호화하고 `new Function`으로 앱을 띄운다. 백엔드 없음.
- ⚠️ **평문 원본(생성기·앱 소스·데이터 JSON)은 이 리포에 절대 커밋하지 말 것.** 리포가 공개라 소스가 들어오는 순간 암호화가 무의미해진다. 원본은 운영자 PC에 있다(`live2_소스_0812.zip`). 커밋된 것은 암호문뿐.
- **비밀번호 변경·데이터 재생성**: 그 zip 안에서 `node build_live2.mjs '새비밀번호'` → 나온 `live2_index.html`을 `pilot2/live2/index.html`로 교체. 현재 비밀번호는 운영자가 보관.
- 같은 탭에서 새로고침하면 `sessionStorage`로 다시 묻지 않는다(미팅 중 실수 방지). 탭을 닫으면 사라진다.
- 강사명은 데이터의 `meta.teacher`(현재 **정재일**)다. 다른 강사용으로 바꾸려면 재생성이 필요하다.

**관문 통과율** — 상세 화면의 머리다. 원장이 "결정적"이라 표시한 지점(G1·G2·G3)별로 통과율·이탈 인원·지역차를 보여준다.
- ⚠️ **원장에 관문↔단계 대응 관계가 없다.** 174문항 중 개수가 맞는 건 25%뿐이다. 관문 서술이 항상 단계 순서를 따라가고 ★가 많은 단계에 놓인다는 관찰에 기대, `pack_live2.py`의 `align_gates()`가 **별 많은 단계를 관문 수만큼 골라 순서대로 배열**한다. 근사이며, 통과율 수치 자체는 시뮬레이션 단계 데이터에서 그대로 나온다. 미팅에서 "관문별 통과율"이라 말해도 되지만 "관문이 몇 번째 단계인지"까지 단정하지 말 것.
- 이탈 인원은 **직전 관문 이후 구간 전체**를 그 관문에 귀속시킨다 — 그래야 이탈 합 + 마지막 관문 통과 = 응시 인원이 된다.
- 학생 상태 태그는 라벨 문자열이 아니라 **식별자(`drop`/`miss`/`time`/`lazy`/`guess`)로 거른다.** 문자열로 거르면 "과제 미입력"과 "과제 부실"이 함께 걸린다(실제 발생, 39명 → 114명).

**데이터 출처가 세 층으로 갈린다 — 설명할 때 섞지 말 것.**

| 층 | 출처 |
|---|---|
| 점수 분포 | **실측 모수** — OMEGA 5~9회 1~4등급 134명에서 추정 (실력 SD 7.6 · 회차 난이도 SD 1.7 · 주간 변동 SD 5.4 · 잔차 자기상관 실질 0) |
| 문항 분석 | **원장 실산출물** — `ledger_items` 162문항의 스킬코드·단계분해·관문·진단질의를 그대로 씀 |
| 학생 반응 | **시뮬레이션** — 가상. 정답 여부를 따로 뽑지 않고 **단계 통과 확률의 곱**으로 유도해 점수와 막힌 지점이 한 모델에서 나온다 |

- 설정: 대치 223명 · 목동 195명(418명), 1~4등급만, 델타모의고사(현장, 입력 98%) + 스킬모의고사(과제, 92%) 각 5회 완료 + 6회 예약.
- 화면 5개: 브리핑 · 막힌 지점 · 스킬 지도 · 학생 · 다음 회차.
- ⚠️ 시뮬레이션 모수를 건드릴 때 아는 함정 셋: ① 실측 잔차 5.4는 **문항 표집 노이즈를 이미 포함**한다 — 따로 더하면 이중 계상(SD가 14.8까지 뜬다) ② '실력'은 로지스틱을 지나며 격차가 **약 2.4배로 증폭**된다 — 평균만 맞추면 SD가 안 맞고, 지역 오프셋도 같은 배율로 커진다 ③ 단계 ★ 계수를 SIG보다 크게 두면 ★★★ 단계가 문턱이 돼 **이탈이 한 지점에 80% 넘게 몰린다**(퍼널이 무의미해진다).
- 2·3점 문항에는 스킬코드를 붙이지 않는다. 붙이면 쉬운 문항의 0% 오답률이 그 도구의 실패율로 둔갑해 스킬 지도가 오염된다(실제로 겪음).

## 원장(ledger) DB — 2026-08-12 적재 완료 (608문항)

> ✅ **현재값(2026-09-01): 원장 3,577행 · 분석분 3,003 · 갈림 미해소 278.** 재가동 등재 —
> 이승열 기출 gpt 881 첫 수거(+578 등재 · 보류 227) + s2 갈림 정답축 187 판정 반영(해소 174).
> exams 539(+9 = MEG-27-OK01~09 오메가 black, 학년도=서술 추론 note). items 시험지 270단위 10,544문항.
> (직전 0828: 2,999·2,425) 비교기 정규화(형 승인 0828 — 선지 번역
> 동치·도구코드 미부여=결손)로 갈림 775→421, 227문항 추가 등재. (직전 0826: 2,772·2,198) v4 완성본 1,503문항이 세라인
> 등재규칙(B안)으로 들어왔다(machine `L1`/`L2`/`L3` · `line_used` 칸 · 갈림 442는
> `line_disputes` 표). `ledger_enroll_uniq` 는 이제 **전면 유일**(exam_code, subject, qno)
> — machine 무관하게 한 문항 한 줄이 물리적으로 강제된다. 규칙·집행 기록은
> `deltaskill-ops/docs/세라인-등재규칙-초안-0823.md`. 아래 절의 수치(1,385·608·777)는 그날 기준.

Cowork(분석 세션)의 원장이 Supabase로 들어왔다. **분석 산출물 저장소이고 학생 데이터와는 별개 계층이다.**
오전에 v4(314문항)를 넣고, 오후에 `run_all_v3` 분할본 5개로 **608문항 전량**을 채웠다.

| 테이블 | 행 | 내용 |
|---|---|---|
| `tool_codes` | 78 | 정규 도구코드 — 원장이 참조하는 유일한 어휘 |
| `code_aliases` | 164 | 원시코드 → 정규코드 치환표 |
| `exams` | 101 | 회차 마스터 (`{SRC}-{학년도2}-{시행키}`) |
| `ledger_items` | **1,385** | 원장 — **분석분 608 + 수집자리 777**(아래 ⚠️) |
| `exam_items` | 152 | 대성 4회차 채점용 문항 (`ans_format` 제약 보유) |
| `held_items` | 2 | **출제사 정답표 오류로 등재 보류**한 문항 (원장 아님) |
| 뷰 4개 | | `lineage_edges` · `practice_candidates` · `practice_ladder` 등 |

### 수집 한 바퀴 완주 — EBS 19회차 (2026-08-14)

**등재 → 검증까지 처음으로 한 바퀴 돌았다.** `item_sources` 0 → 19 · 크롭 801장 · 오류 0.

- **분할 밀림 0** (`scripts/verify_crop_numbers.py`). 크롭 자리(`rects`)의 PDF 텍스트에서
  머리번호를 읽어 붙인 문항번호와 견준다. **Mathpix 키가 필요 없다** — 우리가 묻는 건
  '수식을 잘 읽었나'가 아니라 '분할이 밀렸나'라서 훨씬 싸게 잡힌다.
  ⚠️ 정답 대조로는 이걸 못 잡는다 — 기출 회차의 원장 `ans_value` 가 회차당 3~6개뿐이라
  표본이 너무 작다. **밀림 검사와 수식 검증은 다른 축이다.**
- 검증 가능 708/1,175(60%) · 전부 정상. 나머지 40%는 **엉터리 OCR 레이어·스캔**이라 판정불가로 뺐다
  (쓰레기를 정답지로 삼으면 멀쩡한 판독을 대량으로 뒤집는다).
  ⚠️ **판정불가는 '밀렸다'가 아니다** — 이 방법으로 못 볼 뿐이고, 밀림 증거는 어디에도 없다.
- ⚠️ **`--map` 은 확정표다 — 표에 없는 파일은 자동판정으로 넘어가지 않는다.** 폴백이 켜져 있던
  탓에 표에 없는 파일이 `EDU-05-M10` 으로 판정돼 **FK 위반으로 `--apply` 가 멈췄다**(실측).
  옛 동작은 `--map-fallback` 으로만.
- ⚠️ **`exams` 에 없는 회차는 등재 전에 건너뛴다.** 여기서 `exams` 를 자동 생성하지 말 것 —
  잘못 판정한 코드가 그대로 새 회차가 된다. **FK 는 방어막이지 걸림돌이 아니다.**
- ⚠️ `q.send` 는 SQL 오류를 `Failed to run sql query: ERROR: …` 라는 **글**로 돌려준다.
  파싱 실패로 삼키면 명백한 오타가 'API 고장'으로 둔갑한다. 그리고 **진짜 간헐 빈응답도 있다** —
  `count(*)` 는 언제나 1행을 주므로 그걸 탐침으로 쓰면 '0행'과 '고장'이 갈린다.

### ⚠️ **원장 행 = 분석분이 아니다 — `machine` 으로 가른다** (2026-08-14)

수집 파이프라인이 처음으로 데이터를 썼다(`item_sources` 0 → 19 · 크롭 801장).
`--collect` 가 원장에 **빈 자리 777개**를 만들어 `ledger_items` 가 608 → **1,385** 가 됐다.

```sql
machine = 'INGEST'   → 수집자리 777   좌표·steps·정답 전부 빈 칸 (분석 전)
machine <> 'INGEST'  → 분석분   608   A·A2·B1·B2·C·D·M1·O1·O2·O3
```

- ⚠️ **`count(*) from ledger_items` 를 "분석 완료 문항 수"로 읽지 말 것.** 옛 글의 608 은
  전부 분석분을 가리킨다. 지금 그 수를 세려면 **`where machine <> 'INGEST'`** 를 붙인다.
- ⚠️ `machine is not null` 로는 안 갈린다 — 수집자리도 `INGEST` 라는 값이 들어간다(실측으로 헛짚었다).
- 수집자리 777 은 전부 `h`·`steps` 가 비어 있다. 분석분에도 빈 것이 있으므로(h 48 · steps 64,
  대성 A2 구멍) **빈 칸 유무로 가르면 틀린다.** 가르는 자는 `machine` 하나뿐이다.

**팩별 구성** — `A 140`(가형·B형 원형) · `A2 64`(대성) · `B1 40` · `B2 40`(OMEGA blue) · `C 108`(나형 원형) · `D 66`(현행 기출) · `M1 30` · `O1 40` · `O2 40` · `O3 40` · **`INGEST 777`(수집자리)**.
계보 **59종 · 5,052쌍**. 좌표 규격위반 0. 도구코드 72종이 실제 사용되고 5문항은 코드 미부여(기초분).

⚠️ **적재 팩은 뷰를 `DROP ... CASCADE` 후 재생성하는데 `security_invoker`를 다시 켜지 않고, RLS 구문도 0건이다.** 팩을 다시 돌릴 때마다 뷰의 `security_invoker`가 풀리므로 **적재 직후 반드시 재적용**할 것 (`run_608.py` 끝에 붙여 뒀다). 적용 후 "RLS 미적용 테이블 0건 · security_invoker 꺼진 뷰 0건" 확인까지가 한 세트다.

- **DB가 두 사고를 구조적으로 막는다** — ① `ans_format` CHECK: 객관식 답을 아라비아 숫자로 넣으면 거부(넣으면 전 문항 오답 처리되던 사고) ② `tool_code` FK: 정규 어휘 밖 코드 거부(코드가 138종으로 갈라진 사고). 둘 다 실제로 위반 INSERT를 던져 거부되는 것을 확인했다.
- **서비스 질의가 실제로 돈다** — 학생이 틀린 문항의 `tool_code`로 원형 문항을 좌표순(쉬운 것부터) 조회. 예: 1049호 → 1108(h2w2) → 943(h2w3) → 946·987(h3w3) → 1721(h4w4).
- ⚠️ **RLS는 이관 SQL에 없어서 따로 켰다.** 신규 5개 테이블 `enable row level security` + 뷰 2개 `security_invoker=true`. 정책 0개 = service_role 전용으로, 기존 15개 테이블과 같은 관행. **뷰는 기본값이 소유자 권한이라 RLS를 우회하므로 `security_invoker`를 반드시 켜야 한다.** 뷰를 새로 만들 때마다 잊지 말 것.
- 이관 패키지는 전부 `ON CONFLICT`/`IF NOT EXISTS` — 재실행 안전, DROP 없음.

### h 좌표 — 규격은 1~3, 정렬은 `h_norm` (2026-08-12 결정)

`h`(은닉깊이) 규격은 **1~3**이다. DB CHECK만 1~4로 열어 둔 것은 원본을 그대로 적재하기 위해서고, **h=4는 규격 위반 기록**이다.

h4 32건이 전부 기계 C에서 나왔다. 4점 문항·같은 도구코드끼리 짝지어 비교하니 **문항이 아니라 평정자가 다른 것**이었다 (이관 SQL 원본 314행에서 재검산, Cowork 수치와 전부 일치):

| 비교 | 가중평균 차 | 겹치는 코드 | 높게 매긴 코드 |
|---|---|---|---|
| C − A | **+0.53** | 19 | 15/19 |
| C − D | **+0.69** | 19 | 15/19 |
| A − D | +0.23 | 10 | 4/10 |

A(2.59)와 D(2.47)는 서로 정합인데 **C(3.17)만 위로 밀려 있다.** 규격을 1~4로 넓히면 한 기계의 눈금을 표준으로 승격시키는 셈이라 좌표가 평정자에 의존하게 된다.

- **원본 `h`는 덮어쓰지 않는다.** 감사·재평정용으로 보존하고, 보정값 `h_norm`(기계 C만 −0.62)을 따로 둔다. 보정 후 A 2.59 / C 2.55 / D 2.47로 맞는다.
- ⚠️ `h_norm`은 **반드시 plain 컬럼**이어야 한다. GENERATED 계산 컬럼으로 만들면 안 된다 — 분업팩 SQL(`11`·`12`)이 `h_norm` 값을 **직접 INSERT**하므로 `cannot insert into generated column`으로 적재가 통째로 깨진다. (2026-08-12에 GENERATED로 바꾸자고 제안했다가 12번 팩을 받고 철회했다. 다행히 실행 전이었다.) 새 행의 NULL 문제는 팩이 값을 직접 넣어 해결되고, 서비스 질의는 `ORDER BY h_norm NULLS LAST`로 방어한다.
- **서비스 정렬은 뷰 `practice_ladder`(h_norm 순)를 쓴다.** 기존 `practice_candidates`는 raw `h`만 노출하므로 정렬용으로 쓰지 말 것.
- 기계 C는 108문항이 **전부 4점**이라, 4점 표본에서 구한 보정을 C 전체에 적용해도 외삽이 아니다.
- h=4인 32건은 `needs_rerate=true`로 표시해 다음 검수 때 재평가한다.
- ⚠️ **근본 대책은 앵커 문항**이다 — 분업팩 마스터프롬프트에 h1/h2/h3 기준 문항을 3개씩 박아 모든 기계가 같은 자를 쓰게 한다. 지금은 "은닉깊이 1~3"이라는 서술뿐이라 해석이 갈렸다. (Cowork 담당)

**`/admin/` 화면 구성** — `show()` 로 세 화면을 전환한다: `main`(학생 목록) · `detail`(개인 상담 리포트) · `cohort`(반별 현황).
- `cohort` 는 `PILOT_CLASSES = ["I1","I2","I5"]` 를 기본 선택하고 전체 반은 체크박스로 켤 수 있다. 파일럿 범위를 넓히려면 이 상수만 고친다.
- 회차를 바꿔도 반 선택은 유지된다. 반 이름에 괄호가 들어가므로(`I11(반수반)`) 토글은 **인덱스 기반**으로 넘긴다 — 문자열 보간 금지.
- 반별 표와 "최고–최저 격차"는 **서버 정렬에 기대지 않고** 화면에서 다시 정렬·계산한다(과거 필터 후 격차가 틀리던 버그).
- 이 페이지는 메인 사이트의 다크 테마가 아니라 **인쇄 친화 밝은 화면**(`--red:#c00`, 표 중심)이다. 위 디자인 체계와 별개이니 이 페이지 관례를 따를 것.

## 개인정보 수정란 (2026-08-12)

파일럿에 `profile` 화면을 추가했다 — 이름·학원·구분·**선택과목**을 고친다. 들어가는 길 두 개: 시험 선택 화면의 `⚙︎ 내 정보` 칩, 마이페이지의 [내 정보] 버튼(`/pilot/?profile=1`).

- 저장은 서버(`POST /profile`) + 로컬(`ds_students`) **양쪽**. 서버가 실패하면 로컬만 저장했다고 화면에 알린다.
- 필수 동의(`consent`)는 여기서 못 바꾼다. 알림·홍보 동의는 백엔드가 받지만 화면엔 아직 안 뺐다.
- ⚠️ **선택과목은 저장한 회차마다 다를 수 있다.** 확통→미적분으로 바꾼 뒤 예전 회차를 열면 21~30번이 다른 과목으로 뒤바뀐다. 그래서 저장된 응답의 `area`로 그 회차의 과목을 되살려 `S._subjOv`에 담고, 화면은 `curSubject()`(=`_subjOv` 우선)를 쓴다. 새 회차를 시작할 때 `_subjOv`를 반드시 `null`로 되돌린다.

## 작업 이력

**2026-09-02** (s1 로컬 창 — 9평 당일 등재)
- **MP27_09 등재·배포**(7e37434) — 9월 모평 기하 제외 38문항, 공식정답에서 ans·jum 자동 삽입
  (페이지 실물 재대조 38/38), 4점 steps 16 동반(본부 분해 기반). CATS 기출 soon 자리 연결(ex:28),
  my EXAM_NAMES 추가. 브라우저 스모크 통과(배점 100/100·tc 전량 TOPIC 존재). 본부 05:25 허가 집행.

**2026-09-01** (본부 클라우드 창 — 화요일 아침 재가동)
- **주말(금~월) 각자 작업 완주 취합** — s1/s2 96커밋·통로 76건: 2016~2020 기출 창 직접전사 **81단위 완주**,
  이승열 기출 분석 가동(산출 gpt 881 수거), 형 실물 검수 체계·눈판정대 상설(잔여 0), 조판·그림 규칙 확정.
  정본 취합: ops `docs/지도/작업지도_주말_0829-31.md` → `델타스킬전체지도_260901.html`(재가동판).
- **본부 아침 등재 집행**: line_outputs 9,139 → enroll_v4 확장(--batch·--normalize-gates·--adjudicate) →
  **원장 3,577(분석분 3,003)** · 갈림 452→278 · exams +9(black) · 보류 227(진단질의 결손 — 재작업 큐, 임의 보정 없음).
- 22:00 지도 정례 재활성화. **내일 9/2 = 9월 모평**(22번 검증 예약 유지).
- **(오후) 눈판정대 용역판(judge-caa33b/su1·su2) 렌더 4갈래 갱신** (형 지시·실물 확인 기준 수능2411-13):
  ① 그림 실물 크기 일괄 — 크롭 PNG 픽셀×72/dpi 를 인쇄 본문 10pt 기준 em 으로 환산(그림 속 글자 ≈
  본문 글자, 약간 큼). ⚠️ **items 사본 크롭 1,400장에 dpi 스탬프가 없다**(96 기본값 597·미기록 774) —
  150 넘는 명시값(200×29장)만 믿고 나머지는 표준 230.4 로 본다 ② 보기 ㄱㄴㄷ 상자(`< 보 기 >` 탭·상하
  0.35em — 오탐 잣대: 줄머리 ㄱ. 둘 이상) ③ 분석 글 `$…$` LaTeX 를 KaTeX 직접 렌더(0901 등재 gpt 라인
  62행 — 기존 유니코드 조각 경로는 `$`·`\` 를 몰라 원문 노출) ④ **수식 단독 줄을 블록(위아래 0.35em)으로
  승격** — 분수·∫ 연속 줄 글자 겹침(형 스크린샷 지적) 구조적 해소.
- **(오후) 서바이벌 5~8회 + 서바이벌 프로 8월 파일럿 등재** (형 지시) — items 정본(SDA-2027-SV05~08 ·
  SDA-2026-M08=2027학년도 8월 서바이벌프로, 반입대장으로 정체 확인)에서 SV27_05~08·SP27_08 로
  38문항×5회차 등재. 검증: 세눈 대조 통과·해설↔정답 2원 대조 152문항 불일치 0·배점 100/100·
  왕복 스모크 190문항 불일치 0·JS 오류 0. 분류는 자동(SU1/SU2 확장)+본부 판독 확정표 33건 —
  다섯 회차 모두 공통 수Ⅰ11·수Ⅱ11 로 갈라짐(독립 검산). tc 없음(DSP-27-M08 선례) · CATS 시대인재
  그룹 9회차 · my EXAM_NAMES +5. ⚠️ **분석(steps)은 다섯 회차 모두 아직 없다**(원장 0행·수거 0) —
  단계 UI는 분석 도착 후. 시드 도구 서바시드.py 는 일회성(스크래치패드).
- **(오후) 용어 학생 어휘 일괄** (형 지시: 조각접합·누적적분·max/min 은 학생·교사 미사용) —
  「조각접합」→구간별 함수(DB tool_codes 계열 4행, 마이그레이션 `2026-09-01_도구계열_학생어휘.sql`) ·
  「누적적분」→정적분으로 표현된 함수(deltamap 6·pilot OMEGA_9 steps 2) · max(aₙ,bₙ)→중 최댓값.
  게이트 단답 대조 불일치 0. ⚠️ 원장 재추출 시 같은 사상을 추출기에 걸 것(원장 원본은 보존).
- **(밤) 설명 전략 확장 + 델타맵 위치 첫 실측** (형 지시 — 전부 ops 리포에 정본) —
  ① 교육방법론 검토·보강안 — 상세는 ops `설명전략-0901.md` §6~7 (⚠️ 메커니즘 상세는 공개
  리포에 적지 않는다 — 아래 작업 규칙 참조). ② 홍보·투자 정본 `방법론수렴-홍보투자-0901.md` + 아티팩트
  「델타스킬과 학습과학」. ③ 해설삼원 설계 `해설삼원-델타스킬해설-0901.md`(공식=정답 기준선 ·
  현장=효율 · 우리=구조 · 형=눈금). ④ **위치타당도 1차**(`scripts/위치타당도.py`): 정답 대조로
  증명된 대응만 이음 — SNG-22~25 76/76 · DSP-27-M04/05 52/52 ✅, **앱 OMEGA↔원장 26학년도 전
  조합 기각**(두 해 회차번호 겹침 재실증 — 코드가 비슷해도 잇지 말 것). 전국 n=77 에서 ★총합↔
  정답률 −0.44 · cost 단조(○강 25.6 < ○중 32.8 < ✗ 40.1) — 좌표 방향의 첫 실측 인증.
  ⚠️ **이을 수 있는 기출 대응분의 h 는 0건** — h 축은 검증 불가 상태(앵커·평정 선행).
  ⚠️→✅ 0901 등재분 `ans` 미주입은 **같은 날 밤 재집행으로 해소** — L행 822 중 채움 819
  (어긋 2·대조불가 1은 규칙대로 비워 목록으로, 도장 64 불변). 분석분 ans 3,017/3,020.
  상세 ops `델타맵-위치판정-0901.md` §4 · 보고 `data/정답주입보고_260901.json`.
- **(심야) 수송tv 채록 2단 전량 완료 + 시장 풀이 지도 승인** (형 지시 "전량 확대 진행해") —
  형 유튜브 2026년분 21영상 자막(1단 s1 수거)에서 **설명 사건 292건** 추출(병렬 5기 · 오류 0 ·
  이중검증 10% "방법 건강" 판정). 형 어휘집 후보 16종은 **형 확인 게이트 뒤에만 등재**(후보 단계).
  추출 규격·분포·다음 걸음은 ops `수송tv-2단규격-0901.md`(발화·어휘 실물은 비공개 리포에만).
  **축D 「시장 풀이 다양성 지도」 승인** — 남의 유튜브 해설은 **판정·라벨·링크만 저장**(전사·발화
  불보존 · 눈금 불반입), 착수는 모평 뒤(ops `해설삼원-델타스킬해설-0901.md` §2 축D).
  이어 **3단 짝짓기 1차**(형 "마감은 아니다" — 해설 영상 8개 ↔ 델타맵/원장 19문항 89단계, 밟음
  93%·관문 부합 6/9, ops `수송tv-관문짝짓기-0901.md`). ⚠️ **원장 DSP-27-M04 미적29 오배정 발각**
  — 분석 서술 여섯 칸이 27번 것(정답 41은 옳음). ⚠️→✅ **형 판정 A안으로 당일 정정 완료**:
  원장 27행 신규 이동 등재(ho 6249)·29행 분석 비움(마이그레이션 `2026-09-01_DSP27M04_mi29_오배정정정.sql`),
  델타맵·rx 배지·진단질의·처방 27로 이동(게이트 193문항 불일치 0 후 배포). 29번 재분석은
  분업 라인 배정(형 결정 "a로 분업자에게"). **형 어휘집 1차 판정 완료**(판정대 경유 — 등재 15·보류 1,
  실물·메모는 ops `data/형어휘집_후보.csv`·`수송tv-2단규격-0901.md` §7에만).

**2026-08-28** (본부 클라우드 창 — 휴면 직전 마지막 기록)
- **명칭 통일 (형 지시)**: 은경=**s1** · 재순=**s2** — 통로 글·보고·창 대화 전부. **폴더·파일 경로는 유지**(개명 금지).
- **본부 휴면 (형 확정)**: 주간한도 97% → 금(8/28)~월 각자 작업, **화(9/1) 아침 본부 재가동**(기상 예약 완료).
  토~월 취합·작업지도·대장 기록은 **s1/s2 담당** — 주말 작업지도는 ops `docs/지도/작업지도_주말_0829-31.md`(신규),
  정본 전체지도는 화요일 본부가 갱신. 기간 중 저장 = 매뉴얼 + **DB 직접 쓰기 금지**(파일로만) + 몰아 저장 금지.
  s2 렌더 산출(문항 원문)은 **형 직접 수령·검토 후 공유 드라이브 `렌더산출_s2/날짜/` 저장**(리포 금지).
- **공정 진행선 기준선 = ops `docs/지도/델타스킬전체지도_260828.html`** — 디지털·저장·분석·교정·렌더·렌더수정
  여섯 축의 "어디까지"를 실측으로 못 박음(원장 2,999 · 분석분 2,425 · line_outputs 8,258 · 갈림 421 ·
  기출 미분석 889/킬러 216 · 렌더 감사 153/153). 배정대장 147행으로 갱신(신규 122~125 · 맥 중지 표기).
- **기출 우선 (형 확정)**: s1이 `분석꾸러미_260828_기출`(킬러 216 먼저) 제작·업로드 → 이승열 착수. s2는
  node+katex@0.18.4 설치 → 신규 전환분 렌더 + 갈림 정답축 187 재검수.

**2026-08-26** (본부 클라우드 창)
- **★ 최대병목 종결 — v4 분석 완성본의 원장 등재.** 완성본 2,463문항(58단위)이 원장에 한 건도
  없던 것(analysis_ver v4 = 0행)을 0823 세라인 등재규칙(형 확정 B안)으로 풀었다:
  **등재 1,503**(L1 1,217·L2 154·L3 132) · 원장 1,269 → **2,772행** · 중복 0.
  갈림 442(도구코드 350·정답 32·둘다 60)는 `line_disputes` 표에 — 다수결로 넣지 않았다.
  기하 402 는 등재복원 원칙대로 보류, 기존 기분석 103행은 덮지 않았다.
- 집행 순서: 마이그레이션(`ledger_enroll_uniq` 부분→**전면 유일** + `line_used` 칸 +
  `line_disputes` 표) → 수거 리포갈래 신설(분석_ 2,553 → `line_outputs`, 총 5,097행/6라인) →
  등재기 `enroll_v4.py`(B안 판정, `merge_gpt.enroll_item` 재사용 — `extra` 인자만 추가,
  selftest 10 + 회귀 80) → dry → 표본 1단위 → 전량. 전 단계 검산 일치.
- 본부 확장 셋 → **형 판정 완료(같은 날 저녁)**: L1/L2 라벨 승인("단일 산출도 당연히 등재") ·
  내용 라인 순위 claude 우선 유지(차후 분석이 늘면 **문항별 복합 분석**으로 재결정, 비채택
  라인은 line_outputs 보존) · 정답 주입 착수 승인.
- **정답 주입 단 집행 완료** (`정답주입_v4.py`) — 원천은 items 정본 answer, 주입 시점 재대조
  (교정 이후 어긋 방지) · **채움 1,502 / 빈칸 1**(형 눈판정 대기 문항) · `ans_source` 칸 신설로
  출처·대조 근거 행마다 명기 · **도장(answer_verified) 0 유지** — 인쇄 증인 계보 실측 후 2차.
  ⚠️ 재대조가 실제로 잡은 둘: 정답표 한 칸 밀림(정본 기교정 확인) · 1단 원문자 표기 오기
  (값 일치 — 본부판정 개별 주입, 자동 규칙은 안 풀었다).

**2026-08-10 ~ 2026-08-19 이력 — `_dev/이력아카이브-2608중.md` 로 이관** (2026-09-01 토큰 다이어트 — 형 승인).
그 기간의 결정·함정 대부분은 위 주제 절과 ops `docs/` 가 정본이다. 아카이브에만 굵게 남은 것:
- **수집·검증 작업을 열면 아카이브의 08-13·08-18·08-19 절부터 읽는다** — zip 은 Python zipfile 만 ·
  PDF 세 부류(엉터리 OCR 레이어) · 시험지 규격 시대 구분 · 문항머리 글꼴 문턱 · verify_math 두 축 ·
  HWP 미주 경계 · 레지스트리 지역구획 · 그림 닻 규칙이 전부 거기 있다(각 항목에 ops docs 링크 포함).
- "문항 텍스트를 밖에서 구해 오기·문제은행 복제 = 안 한다" — 조사 종결(08-13). **재조사 금지.**
- 회차코드 대응표(파일럿↔원장)는 만들 수 없음 — 정답 일치율 전수 대조로 확정(08-18).
- 창 사이 통로·분업·창 소유권 규칙: ops `docs/클로드-분업-0814.md` · `로컬-연결-기록-0813.md`.

## 미해결 — 우선순위 확정 (2026-08-12)

**가르는 기준은 핵심 루프(오답 제출 → 진단 → 처방)를 막느냐다.**

1. 🔴 **DSP 4회차의 단계 분해가 아직 없다** — 608 적재로 대성 64문항(`machine='A2'`, 호수 2500~2590)이 원장에 들어왔지만 **`steps` 0/64 · `diagnostic` 0/64 · 좌표는 16/64뿐**이다(다른 9개 팩은 전부 100%). 검산 ⑤의 "좌표미부여 48"이 바로 이 구멍이다.
   - 지금 된 것: **도구코드 64/64** → `deltamap.json`에 `uses`로 넣어 마이페이지·관리자의 "요구 스킬" 칸이 채워진다(2026-08-12 반영).
   - 아직 안 되는 것: 파일럿의 **"어디까지 갔나요?" 단계 UI**. `steps` 없이는 못 띄운다. **steps를 이 리포에서 지어내지 말 것** — Cowork의 A2 재분석 산출물을 받아 교체한다.
2. 🟠 **등급컷 — 앱 반영 9회차로 확대 (2026-08-19)** — 기존 5회차(학원추정) + **대성 4회차
   (공식 수능예상, 확통/미적분 과목별)**. DB 에는 대성 12회차가 있으나 DSP-26 8회차는 앱에
   시험이 미등재라 내보내지 않는다. 나머지 회차는 여전히 전국 모평 컷 폴백 — 컷 소스 확보는
   계속(운영자: "대성 더프는 검색하면 등급컷 자료가 다 있다" — 새 회차 등재 때 함께 적재).
3. 🟡 **deltamap 32회차 중 11회차가 파일럿 미등재** (GDK22_0·JQ27_01·02·03·0603·0701·0702·MP25_06·MP25_09·SN25_11·SU27_06 — 2026-08-13 전수 확인). 반대로 **`JD27_07` 은 앱에만 있고 맵에 없다** — 처방(스킬맵)용. 배점·유형·정답·tc가 담긴 EXAMS 데이터를 Cowork에서 받아야 한다. #1과 같은 작업 흐름이라 함께 처리한다.
4. 🟡 **가입 인증이 없다 — 사업자등록 후 일괄 처리로 결정** (2026-08-13). 지금은 전화번호+PIN만으로 가입돼 번호 소유 증명이 없다. 남은 위험은 **레거시 12행**(인증 붙기 전 계정 — `student_auth`에 번호가 없어 먼저 가입하는 사람이 임자)이고, 그중 실제 학생 8명이 **본인이 가입하면** 그 번호는 닫힌다. ⚠️ 대신 가입시키지 말 것 — `/enroll`의 `consent`는 본인 동의다. 붙일 방식·작업 범위·개인정보처리방침 수정 사항은 `_dev/계정병합-인증-0812.md` §6.
5. ⚪ **404 페이지 · sitemap lastmod · GA 설치 범위** — **지금 하지 말 것.** 유입·측정 인프라인데 진단이 아직 안 돌아간다. #1·#2가 끝나 학생이 실제로 진단을 받는 상태가 된 뒤에 붙인다.

**Cowork 쪽 진행분(이 리포 작업 아님)**: 가형원형 파편 20개는 **통합이 아니라 하차** — DB `ledger_items` 314행 적재를 확인했으므로(2026-08-12) 게이트 통과, Cowork 프로젝트에서 제거하고 압축 인덱스만 남긴다.

## 작업 규칙

1. 수정 후 `node --check`로 JS 문법 확인 (script 블록 추출해서).
2. main에 직접 커밋·push (별도 브랜치·PR 불필요 — 운영자 1인 리포).
3. 커밋 메시지는 한국어로 무엇을 왜 바꿨는지 한 줄.
4. 분석 데이터·파이프라인 관련 요청이 오면: 그건 Cowork 세션 담당 — 완제품을 받아 교체하거나 원장 DB에서 추출한다.

### 저장은 요청받지 않아도 한다 (2026-08-13)

클라우드 세션의 작업 폴더는 **세션이 끝나면 사라진다.** 운영자가 "저장해줘"라고 말하기를
기다리지 말 것 — 아래는 **일이 끝나는 즉시** 저장한다. 한 덩어리가 끝날 때마다 커밋한다
(마지막에 몰아서 하지 않는다 — 세션이 중간에 끊기면 그만큼이 사라진다).

| 무엇을 했나 | 어디에 | 언제 |
|---|---|---|
| 홈페이지 파일 수정 | **deltaskillhomepage** main | 문법 확인 직후 (push = 배포) |
| DB 변경 (DDL·데이터 이동·함수) | **deltaskill-ops** `db/migrations/YYYY-MM-DD_설명.sql` | **실행 전에 파일로 먼저** |
| Edge Function 배포 | **deltaskill-ops** `backend/` (소스 + 롤백 번들) | 배포 직후 |
| 재사용할 스크립트 | **deltaskill-ops** `scripts/` | 검증이 끝나면 |
| 결정·사고 기록 | **deltaskillhomepage** `CLAUDE.md` · `_dev/*.md` | 결론이 났을 때 |

### ⚠️ 커밋했다 ≠ 기록했다 (2026-08-13 원칙화)

**새 창은 `git log` 도, 코드 주석도 읽지 않는다.** 자동으로 읽히는 것은 `CLAUDE.md` 뿐이고,
`docs/*.md` 는 누가 가리켜 줘야 열린다. 그래서 **커밋 메시지와 주석에만 남긴 지식은 사라진 것과 같다.**

실제로 그랬다 — 2026-08-13 오후 Windows 함정 여섯을 전부 커밋·주석에 남겼는데
`CLAUDE.md`·`docs/` 에는 하나도 없어서, 새 창이 열리면 같은 벽에 다시 부딪힐 상태였다.

**한 덩어리가 끝나면 세 곳을 같이 갱신한다. 마지막에 몰아서 하지 않는다.**

| 무엇을 알게 됐나 | 어디에 적나 |
|---|---|
| **다음 창이 모르면 다시 데일 것** (함정·사고·환경 제약) | **`CLAUDE.md` 작업 이력** — 한 줄 요약 + ⚠️ 대책, 상세는 `docs/` 링크 |
| 결론이 난 **결정**(무엇을 왜 그렇게 정했나) | `CLAUDE.md` 해당 절을 **고친다**. 낡은 수치·이름을 남겨 두지 않는다 |
| 재현·검증 과정의 **상세** | `deltaskill-ops/docs/주제-날짜.md` |
| 그 파일을 여는 사람만 알면 되는 것 | 코드 주석 |

**판단 한 줄: "새 창이 이걸 모르면 손해를 보나?" → 그렇다면 `CLAUDE.md`.**

- ⚠️ **낡은 수치를 방치하지 않는다.** 고칠 때 옛 숫자를 함께 고친다
  (미등재 회차가 6→11로 바뀐 걸 두 문서가 모르고 있었다).
- ⚠️ **문서가 서로 어긋나면 어느 쪽이 정본인지 명시한다.** 낡은 문서 머리에 경고를 단다
  (`docs/코워크-0812문서-정오표.md` 가 그 예다).

- **📋 눈판정대·눈대조판을 요구받으면 `deltaskill-ops/docs/눈판정대-프로세스-0902.md` 형식으로 만든다**
  (형 확정 0902 — 좌측 번호 격자·원본 스케일 좌우/위아래 비교·수정의견란·판정 저장.
  빌더는 ops `scripts/judge_app/` 개조, 새로 짓지 않는다).
- **📋 진행 중 작전(8/20~9/1): `deltaskill-ops/docs/델타맵-13일작전-0819.md`** — 목표·라인·일일 리듬·
  본부 토큰 원칙이 거기 있다. 분업 체계는 `작전본부-분업체계-0819.md`, 9/2 당일은 `9월모평-작전-0819.md`.
- **📋 새 창은 이것부터: `deltaskill-ops/docs/새창-시작하기-0818.md`** — **진행 중인 작업**·
  창 넷의 분업·맥북/드라이브 배치·저장위치·숫자·막힌 것·다음 한 걸음이 한 장에 있다.
  **이 문서와 CLAUDE.md 둘만 읽으면 시작할 수 있다.** (앞판 `docs/전체현황-0817.md` 는 8/17 기준)
- **두 리포다.** `deltaskillhomepage`(공개) = 홈페이지 코드. `deltaskill-ops`(비공개) = 백엔드
  소스·DB 마이그레이션·스크립트·live2 평문 원본. 공개 리포에 못 넣는 것이 전부 ops로 간다.
  → `github.com/songjae94/deltaskill-ops` · 그 리포의 규칙은 `deltaskill-ops/CLAUDE.md`.
- **`deltamap.json` 을 건드렸으면 배포 전에 게이트를 돌린다.**
  `python3 ../deltaskill-ops/scripts/check_steps_vs_answers.py .`
  앱 정답표와 맵 마지막 step을 단답형에 한해 대조한다(현재 172문항·불일치 0).
  객관식은 기계로 못 잡으니 사설 팩은 **원본 이미지 재대조**가 따로 필요하다.
- ⚠️ **학생 개인정보와 키는 어느 리포에도 넣지 않는다.** 개인정보는 Supabase 한 곳,
  키는 환경변수·시크릿으로만. 커밋 전에 전화번호·이름·`sbp_`·JWT 패턴을 훑는다.
- ⚠️ **새 발명 메커니즘·눈금 수치는 공개 리포(이 파일 포함)에 먼저 적지 않는다** (2026-09-01) —
  이 리포는 공개라 여기 적는 순간 특허의 신규성 시계가 돈다(**출원이 기록보다 먼저다**).
  상세는 ops 비공개 문서로, 여기엔 무딘 요지와 ops 포인터만. 근거: ops `docs/특허출원전략-0901.md` §6·§8.
- 스크래치패드 중간물은 **다 올리지 않는다** — 다시 만들기 어렵거나 재사용할 것만 고른다.
  스크린샷·일회성 검증 스크립트·중간 JSON은 버린다.
- ⚠️ **DB는 파일로 먼저 남기고 실행한다.** 결과는 백업이 지켜 주지만 *이유*는 안 남는다.
  마이그레이션 파일 머리에 **왜 · 선행 조건 · 실행일**을 주석으로 적는다.
