# 델타스킬 홈페이지 (deltaskill.co.kr) — 관문 (3층 r1, 형 승인 2026-09-03)

수학 학습 AI '델타스킬'의 홈페이지. GitHub Pages — **main push = 곧 배포.**

> **이 파일은 「찾는 법·규격·금지선」만 담는다.** 상세·이력·사고 전문은
> **`_dev/판례집-홈페이지CLAUDE-0903.md`**(옛 CLAUDE.md 동결본 — 절 제목 그대로 Grep) ·
> 운영 규칙 전반은 `deltaskill-ops/CLAUDE.md`(관문)와 그 매핑표·`docs/운영편람-0903.md`.
> ⚖️ **정본 소유는 형** — 개정은 형 오케이로만. **재비대 방지**: 새 절 금지 — 새 지식은
> 함정 한 줄 + 상세는 ops `docs/`·`_dev/`. **상시 본부는 없다** — 각 창이 지침을 읽고 일한다.
> 📍 오늘 상태 한 장 = ops `docs/지도/작업지도_260903_현황판.md` (수치는 여기 안 적는다).

## 최종 목표 (판단이 갈리면 이 문장으로 돌아온다)

**"인간이 수학을 이해하는 방식을 가장 잘 아는 기업."**
문항은 도구다 — 자산은 **사람이 어디서 막히고 무엇으로 넘어가는가**의 기록(델타맵 좌표·단계·
관문·진단질의·처방)이다. 우선순위 잣대: **그 작업이 '이해의 지도'를 더 정밀하게 만드는가.**

## 이름 — 여기서만 정한다

| 이름 | 무엇 | 어디 |
|---|---|---|
| **델타맵** | 문항을 어떻게 푸는가(steps·uses·좌표) | `deltamap.json` |
| **도구코드** | 정규 어휘 78종 — 유일 코드 집합 | DB `tool_codes` |
| **원장** | 문항별 좌표·관문·진단질의·계보 | DB `ledger_items` |
| 교재 스킬 | 아직 없다 — 생기면 `textbook_skills.json` | `skills`라는 이름 재사용 금지 |

⚠️ `skills.json`은 삭제됐다 — 옛 문서에서 보이면 **델타맵으로 읽는다**.

## 구조

`index.html` 랜딩 · `pilot/` **AI 오답노트 파일럿**(핵심 앱) · `my/` 내 오답노트 ·
`cuts.json` 등급컷(`gen_cuts_json.py`로만) · `deltamap.json` 델타맵 · `rx.json` 연습 처방(원장
추출로만) · `admin/` `report/` `partners/` · `privacy/` `terms/`(**동의·개인정보 문구 임의 수정 금지**).

## 코드 관례

- 단일 HTML SPA(CSS·JS 인라인, 빌드 없음). 상태: pilot `S` · my `SG`, 전환은 `S.screen`+`render()`.
- localStorage: `ds_phone`·`ds_students`·`ds_tk`·`ds_tk_ph`(**토큰 저장·삭제 시 항상 짝으로** —
  빠뜨리면 `need_pin` 버그)·`ds_subs`·`ds_subid`.
- 백엔드 = Supabase Edge Function(`CONFIG.api`) — 소스는 ops `backend/`.
- ⚠️ 전역 `input{width:100%}` — 체크박스류는 개별 오버라이드.
- 디자인: **색 3계열뿐**(파랑 `--brand`/`--brandInk` · 코랄 하나 · 초록 `--ok`) — 새 색 금지,
  대비 4.5 확인. 밝은 화면에서 `#5aa2ff` 글자 금지(`--brandInk`로). 테마 전환은 `body.qlight`+
  `colorScheme`+`theme-color` **셋 함께**(하나 빠지면 모바일 다크가 색을 뭉갠다).

## deltamap.json 규격 (델타맵)

```
{ "회차코드": { "트랙": { "문항번호": { "steps":[...], "uses":[...], "anchors":[...],
    "cost":"○강|○중|✗", "cand":true, "dq":{q,ch,a,why} } } } }
```

- 원장 DB에서 **추출로만** 갱신 — **이 리포에서 steps를 지어내지 않는다**(분석은 분석 창 몫).
- 트랙: 공통/확통/미적분 — **기하는 등재 금지**(제거 완료·`EXCLUDED_TRACKS` 강제).
- `cand:true` = 스킬 후보(○강). `uses` 객체는 `useName()`을 거친다(`esc()` 직행 금지).
- `dq` 진단질의: 원장 형식은 정답이 항상 (A) — 화면에서 고정 순열로 섞고 라벨 번역. 데이터는 그대로.
- ⚠️ **앱 `EXAMS[].code`와 맵 키가 다르면 해설이 통째로 안 뜬다** — 회차 추가 때 먼저 대조,
  별칭은 둘 다 보존. `steps` 빈 배열은 안전(단계 UI만 건너뜀 — 부분 등재 가능).
- 배포 게이트: `python3 ../deltaskill-ops/scripts/check_steps_vs_answers.py .` 불일치 0 +
  `node --check`(script 추출). 객관식·사설 팩은 원본 이미지 재대조 별도.

## 회차코드 체계 — `{SRC}-{학년도2}-{KEY}`

**발급·신규 발행처 정본 = `deltaskill-ops/docs/회차코드-등록-원칙-0903.md`**(형 분류 5갈래:
퀄=MEG·시대=SDA·이투스=ETS·THE27=DSP·개인강사=PRV 강사키 · 알파=MEG-AL · 절차 6단계).
여기엔 앱·DB 쪽 함정만:

- **YY = 학년도 = 시행년+1**(`2026년 3월`→27) — 가장 자주 난 사고. 맨 연도(`2027_서바_01`)는
  학년도로 읽는 관례(시행년 표기 발행처면 한 해 어긋남 — `--apply` 전 눈 확인).
- **회차물은 월을 버리고 회차번호로**(`R05`) — 월·회차 함께 있으면 회차가 이긴다. 회차 근거는
  `N회`·이름 맨 끝 숫자만(아무 숫자 줍기 금지). 연도는 폴더에서 빌리되 **회차는 폴더에서 안 빌린다**.
- **MO 시작 파일 = 대성 원본**(끝 두 자리 시행월 — `MO263…03`=DSP-27-M03). 다시 묻지 말 것.
- 옛 코드는 안 지운다 — `exam_aliases` 치환표. `exams` 등재는 마이그레이션 파일로만(자동 생성 금지).
- **코드는 ASCII다 — 한글 정규식으로 코드를 찾지 말 것**(형별 회차 `EDU-05-M03-ga`를 한글로 훑어
  「없다」 오진한 판례). 옛 형별 회차(가/나/A/B형)는 `edition`+`parent_code` 체계가 이미 있다 —
  형별 회차의 기존 행 열쇠는 번호만으로 찾는다(한 시험지 한 트랙 — 과목 열쇠로 찾으면 두 행 사고).
- 앱 회차코드 예: `MP27_06` `HP26_03` `SV27_01`. 파일럿↔원장 대응표는 없다 — 정답 일치율 전수 대조.

## 오답노트 두 갈래 (파일럿)

`S.mode="grade"`(답 입력→채점) / `"note"`(틀린 번호만 탭). `S.mark`: `"x"` 틀림·`"g"` 찍맞.
**저장은 맞은 문항까지 30개 전부**(`result:"o"` — 안 그러면 리포트가 깨진다). 찍맞은
`result:"o"`+`confidence:"찍음"`. 오답 원인 5종(개념·계산·접근·시간·아는데실수).

## 작업 규칙

1. 수정 후 `node --check`(script 추출) → main 직접 push(=배포) → 커밋 메시지는 한국어 한 줄.
2. **저장은 요청받지 않아도, 덩어리 끝날 때마다**: 홈페이지→이 리포 · DB 변경→ops
   `db/migrations/`(실행 전 파일 먼저) · Edge Function→ops `backend/` · 스크립트→ops `scripts/`.
3. **커밋했다 ≠ 기록했다** — 다음 창이 데일 것은 함정 한 줄로 여기(재비대 방지 준수), 상세는
   ops `docs/`. 낡은 수치 방치 금지 · 문서 어긋나면 정본 명시.
4. ⚠️ **개인정보·키는 어느 리포에도 금지**(커밋 전 전화번호·`sbp_`·JWT 훑기) ·
   **새 발명 메커니즘·눈금 수치는 공개 리포에 먼저 적지 않는다**(특허 신규성 — 출원이 기록보다
   먼저. ops `docs/특허출원전략-0901.md` §6·§8) · 분석 데이터 요청은 분석 창 몫(완제품 교체만).
5. ⚠️ **DB 만지는 클라우드 창은 리포 셋을 붙인다**(ops·homepage·`deltaskill-items`) — items 없이
   돌리면 정답 주입은 전건 「정본없음」, 등재는 **선지 대조 층이 조용히 꺼진 채 성공**한다(제5조).
   수거기는 `--batch`를 명시한다(배치명이 폴더 이름으로 뭉개진 판례).
6. 눈판정대 요구 시 `deltaskill-ops/docs/눈판정대-프로세스-0902.md` 형식(빌더는 ops
   `scripts/judge_app/` 개조 — 새로 짓지 않는다).

## 상세·이력이 필요할 때 (판례집 절 제목)

백엔드 구조 · 기관용(파일럿2) 데이터 · `/pilot2/live2/`(평문 원본은 ops — 공개 이동 금지) ·
원장 DB 적재·현재값 이력 · 개인정보 수정란 · 작업 이력(0812~0903) · 미해결 우선순위 → 전부
`_dev/판례집-홈페이지CLAUDE-0903.md` 같은 절 제목으로.
