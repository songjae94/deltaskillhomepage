# 델타스킬 홈페이지 (deltaskill.co.kr)

수학 학습 AI '델타스킬'의 홈페이지. GitHub Pages로 배포되며 **main에 push하면 곧 배포**된다.
운영자: 송재형 (형). 이 리포는 프론트엔드 전용 — 정적 단일 파일 페이지들이다.

## 최종 목표 (2026-08-18 운영자 — 판단이 갈리면 이 문장으로 돌아온다)

**"인간이 수학을 이해하는 방식을 가장 잘 아는 기업."**

- 문항은 도구다. 자산은 **사람이 어디서 막히고 무엇으로 넘어가는가**의 기록이다 —
  델타맵 좌표·단계·관문·진단질의·처방이 본체고, 문제은행은 측정기다.
- 그래서 남의 문항을 모으는 회사와는 길이 갈린다: 문항 원문은 저작권에 묶이지만
  **이해의 지도는 우리 저작물**이고, 그 지도가 촘촘해지면 문항은 생성으로 대체된다(지시 7).
- 우선순위가 갈릴 때의 잣대: **그 작업이 '이해의 지도'를 더 정밀하게 만드는가.**

## 이름 — 여기서만 정한다 (2026-08-13)

**"스킬맵"이라는 말은 쓰지 않는다.** 그 한 단어가 서로 다른 두 층을 가리켜 계속 혼선이 났다.
층마다 이름을 하나씩만 쓴다.

| 부르는 이름 | 무엇 | 어디 |
|---|---|---|
| **델타맵** | 문항을 **어떻게 푸는가** — 단계(`steps`)·도구코드(`uses`)·좌표 | `deltamap.json` (32회차 1,088문항) |
| **도구코드** | 정규 어휘 78종 — 원장이 참조하는 유일한 코드 집합 | 원장 DB `tool_codes` |
| **원장** | 문항별 델타 좌표·관문·진단질의·계보 | 원장 DB `ledger_items` (608) |
| **교재 스킬** | 교재 기준 스킬 체계 — **아직 없다** | 재료가 생기면 `textbook_skills.json` 로 만든다 |

- ⚠️ **`skills.json` 은 삭제됐다**(2026-08-13). 2026-08-12에 델타맵으로 개명하면서 빈 `{}` 만
  남겨 뒀는데, 아무도 읽지 않으면서 이름만 겹쳐 혼선을 키웠다. 교재 스킬이 생기면
  **`skills` 라는 모호한 이름을 다시 쓰지 않는다.**
- ⚠️ **개명 이전 문서·주석은 `skills.json` 을 "문항 데이터"라는 지금과 정반대 뜻으로 쓴다.**
  `_dev/백엔드-인수인계-0812.md` · `_dev/파일럿2-정리.md` · ops 리포의 `backend/*.ts` 주석이 그렇다.
  옛 글에서 `skills.json` 을 보면 **델타맵으로 읽으면 된다.**

## 구조

| 경로 | 역할 |
|---|---|
| `index.html` | 랜딩 페이지 |
| `pilot/index.html` | **AI 오답노트 파일럿** (핵심 앱 — 답 입력·채점·오답 진단·사진 첨부) |
| `my/index.html` | 내 오답노트 (로그인·회원가입·결과 조회) |
| `cuts.json` | **회차별 등급컷** — `exam_cuts` 테이블에서 뽑은 것. `my`가 읽는다 |
| `deltamap.json` | **델타맵** — 회차별 문항의 단계·도구코드·스킬 후보 (아래 규격) |
| `rx.json` | **연습 처방** — 문항→도구코드→원형 기출 사다리. 원장 DB에서 추출로만 갱신. `my`가 읽는다 |
| `admin/` `report/` `partners/` | 관리자·리포트·파트너 페이지 |
| `privacy/` `terms/` | 법적 고지 (**동의 문구·개인정보 문구는 임의 수정 금지**) |

## 코드 관례

- 페이지는 **단일 HTML 파일 SPA** — CSS·JS 전부 인라인. 빌드 없음.
- 상태 객체: pilot은 `S`, my는 `SG`. 화면 전환은 `S.screen` + `render()`.
- localStorage 키: `ds_phone`(전화번호), `ds_students`(프로필), `ds_tk`(토큰), `ds_tk_ph`(**토큰이 어느 번호의 것인지** — 불일치하면 토큰 폐기), `ds_subs`(데모 기록), `ds_subid`(회차→submission_id 매핑).
- 백엔드: Supabase Edge Function (`CONFIG.api`). 엔드포인트: `/enroll` `/login` `/revoke` `/submit` `/submit-update` `/history` `/submission` `/profile` `/photo` `/photo-upload`. 백엔드 코드는 이 리포에 없음(Supabase에 직접 배포, 버전관리 안 됨).
- ⚠️ 전역 `input{width:100%}`가 있어 체크박스 등에는 개별 오버라이드 필요 (과거 버그 원인).
- ⚠️ **토큰을 저장하는 모든 곳은 `ds_tk_ph`도 함께 저장·삭제할 것.** 빠뜨리면 파일럿 저장이 `need_pin`으로 막힌다 (실제 발생했던 버그).

## 디자인 체계 (2026-08-11 확정)

**색은 세 계열뿐이다. 새 색을 추가하지 말 것.**

| 계열 | 값 | 쓰임 |
|---|---|---|
| 파랑 | `--brand #5aa2ff` / `--brandInk` / `--brandSoft` | 브랜드·액션·강조 |
| 코랄 | `#f0806c` (밝은 화면 `#c14933`) | 오답·주의 — **이 하나뿐** |
| 초록 | `--ok #5fd39a` | 정답·완료 |

- **코랄은 한 가지만 쓴다.** 틀림/찍어서 맞음은 색이 아니라 **채움 vs 테두리**로 구분한다
  (`.gn.x`=채움, `.gn.g`=2px 테두리·배경 없음). 예전엔 빨강+주황 2색이었고 서로 13°밖에 안 떨어져 뭉쳐 보였다.
- **파랑은 밝기로 위계를 만든다.** 주 동작만 꽉 찬 `--brand`, 카드 안 단계 버튼은 `--brandSoft` 배경 + `--brandInk` 글자.
- **밝은 화면(qlight)에서 `#5aa2ff`를 글자·테두리로 쓰지 말 것** — 흰 배경 대비 2.61로 기준 미달.
  반드시 `--brandInk`(qlight에서 `#1c4fa0`)를 쓴다. 변수는 테마에 따라 자동으로 바뀐다.
- 새 색을 넣기 전에 **대비 4.5 이상**인지 확인한다 (밝은 화면은 `#fff`, 어두운 화면은 `#05060f` 기준).

**테마 전환** — `quiz`·`note` 화면만 밝고(`body.qlight`) 나머지는 어둡다.
`render()`가 `body.qlight` + `documentElement.style.colorScheme` + `theme-color` 메타를 **함께** 바꾼다.
⚠️ 루트 `color-scheme`을 안 바꾸면 모바일 브라우저(삼성 인터넷 등) 다크 모드가 밝은 화면을 강제로 어둡게 칠해 색이 뭉개진다 (실제 발생).

## deltamap.json 규격 (델타맵)

```
{ "회차코드": { "트랙": { "문항번호": {
    "steps": [...], "uses": [...], "anchors": [...],
    "cost": "○강|○중|✗", "cand": true } } } }
```

문항을 **어떻게 푸는가**(단계·도구코드·좌표)를 담는다. 원장 DB에서 나오고,
세 페이지(`pilot`·`my`·`admin`)가 전부 이 파일 하나를 읽는다. 층 구분은 맨 위 **이름** 절 참고.

- **`cand: true` = 스킬 후보.** 원장의 `COST`가 `○강`인 문항 — 알맞은 도구를 쓰면 풀이가 크게 줄어드는 문항이라 **교재 스킬로 뽑을 1순위**다. `my`·`admin`의 "요구 스킬" 칸에 배지로 뜬다. (전체 608문항 중 ○강 423 · ○중 125 · 미부여 48 · ✗ 12)
- `uses` 항목은 문자열이거나 `{skill, step}` 객체다. **객체를 그대로 `esc()`하면 `[object Object]`가 찍힌다** — `useName()`을 거칠 것 (admin에서 실제로 전 회차가 그렇게 깨져 있었다, 2026-08-12 수정).
- **`dq` = 진단질의** (2026-08-19 추가, 대성 4회차 64문항) — `{q, ch:[3], a:"A|B|C", why}`.
  원장 `diagnostic` 텍스트를 구조화 파싱해 추출한 것. 파일럿 오답 카드가 자가확인 UI로 띄운다.
  ⚠️ **원장 형식은 정답이 항상 (A)다** — 화면에서 문항번호 기반 고정 순열로 섞고,
  `why` 의 원본 라벨(B는/C는)도 표시 기호(①②③)로 함께 번역한다. 데이터는 원장 순서 그대로 둔다.
  ⚠️ 파싱 때 선택지 속 수학 표기 `n(A)` 가 보기 마커로 오인된다 — 공백 뒤 마커만 경계로 삼는다.
- ⚠️ **회차코드가 앱과 맵에서 어긋나면 해설이 통째로 안 뜬다** — 수능이 앱은 `SN26`, 맵은 `SN26_11`이라 한 번도 주입되지 않았다(2026-08-12 별칭 추가로 복구, 둘 다 보존). 회차를 추가할 때 `EXAMS[].code`와 맵 키가 같은지 먼저 확인할 것.
- 파일럿은 `steps`만 읽는다. `steps`가 빈 배열이면 "어디까지 갔나요?" 단계 UI를 조용히 건너뛴다 — 부분 등재가 안전하다.

- 트랙: `공통` / `확통` / `미적분` — **기하는 등재 금지**. 2026-08-12 저녁에 기하 104문항(13회차)을 실제로 제거해 이제 잔존 0이다 (분석 제외 원칙). 기관용 `admin-api`도 같은 규칙을 `EXCLUDED_TRACKS`로 강제한다.
- 회차코드 예: `MP27_06`(평가원), `HP26_03`(학평), `OMEGA_5`, `SV27_01`(서바), `JQ27_02`(지QUEL).
- **내용(steps 등)은 수정하지 말 것** — 분석·검수는 Cowork(분석 세션)에서 하고, 완성본 교체 또는 원장 DB에서 추출로만 갱신한다. 이 리포에서 steps를 지어내지 않는다.

## 오답노트 두 갈래 (2026-08-11)

시험을 고르면 인트로에서 **두 카드 중 하나를 눌러** 시작한다 (하단 버튼 없음).

| 갈래 | `S.mode` | 화면 | 내용 |
|---|---|---|---|
| 채점부터 하기 | `"grade"` | `quiz` | 30문항 답 입력 → 자동 채점 → 틀린 문항 원인 |
| 채점은 했어요 · 오답만 체크 | `"note"` | `note` | 답 입력 없음. 틀린 번호만 탭 → 점수 자동 계산 |

- `S.mark[문항] = "x"`(틀림) / `"g"`(찍어서 맞음). 점수 = 100 − 틀린 문항 배점 합.
- 번호 탭 → 카드 하나만 펼침(`S.open`) → 상태·원인 입력 → [입력 완료]로 접힘. 번호나 접힌 줄을 다시 탭하면 수정.
- **저장할 때 맞은 문항까지 30개 전부 보낸다** (`my_answer:""`, `result:"o"`). 안 그러면 마이페이지 영역별 정답률·리포트가 깨진다.
- 찍어서 맞음은 `result:"o"` + `confidence:"찍음"`으로 저장 — **백엔드 스키마 변경 없이** 표현된다.
- 수정 진입 시 `my_answer` 유무로 어느 화면을 열지 자동 판별한다.
- 오답 원인은 5개: 개념 모름 · 계산 실수 · 접근 안 떠오름 · 시간 부족 · 아는데 실수
  ("안 배움"은 "개념 모름"과 중복이라 제거. 단 기존 기록용으로 `REASONFIX` 매핑은 남겨 둠).

## 백엔드 구조 (2026-08-12 확인)

- Edge Function `api`(**v8**, 파일명 `deltaskill_api_index_v8.ts`)는 **supabase-js(PostgREST REST) + service_role 키**로 DB에 접근 — Postgres 직결(5432/6543) 아님. 클라이언트는 모듈 스코프 전역 1회 생성이라 연결 고갈 위험 없음. 롤백본은 `api_v7_rollback.eszip`(운영자 PC로 옮겨 둘 것).
- **소유 판정은 student_id가 아니라 전화번호다.** `my_history`·`my_submission`·`/submit-update`가 모두 `students.phone` 으로 매칭한다 — 기기 변경·재가입으로 student_id가 갈라져도 이력이 보인다. 반대로 `review_items`(복습 원장)는 `student_id` 로만 묶이므로 계정이 갈라지면 **복습 카드만 쪼개진다.**
- 인증: 자체 토큰의 SHA-256 해시를 RPC `auth_verify`로 검증. `auth_enroll`·`auth_login`(레이트리밋 포함)·`auth_revoke_all`·`my_history`·`my_submission`·`my_photo_ok` RPC 사용.
### 같은 번호로 계정이 갈라진 사고 (2026-08-12 해결)

> 전체 기록(병합 되돌리는 법 · 프론트 수정 내역 · 검증 · 인증 조사): **`_dev/계정병합-인증-0812.md`**

마이페이지에서 [수정하기]를 눌렀는데 "먼저 로그인해 주세요"가 뜨고, 다시 입력했더니 **같은 번호로 학생이 하나 더 생기던** 문제. 원인이 앞뒤로 두 겹이었다.

| 층 | 무엇이 잘못됐나 |
|---|---|
| 프론트 | `/my/` 로그인이 `ds_students`(로컬 프로필)를 저장하지 않았다 → 파일럿이 프로필을 못 찾아 **가입 화면**을 다시 띄웠다 |
| 백엔드 | `/enroll`이 `students` 행을 **먼저 INSERT** 한 뒤 `auth_enroll`을 불렀다. 이미 등록된 번호면 `already_enrolled`(409)로 막히는데 **방금 만든 행은 롤백되지 않았다** → 빈 유령 계정이 쌓임 |

- 피해: 7개 번호 11행(빈 행 8 · 데이터 있는 행 3). 제출 이력은 `my_history`가 전화번호로 매칭해 계속 보였지만 **복습 카드(`review_items` 12장)는 student_id 기준이라 갈라져 있었다.**
- 병합: `student_auth`가 가리키는 행을 정본으로 삼아 `submissions`·`review_items`·`auth_tokens`를 옮기고 중복 행에 `merged_into`를 표시했다(**삭제하지 않는다** — FK·감사 보존). 충돌 0건. 원본은 `students_merge_log`에 `to_jsonb` 통째로 남겼다.
- 재발 방지 **세 겹**:
  1. DB — `students_phone_active_uniq` 부분 UNIQUE 인덱스 `on students(phone) where merged_into is null`. 병합된 행은 예외라 인덱스가 유지된다.
  2. 백엔드 v8 — `/enroll`이 살아 있는 같은 번호 행을 먼저 찾아 **재사용**하고, `auth_enroll`이 실패하면 이번에 만든 행을 **보상 삭제**한다. `already_enrolled`(409)는 그대로 유지 — 프론트의 `/login` 폴백이 이 문자열에 걸려 있다.
  3. 프론트 — `/my/`가 로그인·가입·재진입 때 `ds_students`를 저장하고, 파일럿은 로컬 프로필이 없어도 토큰이 있으면 `/profile`에서 받아 복구한다(부팅·[시작하기]·`?edit=`·`?profile=` 네 경로 전부).
- ⚠️ **토큰만 있고 프로필이 없다고 가입 화면을 띄우지 말 것.** 그게 이 사고의 방아쇠였다. 진짜 신규(토큰 없음)만 가입으로 보낸다.

- **`review_items` 테이블(간격반복 복습 원장)이 `responses.id`를 FK(NO ACTION)로 참조** — responses를 함부로 DELETE하면 막히거나 복습 이력이 끊긴다. `/submit-update`가 UPSERT인 이유.
- `responses(submission_id, item_no)` unique index `responses_sub_item_uniq` 존재 (UPSERT 전제조건).
- 프로젝트: `deltaskill-pilot`(ref cvlrphtupjmaefahodgz, 서울 ap-northeast-2). 조직은 **Pro 플랜**(2026-08-12 업그레이드) — 일일 자동 백업 7일 보관(매일 KST 0시 33분경), PITR은 미사용. 같은 조직의 `deltaskill` 프로젝트는 옛것(일시정지 상태 — resume하면 과금되니 그대로 둘 것).

## 기관용(파일럿2) 데이터 — 2026-08-12 확인

**`admin-api`(Edge Function, v4)가 이미 기관 백엔드의 절반이다.** `admins(role, academy)` + `admin_sessions`(토큰 해시·12시간 만료) + `admin_login`·`rl_hit` RPC + **서버측 학원 범위 강제**(`role='academy'`면 `students.academy` 밖을 못 봄). 관리자 계정은 현재 **1개, `role='owner'`**(운영자 본인).

| 테이블 | 내용 | 읽는 액션 |
|---|---|---|
| `official_scores` | **운영자가 강사로서 보유한 학원 성적 명단.** 623행(OMEGA_5·6·7·8 I반) + JEONDAE_7 218행(일산) + JEONDAE_8 332행(신촌) + **신촌 9회차 1,825행(2026-08-19 — 성적일람표에서 수학 응시자만: DSP-27-M05 202·JEONDAE_7 210·OMEGA_1/3~8 1,413. `class`='신촌 NN반')**. 지점은 `class` 접두로 가른다(I반/일산/신촌). ✅ **같은 시험은 지점 합산이 더 정확하다**(형 확인 2026-08-19) — JEONDAE_7 은 일산+신촌 428명 합산으로 평균 66.1. ⚠️ 신촌 오메가 5~8 = I반과 같은 시험(정답 238문항 대조 0 불일치로 확정 — 두 해 회차번호 겹침 함정 검사함). 등급: DSP-27-M05 만 공식 수능예상 컷으로 채움, 오메가·전대실모는 null(I반 학원추정컷을 신촌에 적용 금지). 신촌 9회차는 `student_no` 없음(일람표에 학번 칸 없음) | `cohort` |
| `item_stats` | 문항별 `correct_rate` + `c1`~`c5`(**선택지별 선택률 — 정답 포함**). **모집단 3종을 `source` 로 가른다: NULL=운영자 학원 I반 · `'SINCHON'`=신촌메가 · `'KCSAT-ML'`=전국 공식.** OMEGA_5~9 확통·미적분 298행(NULL) + KCSAT-ML 322행(수능 2014~2025, 기하 제외) + **SINCHON 660행(2026-08-19 완성 — 메대프 4·5월, 전대실모 7·8월, 오메가 2~8회 × 각 60)**. ✅ **admin-api v6 배포(2026-08-19)** — `items` 액션이 source 를 가른다(미지정=NULL 우선 · `source:"SINCHON"` 지정 · 응답에 source/sources 명시). ⚠️ 이 적재를 위해 **item_stats PK 를 source 포함 유니크(NULLS NOT DISTINCT)로 변경** — 옛 PK (exam,subject,item_no) 가 두 모집단의 공존을 막고 있었다(409 실측, 마이그레이션 2026-08-19_item_stats_pk_source포함.sql). 섞어 밴드를 매기지 말 것 | `items` |
| `review_tokens`·`notify_log` | 미사용(0행) | — |

**admin-api v5 (2026-08-12 배포) — 추가된 액션 2개, 둘 다 `role='owner'` 전용(403으로 차단):**

- **`cohort`** `{exam_code?, prev_exam_code?, classes?[]}` → 회차·반 목록, 반별 집계(인원·평균·SD·평균등급·선택과목 분포), 반×회차 추이, 알림(직전 회차 대비 **−10점 이상 급락** / 미응시). 회차 생략 시 최신 회차.
- **`items`** `{exam_code, subject?}` → 문항별 정답률·정답·선택지 분포. **단답형은 `objective:false`** 로 표시하고 오답 분포를 만들지 않는다(c1~c5가 전부 0이라 만들면 거짓이 된다). 객관식은 `top_wrong`(최다 오답 선택지)을 준다.
- **기하 제외**: 상수 `EXCLUDED_TRACKS`로 두 액션 모두에서 걸러낸다. `subject:"기하"`를 직접 요청하면 `excluded_track`(400). 영향은 미미 — OMEGA 4회차 통틀어 16명분(I2·I4·I8·I11), I2 평균이 72.6→72.7로 움직이는 정도.
- 롤백은 v4 재배포. 기존 액션(`login`·`logout`·`students`·`student`)은 미변경.

- **원본은 회차마다 엑셀 2개**: 성적(`DB` 시트 + 반별 시트 + 평균) · 문항분석(과목별 시트, 7회는 `DB` 통합). 원본에는 **학번·탐구1/2·국어유형(화작/언매)·수험번호**가 더 있고 그중 학번만 적재했다.
- **적재 규칙: 수학 응시자만.** 원본 248/245/244/241명 → DB 184/147/137/155행으로 정확히 일치(총 623).
- **조인 키는 `student_no`.** 이름도 원본에서 수기로 구분돼(`강건우1`·`김유찬1`) 실제로는 유일하지만, 그 규칙에 기대지 않는다.
- ⚠️ `official_scores`는 **`role='owner'` 전용**으로 다룰 것. 학원 계정(`role='academy'`)에 열면 동의 범위 밖 개인정보 제공이 된다.
- **등급컷 확장 (2026-08-13)** — 대성 더프리미엄 공식 등급컷 12회차를 받아 적재했다(48행).
  `exam_cuts` 에 **`subject`·`cut6~8`** 을 더하고 키를 `(exam_code, subject, basis)` 로 넓혔다.
  기존 5회차는 `subject=''`(과목 무관)로 그대로 산다. `grade_of()` 는 **옛 시그니처를 유지**한 채
  과목·기준을 선택 인자로 받는다(호환 확인함).
  - 담긴 것: 2025년 시행 8회차 + 2026년 시행 4회차 · **확통·미적분**(기하 제외) ·
    **1~8등급** · 기준 둘 — `실제응시`(더프리미엄 응시집단) / `수능예상`(실수능 분포 환산).
  - ⚠️ **선택과목별로 컷이 다르다.** 같은 85점이 `DSP-27-M03` 에선 2등급, `DSP-27-M07` 에선 3등급이다.
    회차당 컷 한 벌로 다루면 틀린다.
  - ✅ **프론트 반영 완료 (2026-08-19)** — `cuts.json` 이 과목별 컷(`subj:{확통,미적분}`)을 담고
    `estGrade(score,subj,code)`·`examCuts(code,subj)` 가 과목 컷을 고른다. 옛 모양(cuts 배열)
    호환 유지. **기준은 `수능예상`만 내보낸다**(실제응시를 섞으면 같은 점수가 두 등급을 가진다).
    cuts.json 은 `deltaskill-ops/scripts/gen_cuts_json.py` 로만 재생성한다(손 수정 금지).
    ⚠️ DB `grade_of` 는 basis 를 안 주면 두 기준 중 아무 행이나 잡는다(order by 가 subject 만
    본다) — 호출할 때 **basis 를 명시**할 것. 또 grade_of 가 2인자·4인자 두 벌이라 PostgREST
    rpc 는 `p_subject` 를 안 주면 300 Multiple Choices 가 난다.

### 회차코드 체계 — `{SRC}-{학년도2}-{KEY}` (2026-08-13 확정)

> ✅ **분류체계 재정리 (형 승인 2026-08-27)** — 두 갈래 두 층위: **기출(정부)=시험 제도 코드**
> (SNG·PYG·EDU·EBS) / **사설=발행 주체 코드 주체당 1개**, 시리즈는 KEY 앞 글자.
> 메가 통합이 본체: OMG·OMB(·black·the27) → **MEG-{YY}-OG/OB/OK/OT{NN}**, 전대실모 J{MM}.
> 옛 코드는 안 지운다 — DB `exam_aliases` 치환표(30행)가 잇고 **신규 등재부터 새 체계**.
> JR↔JRO·MEGA↔MEG 폴더 철자 통일은 라인 배치 완료 후. 상세: `deltaskill-ops/docs/코드체계-재정리안-0827.md`.
> ✅ 지QUEL(JQL)=메가 QUEL 계열 판정(형 확인 0827 '퀄은 메가뿐'+R2 파일명 전수 메가 표기) → MEG 포함, 신규 등재 금지.
> ⚠️ OMJ 2행은 MEG-26-J09와 겹침 의심 — 치환표에서 제외해 둠(원본 대조 후 결정).

1,326개 PDF 를 넣기 직전에 세웠다. **도구코드가 138종으로 갈라졌던 사고를 회차코드에서
반복하지 않기 위해**, SRC 를 DB 표 `src_codes` 에 등록하고 `exams.src` 에 **FK** 를 걸었다.

- **YY 는 학년도**(시행년+1). `2026년 3월` → 27 · `2027학년도` → 27.
  ⚠️ 이걸 섞으면 한 해가 통째로 어긋난다 — 이 프로젝트에서 가장 자주 난 사고다.
- **KEY**: 시험은 시행키(`M03`·`M06`·`M09`·`M11`·`R05`), 교재는 교재키(ASCII).
- **`MO` 로 시작하는 파일 = 대성학원 모의고사 원본이다** (운영자 확정 — 다시 묻지 말 것).
  드라이브 `연월별사설 모의고사/{연도}/` 안의 `MO{YY}…{MM}` 꼴: 끝 두 자리가 시행월.
  예: `MO263…03` = 2026년 3월 시행 = **DSP-27-M03**. 파일명이 다 가려져 있어도 MO 면 대성이다.
- SRC: `SNG`수능 · `PYG`평가원 · `EDU`교육청 · `EBS`교재 / `DSP`대성 더프리미엄 ·
  `SDA`시대인재 · `OMG`·`OMB`OMEGA · `MEG`메가 · `JQL`지퀄 · `PRV`개인 제작(강사)
- **한 발행처가 여러 시리즈를 낸다** — 시대인재는 서바이벌·숏컷 등. SRC 는 발행 주체
  하나로 두고 **시리즈는 KEY 앞 2글자**로 가른다(`SV01`·`SC01`). 새 시리즈는 `src_codes.note` 에 먼저 적는다.

**⚠️ 회차물은 시행월을 쓰지 않는다 — 회차번호로 만든다** (2026-08-14 운영자 확인).
오메가·서바이벌은 **시행일이 정해져 있지 않고 학원마다 다른 날 친다.** 파일에 `2026년 10월` 이
적혀 있어도 그건 그 학원이 친 날일 뿐 시험의 성질이 아니다.
- **월을 코드에 넣으면 같은 5회차가 학원마다 다른 회차코드로 갈라진다** — 도구코드가 138종으로
  갈라졌던 사고와 같은 모양이다. 그래서 월을 **의도적으로 버린다.**
- 있는 것은 **"몇 학년도 몇 회차"** 둘뿐이고 코드도 그 둘로만 만든다: `OMG-27-R05` · `SDA-27-SV01`.
  OMEGA 는 시리즈 구분이 SRC 층(OMG/OMB)에 이미 있어 `R` 하나면 된다.
- ⚠️ **월조차 없을 수 있다.** 연도와 회차만 있는 이름이 정상이다 — 월이 없다고 판별 실패로 보지 말 것.
- ⚠️ **맨 연도(`2027_서바이벌_01`)는 학년도로 읽는다.** 사설 회차물은 대비 학년도로 이름 붙여 파는
  관례다. **시행년으로 이름 붙이는 발행처가 있으면 그만큼이 한 해 어긋난다** — 보고서의
  `연도근거=맨연도→학년도` 를 `--apply` 전에 눈으로 확인할 것.
- ⚠️ 회차는 **근거가 있을 때만** 읽는다(`N회`·`제N회`, 또는 이름 **맨 끝**의 숫자). 아무 숫자나
  주우면 `2016EBS7030A형` 이 7030회가 된다. 규칙 검사는 `ingest_local.py --selftest`(12건).
- **월과 회차가 함께 있으면 회차가 이긴다.** 회차는 시험의 성질이고 월은 그 학원이 친 날이다.
  **회차 표기가 없을 때만** 월로 만든다(`OMG-27-M10`).
- **연도는 폴더명에서 빌린다** — 오메가는 연도를 폴더로 구분해 뒀다(운영자 확인).
  `2027 오메가/OMEGA_5회.pdf` 처럼 파일명에 연도가 없는 것이 정상이다.
  ⚠️ **폴더에서 회차는 빌리지 않는다** — 한 폴더에 여러 회차가 들어 있어 폴더로 회차를 읽으면
  그 폴더가 통째로 한 회차로 뭉개진다.
- ⚠️ **파일명이 일련번호뿐이면 근거로 치지 않는다**(`201103가18`·`2016EBS7030A형`).
  근거로 세우면 인쇄 제목과 어긋나 **멀쩡한 문서가 통째로 검토필요로 막힌다** → 그럴 땐 제목만 본다.

**⚠️ 기출 폴더는 `고N` + 시행월이면 출처를 정한다** (2026-08-14 실측). `--gichul` 은 사람이
"이 폴더는 기출"이라고 보증한 것이다. 그 안에서 이름이 학년과 월을 함께 밝히면 그걸로 정한다.
- 이걸 안 하면 `2003년-06월-문제및해설고3-수리(가형)` 꼴 **339개**(EBS 267 + 년도별 72)가
  전부 코드미상이 된다 — 이름에 '전국연합학력평가'라는 낱말이 없어서다. **스캔본이면 인쇄
  제목도 못 읽어 영원히 막힌다.**
- ⚠️ **학년이 결정적이다** — 고3만 6·9월이 평가원이고 고1·고2는 6·9월도 학력평가다.
  학년이 없으면 이 길을 쓰지 않는다(개인 제작이 섞인다).
- ⚠️ **회차는 괄호 안에 있을 수 있다**(`OMEGA BLUE 수학_문제지(2회)`). 꼬리표 `(문제)`·`(해설)`
  을 떼기 **전에** '회' 형을 먼저 본다 — 먼저 떼면 회차까지 날아간다. 반대로 안 떼면
  `2027_서바이벌_01(문제)` 의 01 이 '이름 맨 끝'에 안 걸린다(서바 8개가 그래서 막혔다).
- **OMEGA 는 이름의 `BLUE` 로 OMG/OMB 를 가른다.** 예전엔 통째로 사람에게 넘겼는데
  **표기가 있는 것까지 넘길 이유가 없다** — 표기가 없을 때만 넘긴다.
- **파일명도 출처의 근거다** — 본문만 보면 스캔본에서 아무것도 못 얻는다.

**⚠️ 연도 미상은 코드미상이 아니다 — 출처를 남긴다** (2026-08-14 운영자 지시).
출처를 알아냈는데 연도만 모르는 것을 '아무것도 모름'과 한 칸에 넣으면 **알아낸 것을 버리는 셈**이다.
- 학년도 자리에 `XX` 를 넣어 보고서 `연도미상` 칸에 모은다: `OMB-XX-R02` · `DSP-XX-M10`.
  사람은 **연도만** 채우면 끝난다.
- ⚠️ **`XX` 코드는 등재하지 않는다.** 다른 해의 같은 회차와 한 코드로 겹쳐 조용히 덮어쓴다.
  확정은 `--map` 에서 한다.
- ⚠️ `yy_month()` 는 연도를 못 찾아도 **월은 계속 읽는다.** 예전엔 `(None, None)` 을 돌려주며
  읽어낸 월까지 버렸다.

**⚠️ 폴더가 연도를 알려 주는 곳이 많다 — 다만 하나일 때만 빌린다** (2026-08-14).
`D:\수학 NEW\4. 모의고사 모음\연도별 사설포함 모의고사` 는 `2021년 모의고사`…`2025` 로 **연도별 폴더**다.
- 연도를 **형태 가리지 않고 세어 정확히 하나일 때만** 빌린다. 형태별로 세면
  `[년도별] … (2003-2024년)` 에서 '년' 붙은 게 2024 하나뿐이라 통과해 **22년치가 통째로 2024년으로 뭉개진다**(실측).
- ⚠️ **형제 폴더끼리 표기가 어긋난다** — `2024년 모의고사` 옆에 맨숫자 `2025` 가 있다.
  앞은 시행년(→25학년도), 뒤는 학년도(→25학년도)로 읽혀 **같은 25가 다른 해를 가리킨다.** 사람이 확인할 것.
- ⚠️ **`사설포함` 폴더에 `--gichul` 을 주지 말 것.** 주면 `고N`+월 규칙이 **개인 강사 모의고사를
  교육청 기출로 등재한다.** 안 줘도 문서에 출처 표기가 있는 기출은 정상으로 잡힌다.
- ⚠️ **`.hwp` 는 자동으로 무시된다**(PDF 만 훑는다) — 한글본은 오타 위험으로 이미 접은 갈래다.

**⚠️⚠️ 오메가는 두 해 분량이 있고 회차 번호가 겹친다** (2026-08-17, GPT F 판정으로 발각).
인쇄된 제목을 읽어 보니 같은 폴더에 **2026학년도와 2027학년도가 섞여 있었다.**

| 파일 이름 꼴 | 인쇄된 제목 | 코드 |
|---|---|---|
| `OMEGA_모의고사_수학_NN회_문제지(검토본).pdf` | **2026학년도** OMEGA 모의고사 N회 | `OMG-26-R{NN}` |
| `OMEGA_NN회_수학_문제지_unlocked.pdf` | **2027학년도** OMEGA 모의고사 N회 | `OMG-27-R{NN}` |

- **5·6·7·9·10 회가 양쪽에 다 있다.** 이름 규칙만으로는 **절대 못 가른다** — 내가 만든
  `map_ledger.json` 이 둘 다 `OMG-26` 으로 뭉개 **5건이 한 해 어긋나 있었다.**
- ⚠️ **가르는 유일한 근거는 문서 안의 인쇄된 제목이다.** 파일명·폴더명은 이 경우 무력하다.
- ⚠️ **OMEGA blue 는 2026학년도**로 확인됐다(원장 `OMB-26` 과 일치). blue 와 일반이 **다른 해**다.
- **저작권자는 둘 다 메가스터디교육(주)** 다. SRC 를 발행 주체로 두는 원칙과 어긋나지만
  원장이 이미 `OMG`·`OMB` 를 쓰므로 **바꾸지 않는다** — 기록만 남긴다.
- 피해: 등재된 오메가 21행이 거의 전부 `kind=answer`(해설지)라 **분할이 안 돼 문항 오염은 없다.**
- ⚠️ **`item_sources` 에 원본 파일명 칸이 없어 어느 행이 어느 파일에서 왔는지 DB 만으로는 몰랐다.**
  `source_name` 을 추가한다(`db/migrations/2026-08-17_…`). 다음엔 되짚을 수 있어야 한다.
- 확정표: `db/map_확정.json`(194개) — **F 판정이 내 추정을 이긴다.** 만드는 도구는 `scripts/f_to_map.py`.

**취합 후 일괄 정리** — 판정은 초안이고 확정은 모아서 한 번에 한다: `deltaskill-ops/docs/회차코드-일괄정리-0814.md`.
갈라짐·겹침·중복·빈칸은 **여러 파일을 모아 놓아야만 보인다.** 가르는 자는 이름이 아니라 **정답표 해시**다.
⚠️ 최종 산출물은 `--map` 확정표다 — 그래야 나중에 판정 로직을 고쳐도 확정된 대응이 안 흔들린다.

**자동 판정** (`ingest_local.py`) — 사람이 `--src` 를 안 줘도 되는 경우가 많다:
① 문서 전문(앞16쪽+뒤8쪽)에서 **저작권·발행처 표기**로 출처 ② 문항 1번 쪽 상단의
**인쇄된 제목**에서 학년도·시행월 ③ 파일명에서도 뽑아 ②와 **대조 — 어긋나면 중단**.

⚠️ **여기서 실제로 잡은 함정 넷** (전부 조용히 틀린 회차로 등재될 뻔했다):
1. **사설이 수능 제목을 흉내 낸다.** 대성 제목이 `2027학년도대학수학능력시험3월모의고사문제지` 다.
   "대학수학능력시험"이 있어도 **시행월이 11월이 아니면 수능이 아니다.**
2. **출처를 모르면 코드를 만들지 않는다.** 예전엔 시행월만 보고 `3월 → EDU(교육청)` 로 단정했다.
   그러면 **개인 강사 모의고사가 교육청 기출로 등재된다.** 개인 제작은 제목이 제각각이라
   이 오분류가 대량으로 났을 것이다. 기출은 `전국연합학력평가`·`한국교육과정평가원` 같은
   **적극적 근거**가 있을 때만 인정한다.
3. **저작권 표기 위치를 가정하지 않는다.** 대성은 쪽 하단이지만 모두가 그렇지 않다 → 전문을 훑는다.
4. **표기가 갈리면 집계가 갈린다.** 대성 제목이 `대성 THE PREMIUM` / `더프리미엄` /
   `대성 더프리미엄` 세 갈래였다 → `대성 더프리미엄 {학년도}학년도 {월}월 고3` 로 통일.

⚠️ 아직 사람 손이 필요한 곳: **OMEGA**(OMG/OMB 를 이름으로 못 가름) · **스캔본**(텍스트 없음) ·
**개인 제작**(제목 제각각) — 전부 `--src` 또는 `--map` 으로 준다.

⚠️ **대성 더프리미엄은 응시 규모가 교육청 다음으로 크다**(운영자 확인). 등급컷·정답률
표본으로 값지다 — 사설 중에서 우선 순위를 높게 둔다.

### ⚠️ 데이터 모집단이 셋으로 갈린다 — 섞으면 안 된다 (2026-08-13)

같은 회차라도 어느 집단의 숫자인지가 다르다. 이걸 섞으면 "정답률 X%면 Y등급" 같은 **틀린 추론**이 나온다.

| 데이터 | 모집단 | 어디 |
|---|---|---|
| **대성 등급컷** | **대성 더프리미엄 전국 응시자** | `exam_cuts` basis=`실제응시`/`수능예상` |
| **문항 정답률·변별도** | **한 학원 학생** (운영자가 강사로 있는 곳) | `item_stats` |
| **성적 명단** | **같은 한 학원 학생** | `official_scores` |
| 옛 등급컷 5회차 | 같은 한 학원 응시자 추정 | `exam_cuts` basis=`학원응시자상대추정` |

- ⚠️ **문항 정답률은 전국 정답률이 아니다.** 그 학원 표본이라 전체 평균과 다를 수 있다(운영자 확인).
- ✅ 반대로 **둘을 대조하면 그 학원의 전국 대비 위치를 잴 수 있다** — 같은 회차에
  전국 등급컷과 학원 점수 분포가 함께 있는 회차가 생겼기 때문이다. 이건 새로 열린 길이다.
- ⚠️ 학생 성적 명단은 **개인정보** — 어느 리포에도 넣지 않는다. Supabase 한 곳뿐이다.

- **등급컷 — 회차별 적용 (2026-08-12 해결, 5회차분)**: DB `exam_cuts` + 함수 `grade_of(exam,score)` · `delta_to_next(exam,score)` · 뷰 `cut_coverage`. 여기서 뽑은 `cuts.json`을 `my/`가 읽어 `estGrade(score, subj, code)`가 **회차 컷을 우선** 쓰고, 없으면 기존 `GRADE_CUTS`(전국 모평 기준)로 되돌아간다.
  - 보유: `OMEGA_5·6·7·8` · `JD27_07`(전대실모 7월) 5회차. 나머지 98회차는 아직 컷이 없다.
  - ⚠️ **`basis='학원응시자상대추정'`** — 그 학원 응시자 분포에서 뽑은 값이라 **전국 등급이 아니다.** 파일럿(해당 학원 학생) 진단엔 맞지만 전국 등급으로 말하면 안 된다. `my/` 표에서 그 회차는 `*` 표시 + 각주로 밝힌다.
  - **가진 컷 아래로는 추측하지 않는다** — 5등급컷이 있는 회차만 6등급까지 말하고, 없으면 `null`(화면엔 `-`). DB `grade_of`와 프론트 `estGrade`가 같은 규칙이다.
  - 효과: 같은 85점이 OMEGA_5에선 2등급, OMEGA_6·8에선 3등급으로 갈린다(예전엔 전부 2등급).
- 기하: 문항분석에 3회차 모두 존재하고 실제 선택 학생도 있지만(I2·I4·I11), **제외로 확정**했다(2026-08-12).

## `/pilot2/live2/` — 단과 강사 미팅용 리포트 (2026-08-12)

첫 유료 제안용 데모. **비밀번호를 넣어야 내용이 나온다.**

- **암호화 방식**: 앱 코드와 데이터 전체를 AES-256-GCM으로 봉인해 base64로 심었다. 잠금 해제 시 PBKDF2-SHA256 310,000회로 키를 파생해 WebCrypto가 복호화하고 `new Function`으로 앱을 띄운다. 백엔드 없음.
- ⚠️ **평문 원본(생성기·앱 소스·데이터 JSON)은 이 리포에 절대 커밋하지 말 것.** 리포가 공개라 소스가 들어오는 순간 암호화가 무의미해진다. 원본은 운영자 PC에 있다(`live2_소스_0812.zip`). 커밋된 것은 암호문뿐.
- **비밀번호 변경·데이터 재생성**: 그 zip 안에서 `node build_live2.mjs '새비밀번호'` → 나온 `live2_index.html`을 `pilot2/live2/index.html`로 교체. 현재 비밀번호는 운영자가 보관.
- 같은 탭에서 새로고침하면 `sessionStorage`로 다시 묻지 않는다(미팅 중 실수 방지). 탭을 닫으면 사라진다.
- 강사명은 데이터의 `meta.teacher`(현재 **정재일**)다. 다른 강사용으로 바꾸려면 재생성이 필요하다.

**관문 통과율** — 상세 화면의 머리다. 원장이 "결정적"이라 표시한 지점(G1·G2·G3)별로 통과율·이탈 인원·지역차를 보여준다.
- ⚠️ **원장에 관문↔단계 대응 관계가 없다.** 174문항 중 개수가 맞는 건 25%뿐이다. 관문 서술이 항상 단계 순서를 따라가고 ★가 많은 단계에 놓인다는 관찰에 기대, `pack_live2.py`의 `align_gates()`가 **별 많은 단계를 관문 수만큼 골라 순서대로 배열**한다. 근사이며, 통과율 수치 자체는 시뮬레이션 단계 데이터에서 그대로 나온다. 미팅에서 "관문별 통과율"이라 말해도 되지만 "관문이 몇 번째 단계인지"까지 단정하지 말 것.
- 이탈 인원은 **직전 관문 이후 구간 전체**를 그 관문에 귀속시킨다 — 그래야 이탈 합 + 마지막 관문 통과 = 응시 인원이 된다.
- 학생 상태 태그는 라벨 문자열이 아니라 **식별자(`drop`/`miss`/`time`/`lazy`/`guess`)로 거른다.** 문자열로 거르면 "과제 미입력"과 "과제 부실"이 함께 걸린다(실제 발생, 39명 → 114명).

**데이터 출처가 세 층으로 갈린다 — 설명할 때 섞지 말 것.**

| 층 | 출처 |
|---|---|
| 점수 분포 | **실측 모수** — OMEGA 5~9회 1~4등급 134명에서 추정 (실력 SD 7.6 · 회차 난이도 SD 1.7 · 주간 변동 SD 5.4 · 잔차 자기상관 실질 0) |
| 문항 분석 | **원장 실산출물** — `ledger_items` 162문항의 스킬코드·단계분해·관문·진단질의를 그대로 씀 |
| 학생 반응 | **시뮬레이션** — 가상. 정답 여부를 따로 뽑지 않고 **단계 통과 확률의 곱**으로 유도해 점수와 막힌 지점이 한 모델에서 나온다 |

- 설정: 대치 223명 · 목동 195명(418명), 1~4등급만, 델타모의고사(현장, 입력 98%) + 스킬모의고사(과제, 92%) 각 5회 완료 + 6회 예약.
- 화면 5개: 브리핑 · 막힌 지점 · 스킬 지도 · 학생 · 다음 회차.
- ⚠️ 시뮬레이션 모수를 건드릴 때 아는 함정 셋: ① 실측 잔차 5.4는 **문항 표집 노이즈를 이미 포함**한다 — 따로 더하면 이중 계상(SD가 14.8까지 뜬다) ② '실력'은 로지스틱을 지나며 격차가 **약 2.4배로 증폭**된다 — 평균만 맞추면 SD가 안 맞고, 지역 오프셋도 같은 배율로 커진다 ③ 단계 ★ 계수를 SIG보다 크게 두면 ★★★ 단계가 문턱이 돼 **이탈이 한 지점에 80% 넘게 몰린다**(퍼널이 무의미해진다).
- 2·3점 문항에는 스킬코드를 붙이지 않는다. 붙이면 쉬운 문항의 0% 오답률이 그 도구의 실패율로 둔갑해 스킬 지도가 오염된다(실제로 겪음).

## 원장(ledger) DB — 2026-08-12 적재 완료 (608문항)

> ✅ **현재값(2026-09-01): 원장 3,577행 · 분석분 3,003 · 갈림 미해소 278.** 재가동 등재 —
> 이승열 기출 gpt 881 첫 수거(+578 등재 · 보류 227) + s2 갈림 정답축 187 판정 반영(해소 174).
> exams 539(+9 = MEG-27-OK01~09 오메가 black, 학년도=서술 추론 note). items 시험지 270단위 10,544문항.
> (직전 0828: 2,999·2,425) 비교기 정규화(형 승인 0828 — 선지 번역
> 동치·도구코드 미부여=결손)로 갈림 775→421, 227문항 추가 등재. (직전 0826: 2,772·2,198) v4 완성본 1,503문항이 세라인
> 등재규칙(B안)으로 들어왔다(machine `L1`/`L2`/`L3` · `line_used` 칸 · 갈림 442는
> `line_disputes` 표). `ledger_enroll_uniq` 는 이제 **전면 유일**(exam_code, subject, qno)
> — machine 무관하게 한 문항 한 줄이 물리적으로 강제된다. 규칙·집행 기록은
> `deltaskill-ops/docs/세라인-등재규칙-초안-0823.md`. 아래 절의 수치(1,385·608·777)는 그날 기준.

Cowork(분석 세션)의 원장이 Supabase로 들어왔다. **분석 산출물 저장소이고 학생 데이터와는 별개 계층이다.**
오전에 v4(314문항)를 넣고, 오후에 `run_all_v3` 분할본 5개로 **608문항 전량**을 채웠다.

| 테이블 | 행 | 내용 |
|---|---|---|
| `tool_codes` | 78 | 정규 도구코드 — 원장이 참조하는 유일한 어휘 |
| `code_aliases` | 164 | 원시코드 → 정규코드 치환표 |
| `exams` | 101 | 회차 마스터 (`{SRC}-{학년도2}-{시행키}`) |
| `ledger_items` | **1,385** | 원장 — **분석분 608 + 수집자리 777**(아래 ⚠️) |
| `exam_items` | 152 | 대성 4회차 채점용 문항 (`ans_format` 제약 보유) |
| `held_items` | 2 | **출제사 정답표 오류로 등재 보류**한 문항 (원장 아님) |
| 뷰 4개 | | `lineage_edges` · `practice_candidates` · `practice_ladder` 등 |

### 수집 한 바퀴 완주 — EBS 19회차 (2026-08-14)

**등재 → 검증까지 처음으로 한 바퀴 돌았다.** `item_sources` 0 → 19 · 크롭 801장 · 오류 0.

- **분할 밀림 0** (`scripts/verify_crop_numbers.py`). 크롭 자리(`rects`)의 PDF 텍스트에서
  머리번호를 읽어 붙인 문항번호와 견준다. **Mathpix 키가 필요 없다** — 우리가 묻는 건
  '수식을 잘 읽었나'가 아니라 '분할이 밀렸나'라서 훨씬 싸게 잡힌다.
  ⚠️ 정답 대조로는 이걸 못 잡는다 — 기출 회차의 원장 `ans_value` 가 회차당 3~6개뿐이라
  표본이 너무 작다. **밀림 검사와 수식 검증은 다른 축이다.**
- 검증 가능 708/1,175(60%) · 전부 정상. 나머지 40%는 **엉터리 OCR 레이어·스캔**이라 판정불가로 뺐다
  (쓰레기를 정답지로 삼으면 멀쩡한 판독을 대량으로 뒤집는다).
  ⚠️ **판정불가는 '밀렸다'가 아니다** — 이 방법으로 못 볼 뿐이고, 밀림 증거는 어디에도 없다.
- ⚠️ **`--map` 은 확정표다 — 표에 없는 파일은 자동판정으로 넘어가지 않는다.** 폴백이 켜져 있던
  탓에 표에 없는 파일이 `EDU-05-M10` 으로 판정돼 **FK 위반으로 `--apply` 가 멈췄다**(실측).
  옛 동작은 `--map-fallback` 으로만.
- ⚠️ **`exams` 에 없는 회차는 등재 전에 건너뛴다.** 여기서 `exams` 를 자동 생성하지 말 것 —
  잘못 판정한 코드가 그대로 새 회차가 된다. **FK 는 방어막이지 걸림돌이 아니다.**
- ⚠️ `q.send` 는 SQL 오류를 `Failed to run sql query: ERROR: …` 라는 **글**로 돌려준다.
  파싱 실패로 삼키면 명백한 오타가 'API 고장'으로 둔갑한다. 그리고 **진짜 간헐 빈응답도 있다** —
  `count(*)` 는 언제나 1행을 주므로 그걸 탐침으로 쓰면 '0행'과 '고장'이 갈린다.

### ⚠️ **원장 행 = 분석분이 아니다 — `machine` 으로 가른다** (2026-08-14)

수집 파이프라인이 처음으로 데이터를 썼다(`item_sources` 0 → 19 · 크롭 801장).
`--collect` 가 원장에 **빈 자리 777개**를 만들어 `ledger_items` 가 608 → **1,385** 가 됐다.

```sql
machine = 'INGEST'   → 수집자리 777   좌표·steps·정답 전부 빈 칸 (분석 전)
machine <> 'INGEST'  → 분석분   608   A·A2·B1·B2·C·D·M1·O1·O2·O3
```

- ⚠️ **`count(*) from ledger_items` 를 "분석 완료 문항 수"로 읽지 말 것.** 옛 글의 608 은
  전부 분석분을 가리킨다. 지금 그 수를 세려면 **`where machine <> 'INGEST'`** 를 붙인다.
- ⚠️ `machine is not null` 로는 안 갈린다 — 수집자리도 `INGEST` 라는 값이 들어간다(실측으로 헛짚었다).
- 수집자리 777 은 전부 `h`·`steps` 가 비어 있다. 분석분에도 빈 것이 있으므로(h 48 · steps 64,
  대성 A2 구멍) **빈 칸 유무로 가르면 틀린다.** 가르는 자는 `machine` 하나뿐이다.

**팩별 구성** — `A 140`(가형·B형 원형) · `A2 64`(대성) · `B1 40` · `B2 40`(OMEGA blue) · `C 108`(나형 원형) · `D 66`(현행 기출) · `M1 30` · `O1 40` · `O2 40` · `O3 40` · **`INGEST 777`(수집자리)**.
계보 **59종 · 5,052쌍**. 좌표 규격위반 0. 도구코드 72종이 실제 사용되고 5문항은 코드 미부여(기초분).

⚠️ **적재 팩은 뷰를 `DROP ... CASCADE` 후 재생성하는데 `security_invoker`를 다시 켜지 않고, RLS 구문도 0건이다.** 팩을 다시 돌릴 때마다 뷰의 `security_invoker`가 풀리므로 **적재 직후 반드시 재적용**할 것 (`run_608.py` 끝에 붙여 뒀다). 적용 후 "RLS 미적용 테이블 0건 · security_invoker 꺼진 뷰 0건" 확인까지가 한 세트다.

- **DB가 두 사고를 구조적으로 막는다** — ① `ans_format` CHECK: 객관식 답을 아라비아 숫자로 넣으면 거부(넣으면 전 문항 오답 처리되던 사고) ② `tool_code` FK: 정규 어휘 밖 코드 거부(코드가 138종으로 갈라진 사고). 둘 다 실제로 위반 INSERT를 던져 거부되는 것을 확인했다.
- **서비스 질의가 실제로 돈다** — 학생이 틀린 문항의 `tool_code`로 원형 문항을 좌표순(쉬운 것부터) 조회. 예: 1049호 → 1108(h2w2) → 943(h2w3) → 946·987(h3w3) → 1721(h4w4).
- ⚠️ **RLS는 이관 SQL에 없어서 따로 켰다.** 신규 5개 테이블 `enable row level security` + 뷰 2개 `security_invoker=true`. 정책 0개 = service_role 전용으로, 기존 15개 테이블과 같은 관행. **뷰는 기본값이 소유자 권한이라 RLS를 우회하므로 `security_invoker`를 반드시 켜야 한다.** 뷰를 새로 만들 때마다 잊지 말 것.
- 이관 패키지는 전부 `ON CONFLICT`/`IF NOT EXISTS` — 재실행 안전, DROP 없음.

### h 좌표 — 규격은 1~3, 정렬은 `h_norm` (2026-08-12 결정)

`h`(은닉깊이) 규격은 **1~3**이다. DB CHECK만 1~4로 열어 둔 것은 원본을 그대로 적재하기 위해서고, **h=4는 규격 위반 기록**이다.

h4 32건이 전부 기계 C에서 나왔다. 4점 문항·같은 도구코드끼리 짝지어 비교하니 **문항이 아니라 평정자가 다른 것**이었다 (이관 SQL 원본 314행에서 재검산, Cowork 수치와 전부 일치):

| 비교 | 가중평균 차 | 겹치는 코드 | 높게 매긴 코드 |
|---|---|---|---|
| C − A | **+0.53** | 19 | 15/19 |
| C − D | **+0.69** | 19 | 15/19 |
| A − D | +0.23 | 10 | 4/10 |

A(2.59)와 D(2.47)는 서로 정합인데 **C(3.17)만 위로 밀려 있다.** 규격을 1~4로 넓히면 한 기계의 눈금을 표준으로 승격시키는 셈이라 좌표가 평정자에 의존하게 된다.

- **원본 `h`는 덮어쓰지 않는다.** 감사·재평정용으로 보존하고, 보정값 `h_norm`(기계 C만 −0.62)을 따로 둔다. 보정 후 A 2.59 / C 2.55 / D 2.47로 맞는다.
- ⚠️ `h_norm`은 **반드시 plain 컬럼**이어야 한다. GENERATED 계산 컬럼으로 만들면 안 된다 — 분업팩 SQL(`11`·`12`)이 `h_norm` 값을 **직접 INSERT**하므로 `cannot insert into generated column`으로 적재가 통째로 깨진다. (2026-08-12에 GENERATED로 바꾸자고 제안했다가 12번 팩을 받고 철회했다. 다행히 실행 전이었다.) 새 행의 NULL 문제는 팩이 값을 직접 넣어 해결되고, 서비스 질의는 `ORDER BY h_norm NULLS LAST`로 방어한다.
- **서비스 정렬은 뷰 `practice_ladder`(h_norm 순)를 쓴다.** 기존 `practice_candidates`는 raw `h`만 노출하므로 정렬용으로 쓰지 말 것.
- 기계 C는 108문항이 **전부 4점**이라, 4점 표본에서 구한 보정을 C 전체에 적용해도 외삽이 아니다.
- h=4인 32건은 `needs_rerate=true`로 표시해 다음 검수 때 재평가한다.
- ⚠️ **근본 대책은 앵커 문항**이다 — 분업팩 마스터프롬프트에 h1/h2/h3 기준 문항을 3개씩 박아 모든 기계가 같은 자를 쓰게 한다. 지금은 "은닉깊이 1~3"이라는 서술뿐이라 해석이 갈렸다. (Cowork 담당)

**`/admin/` 화면 구성** — `show()` 로 세 화면을 전환한다: `main`(학생 목록) · `detail`(개인 상담 리포트) · `cohort`(반별 현황).
- `cohort` 는 `PILOT_CLASSES = ["I1","I2","I5"]` 를 기본 선택하고 전체 반은 체크박스로 켤 수 있다. 파일럿 범위를 넓히려면 이 상수만 고친다.
- 회차를 바꿔도 반 선택은 유지된다. 반 이름에 괄호가 들어가므로(`I11(반수반)`) 토글은 **인덱스 기반**으로 넘긴다 — 문자열 보간 금지.
- 반별 표와 "최고–최저 격차"는 **서버 정렬에 기대지 않고** 화면에서 다시 정렬·계산한다(과거 필터 후 격차가 틀리던 버그).
- 이 페이지는 메인 사이트의 다크 테마가 아니라 **인쇄 친화 밝은 화면**(`--red:#c00`, 표 중심)이다. 위 디자인 체계와 별개이니 이 페이지 관례를 따를 것.

## 개인정보 수정란 (2026-08-12)

파일럿에 `profile` 화면을 추가했다 — 이름·학원·구분·**선택과목**을 고친다. 들어가는 길 두 개: 시험 선택 화면의 `⚙︎ 내 정보` 칩, 마이페이지의 [내 정보] 버튼(`/pilot/?profile=1`).

- 저장은 서버(`POST /profile`) + 로컬(`ds_students`) **양쪽**. 서버가 실패하면 로컬만 저장했다고 화면에 알린다.
- 필수 동의(`consent`)는 여기서 못 바꾼다. 알림·홍보 동의는 백엔드가 받지만 화면엔 아직 안 뺐다.
- ⚠️ **선택과목은 저장한 회차마다 다를 수 있다.** 확통→미적분으로 바꾼 뒤 예전 회차를 열면 21~30번이 다른 과목으로 뒤바뀐다. 그래서 저장된 응답의 `area`로 그 회차의 과목을 되살려 `S._subjOv`에 담고, 화면은 `curSubject()`(=`_subjOv` 우선)를 쓴다. 새 회차를 시작할 때 `_subjOv`를 반드시 `null`로 되돌린다.

## 작업 이력

**2026-09-01** (본부 클라우드 창 — 화요일 아침 재가동)
- **주말(금~월) 각자 작업 완주 취합** — s1/s2 96커밋·통로 76건: 2016~2020 기출 창 직접전사 **81단위 완주**,
  이승열 기출 분석 가동(산출 gpt 881 수거), 형 실물 검수 체계·눈판정대 상설(잔여 0), 조판·그림 규칙 확정.
  정본 취합: ops `docs/지도/작업지도_주말_0829-31.md` → `델타스킬전체지도_260901.html`(재가동판).
- **본부 아침 등재 집행**: line_outputs 9,139 → enroll_v4 확장(--batch·--normalize-gates·--adjudicate) →
  **원장 3,577(분석분 3,003)** · 갈림 452→278 · exams +9(black) · 보류 227(진단질의 결손 — 재작업 큐, 임의 보정 없음).
- 22:00 지도 정례 재활성화. **내일 9/2 = 9월 모평**(22번 검증 예약 유지).
- **(오후) 눈판정대 용역판(judge-caa33b/su1·su2) 렌더 4갈래 갱신** (형 지시·실물 확인 기준 수능2411-13):
  ① 그림 실물 크기 일괄 — 크롭 PNG 픽셀×72/dpi 를 인쇄 본문 10pt 기준 em 으로 환산(그림 속 글자 ≈
  본문 글자, 약간 큼). ⚠️ **items 사본 크롭 1,400장에 dpi 스탬프가 없다**(96 기본값 597·미기록 774) —
  150 넘는 명시값(200×29장)만 믿고 나머지는 표준 230.4 로 본다 ② 보기 ㄱㄴㄷ 상자(`< 보 기 >` 탭·상하
  0.35em — 오탐 잣대: 줄머리 ㄱ. 둘 이상) ③ 분석 글 `$…$` LaTeX 를 KaTeX 직접 렌더(0901 등재 gpt 라인
  62행 — 기존 유니코드 조각 경로는 `$`·`\` 를 몰라 원문 노출) ④ **수식 단독 줄을 블록(위아래 0.35em)으로
  승격** — 분수·∫ 연속 줄 글자 겹침(형 스크린샷 지적) 구조적 해소.
- **(오후) 서바이벌 5~8회 + 서바이벌 프로 8월 파일럿 등재** (형 지시) — items 정본(SDA-2027-SV05~08 ·
  SDA-2026-M08=2027학년도 8월 서바이벌프로, 반입대장으로 정체 확인)에서 SV27_05~08·SP27_08 로
  38문항×5회차 등재. 검증: 세눈 대조 통과·해설↔정답 2원 대조 152문항 불일치 0·배점 100/100·
  왕복 스모크 190문항 불일치 0·JS 오류 0. 분류는 자동(SU1/SU2 확장)+본부 판독 확정표 33건 —
  다섯 회차 모두 공통 수Ⅰ11·수Ⅱ11 로 갈라짐(독립 검산). tc 없음(DSP-27-M08 선례) · CATS 시대인재
  그룹 9회차 · my EXAM_NAMES +5. ⚠️ **분석(steps)은 다섯 회차 모두 아직 없다**(원장 0행·수거 0) —
  단계 UI는 분석 도착 후. 시드 도구 서바시드.py 는 일회성(스크래치패드).
- **(오후) 용어 학생 어휘 일괄** (형 지시: 조각접합·누적적분·max/min 은 학생·교사 미사용) —
  「조각접합」→구간별 함수(DB tool_codes 계열 4행, 마이그레이션 `2026-09-01_도구계열_학생어휘.sql`) ·
  「누적적분」→정적분으로 표현된 함수(deltamap 6·pilot OMEGA_9 steps 2) · max(aₙ,bₙ)→중 최댓값.
  게이트 단답 대조 불일치 0. ⚠️ 원장 재추출 시 같은 사상을 추출기에 걸 것(원장 원본은 보존).
- **(밤) 설명 전략 확장 + 델타맵 위치 첫 실측** (형 지시 — 전부 ops 리포에 정본) —
  ① 교육방법론 검토·보강안 — 상세는 ops `설명전략-0901.md` §6~7 (⚠️ 메커니즘 상세는 공개
  리포에 적지 않는다 — 아래 작업 규칙 참조). ② 홍보·투자 정본 `방법론수렴-홍보투자-0901.md` + 아티팩트
  「델타스킬과 학습과학」. ③ 해설삼원 설계 `해설삼원-델타스킬해설-0901.md`(공식=정답 기준선 ·
  현장=효율 · 우리=구조 · 형=눈금). ④ **위치타당도 1차**(`scripts/위치타당도.py`): 정답 대조로
  증명된 대응만 이음 — SNG-22~25 76/76 · DSP-27-M04/05 52/52 ✅, **앱 OMEGA↔원장 26학년도 전
  조합 기각**(두 해 회차번호 겹침 재실증 — 코드가 비슷해도 잇지 말 것). 전국 n=77 에서 ★총합↔
  정답률 −0.44 · cost 단조(○강 25.6 < ○중 32.8 < ✗ 40.1) — 좌표 방향의 첫 실측 인증.
  ⚠️ **이을 수 있는 기출 대응분의 h 는 0건** — h 축은 검증 불가 상태(앵커·평정 선행).
  ⚠️→✅ 0901 등재분 `ans` 미주입은 **같은 날 밤 재집행으로 해소** — L행 822 중 채움 819
  (어긋 2·대조불가 1은 규칙대로 비워 목록으로, 도장 64 불변). 분석분 ans 3,017/3,020.
  상세 ops `델타맵-위치판정-0901.md` §4 · 보고 `data/정답주입보고_260901.json`.
- **(심야) 수송tv 채록 2단 전량 완료 + 시장 풀이 지도 승인** (형 지시 "전량 확대 진행해") —
  형 유튜브 2026년분 21영상 자막(1단 s1 수거)에서 **설명 사건 292건** 추출(병렬 5기 · 오류 0 ·
  이중검증 10% "방법 건강" 판정). 형 어휘집 후보 16종은 **형 확인 게이트 뒤에만 등재**(후보 단계).
  추출 규격·분포·다음 걸음은 ops `수송tv-2단규격-0901.md`(발화·어휘 실물은 비공개 리포에만).
  **축D 「시장 풀이 다양성 지도」 승인** — 남의 유튜브 해설은 **판정·라벨·링크만 저장**(전사·발화
  불보존 · 눈금 불반입), 착수는 모평 뒤(ops `해설삼원-델타스킬해설-0901.md` §2 축D).

**2026-08-28** (본부 클라우드 창 — 휴면 직전 마지막 기록)
- **명칭 통일 (형 지시)**: 은경=**s1** · 재순=**s2** — 통로 글·보고·창 대화 전부. **폴더·파일 경로는 유지**(개명 금지).
- **본부 휴면 (형 확정)**: 주간한도 97% → 금(8/28)~월 각자 작업, **화(9/1) 아침 본부 재가동**(기상 예약 완료).
  토~월 취합·작업지도·대장 기록은 **s1/s2 담당** — 주말 작업지도는 ops `docs/지도/작업지도_주말_0829-31.md`(신규),
  정본 전체지도는 화요일 본부가 갱신. 기간 중 저장 = 매뉴얼 + **DB 직접 쓰기 금지**(파일로만) + 몰아 저장 금지.
  s2 렌더 산출(문항 원문)은 **형 직접 수령·검토 후 공유 드라이브 `렌더산출_s2/날짜/` 저장**(리포 금지).
- **공정 진행선 기준선 = ops `docs/지도/델타스킬전체지도_260828.html`** — 디지털·저장·분석·교정·렌더·렌더수정
  여섯 축의 "어디까지"를 실측으로 못 박음(원장 2,999 · 분석분 2,425 · line_outputs 8,258 · 갈림 421 ·
  기출 미분석 889/킬러 216 · 렌더 감사 153/153). 배정대장 147행으로 갱신(신규 122~125 · 맥 중지 표기).
- **기출 우선 (형 확정)**: s1이 `분석꾸러미_260828_기출`(킬러 216 먼저) 제작·업로드 → 이승열 착수. s2는
  node+katex@0.18.4 설치 → 신규 전환분 렌더 + 갈림 정답축 187 재검수.

**2026-08-26** (본부 클라우드 창)
- **★ 최대병목 종결 — v4 분석 완성본의 원장 등재.** 완성본 2,463문항(58단위)이 원장에 한 건도
  없던 것(analysis_ver v4 = 0행)을 0823 세라인 등재규칙(형 확정 B안)으로 풀었다:
  **등재 1,503**(L1 1,217·L2 154·L3 132) · 원장 1,269 → **2,772행** · 중복 0.
  갈림 442(도구코드 350·정답 32·둘다 60)는 `line_disputes` 표에 — 다수결로 넣지 않았다.
  기하 402 는 등재복원 원칙대로 보류, 기존 기분석 103행은 덮지 않았다.
- 집행 순서: 마이그레이션(`ledger_enroll_uniq` 부분→**전면 유일** + `line_used` 칸 +
  `line_disputes` 표) → 수거 리포갈래 신설(분석_ 2,553 → `line_outputs`, 총 5,097행/6라인) →
  등재기 `enroll_v4.py`(B안 판정, `merge_gpt.enroll_item` 재사용 — `extra` 인자만 추가,
  selftest 10 + 회귀 80) → dry → 표본 1단위 → 전량. 전 단계 검산 일치.
- 본부 확장 셋 → **형 판정 완료(같은 날 저녁)**: L1/L2 라벨 승인("단일 산출도 당연히 등재") ·
  내용 라인 순위 claude 우선 유지(차후 분석이 늘면 **문항별 복합 분석**으로 재결정, 비채택
  라인은 line_outputs 보존) · 정답 주입 착수 승인.
- **정답 주입 단 집행 완료** (`정답주입_v4.py`) — 원천은 items 정본 answer, 주입 시점 재대조
  (교정 이후 어긋 방지) · **채움 1,502 / 빈칸 1**(형 눈판정 대기 문항) · `ans_source` 칸 신설로
  출처·대조 근거 행마다 명기 · **도장(answer_verified) 0 유지** — 인쇄 증인 계보 실측 후 2차.
  ⚠️ 재대조가 실제로 잡은 둘: 정답표 한 칸 밀림(정본 기교정 확인) · 1단 원문자 표기 오기
  (값 일치 — 본부판정 개별 주입, 자동 규칙은 안 풀었다).

**2026-08-19 오후** (클라우드 창)
- **분업 3인 체제 가동 준비 완료** — 형이 워크스페이스(admin@deltaskill.co.kr)에 누나 두 분
  계정(sister1·sister2)을 만들고 **공유 드라이브 「델타스킬 작업공유」** 개설(공유 드라이브는
  데스크톱이 그냥 동기화 — 단 새 드라이브는 앱 재시작해야 보인다, 맥 실측). 맥이 v3.2 키트
  445파일·규칙·배정 배치. 비개발자용 **시작프롬프트**(claude.ai 에 붙여넣으면 클로드가 안내자가
  되는 포인터 프롬프트 — 규칙 본문은 README 가 정본) `docs/분석원-시작프롬프트-0819.md`.
  첫 배정: 두 분 0차 18문항(3인 눈금 대조용) → 은경 SNG-2024-M11·PYG-2024-M09 /
  재순 SNG-2023-M11·PYG-2023-M09 4점 전량(**밀림 잔여 34단위 피해서 선정** — 목록 00_키트/밀림_잔여.csv).
  누나 폴더는 **번호식 완결 꾸러미**(0_안내·1_프롬프트·2~4_규칙·5~9_시험·그림 5장 — 각 16파일,
  전부 v3.2 정본에서 복사)라 폴더 밖 탐색이 필요 없다. **체제 확장(형): 클로드코드 자율 작업** —
  각 폴더에 `작업지시서_클로드코드용.md`(작업 큐·재개 규칙·자가 검증·금지 조항 내장),
  사람 명령은 "지시서 읽고 그대로 작업해줘" 한 줄. **최종판(에이전트 병렬)**: 지시서에
  문항마다 Task 에이전트 하나·동시 4~8·주 세션 4종 검수(1회 재시도) + 에이전트 프롬프트 틀 내장.
  배정 = **깨끗한 현행 25단위 전부**(밀림 잔여 34 회피·중복 0): 재순 9단위(우선순위 트랙 —
  최신 수능·9월 모평, 형: AI 경험 많음) · 은경 8단위(학평·옛 평가원) · 이승열 8단위.
  0차 포함 총 ~390문항 — 소진되면 게이트가 다음 배정.
  **작전 본부 체계(형 확정)**: 본부(클라우드)=전략·최종 게이트 / 이승열·은경=자동화 생산
  라인(AI 돌려놓고 본업, 하루 재개 명령 2~3번) / **재순=낮 1차 검수 전담**(밤엔 생산) —
  표본 재검산 3문항·answer_mismatch 전수·skipped 검토를 `검수지시서_클로드코드용.md` 로
  자율 수행, 검수보고 붙은 묶음부터 원장 적재. 상세 `deltaskill-ops/docs/작전본부-분업체계-0819.md`. **이승열 왕복은 본인 공유 폴더
  「델타스킬」 유지**(형 확정 — 종료 표시 철회), 새 드라이브의 10_제출_이승열/ 은 게이트 보관용.
- **이승열 0차 v3 재실시 18문항 — 합격 확정** (유사도 최대 0.52 독립 재확인 · 도구코드 13/18 ·
  answer_mismatch 0 · v2 와 같은 자리 같은 불일치 = 눈금 일관). ★ 직원이 **밀림 규칙을 독립
  도출**("PIC 태그가 지문 머리면 앞 문항 것")했고 그림 문항 판정 5개가 분해기 수정·figures 실물과
  삼자 일치 — 0차용 추가 크롭 7장은 유령으로 판명, 중지. 2차 개시(SNG-2021-M11·PYG-2022-M09).
  잔여: 도구코드 불일치 4건 눈금 회의(게이트 자료 작성 예정).
- **PAT 재발급(형) → 막혔던 셋 즉시 처리** — ① admin-api **v6 배포**(items 에 source 필터,
  임시 세션 스모크 4/4 + unknown_source 방어) ② **item_stats PK → source 포함 유니크**(옛 PK 가
  두 모집단 공존을 409 로 막고 있었다 — 실측) ③ 신촌 OMEGA_5~8 240행 적재 → **SINCHON 660행 완성.**
  ⚠️ api.supabase.com 은 Python 기본 UA 를 403 으로 막는다(Cloudflare) — curl 또는 UA 지정 필수.
  ⚠️ 새 PAT 는 이 세션 .env 에만 — 세션 끝나면 사라지므로 새 세션마다 형이 다시 준다.
- **직원(분석창) 0차 게이트 대조 — 합격 판정.** 문구 유사도 최대 0.57(자연 겹침 수준, 베낌 0) =
  **가림 실패 오염 없음·독립 분석 확인.** 도구코드 13/17 · cost 15/17 · answer_mismatch 0 ·
  어휘 밖 코드 0. 직원이 M04 확통29 조건을 30번 머리에서 스스로 재조립하고 확통30 에 "조건 표
  누락 — 해설 치환식으로 복원"을 명기 — **밀림을 현장에서 먼저 감지했다.** 전 18문항 재실시 취소,
  잔여: 그림 문항 재실시 + 도구코드 불일치 4건 눈금 회의(게이트가 준비). 하루 17문항(첫날·장애물
  포함)이 분업 인력 산정의 실측 기준점이 됐다.
- ⚠️⚠️ **기출 코퍼스 계통 결함 발견 — 조건 블록·그림이 다음 문항으로 밀려 붙는다** (맥 실측,
  120쌍/102단위, 통로 메모 147). 실물: EDU-2021-M04 확통 29번의 조건 (가)(나)와 그림이 통째로
  30번 지문에 들어가 있고 29번은 비어 있다. **검산 넷(문항수·배점합·미주수·번호묻힘)을 전부
  통과하는 경계 사고**의 두 번째 꼴(표지 빨림과 같은 계열). ✅ **분해기 수정 완료**(클라우드 —
  split_hwp_items 는 클라우드 소유로 판명, a5e5ca4): en 경계 span 머리의 고아 블록(개체 문단·
  조건 줄)을 앞 문항 stem 으로 복원 + 과식 방지(한글 실린 문단은 안 삼킴) + **다섯째 검사**
  (ⓐ 조건머리 시작=실패 · ⓑ 그림언급↔pics 어긋난 쌍=의심 기록 — A∧¬B 갈래가 정상적으로 이
  모양이라 실패로 안 둠). 자가검사 25→34건. 맥이 0차 3단위 재생성(v3.1)→전량 259 재생성+
  재검증 진행. **재생성 전까지 120쌍 문항은 분석·서비스에 쓰지 말 것**(목록: 그림밀림_의심.csv).
- **직원(분석창)이 v2 로 0차 17문항을 이미 제출** — v2 는 원장 가림이 실패한 판이라 보정
  표본으로서의 독립성이 증명 불가. 판정: **대조용 강등**(게이트가 원장 18행과 문구 유사도·눈금
  차를 이중 검사), 재실시는 밀림 수정 후 v3.1 로 전 18문항. 산출물 원본은 보존.
- **분석창 키트 v3 재작성** (형: "파이프라인 다시 확인" 피드백 → 전면 재점검, 통로 메모 140·141).
  잡은 결함 둘: ① ⚠️ **v2 원장 가림 실패** — "0차 회차 원장 행을 보지 말라"고 README 로만
  막고 `ledger_608.json` 에 18행이 그대로 있었다. **가림은 물리적으로**(608→590 제거) —
  글로 막는 가림은 가림이 아니다. ② **0차 18문항 중 9개가 그림 문항인데 키트에 그림 0장**
  (규칙대로면 skipped → 보정 표본 반토막). 맥 크롭 170장+manifest 동봉, 그림 규칙(불일치=보고)
  README 에 명문화. 나머지 pics>0 7문항 크롭은 맥에 지시.
  **전달처(형 확정): 구글드라이브 공유 문서함의 이승열 작성 "델타스킬" 폴더** — 직원 쪽이 만들어
  공유한 폴더에 키트를 넣는다(메모 142). 내 드라이브 분업 폴더 v2 는 폐기 표시.
- **신촌메가 실데이터 적재 완료** — official_scores 1,825행(9회차, 수학 응시자만) +
  item_stats `source='SINCHON'` 420행(6회차 + JEONDAE_8 라벨 정정). 위 기관용 표가 정본.
  기록: `deltaskill-ops/db/migrations/2026-08-19_신촌9회차_official_scores.sql` · `_신촌문항분석_item_stats.sql`.
  - ★ **적재 전에 신촌 오메가 5~8 정답 ↔ DB I반 정답 238문항 대조 — 불일치 0.** 같은 exam_code 를
    쓰려면 같은 시험임을 먼저 증명해야 한다(오메가 두 해 회차번호 겹침 사고의 재발 검사).
    덤으로 OMEGA 정답표의 제3자 확인이 됐다.
  - ⚠️ **PostgREST 일괄 INSERT 는 배치 안 모든 객체의 키가 같아야 한다** — 키가 섞이면 400.
    행마다 키 집합을 통일(없는 값은 null)하고 넣을 것 (실측).
  - ⚠️ **신촌 OMEGA_5~8 문항통계는 보류** — admin-api v5 `items` 가 source 를 안 걸러
    같은 exam_code 의 두 모집단이 owner 화면에 겹쳐 뜬다. **admin-api v6 일감: `items` 액션에
    source 필터(기본 NULL=I반) 추가 후 적재.** 배포는 PAT 필요 — 클라우드 PAT 사망 중이라 대기.
- **백데이터 궤적 갱신 (형 지시 반영)** — 7월 전대실모는 신촌+일산 **합산 428명**(평균 66.1 · SD 15.9,
  일산 68.8 > 신촌 63.2)으로 상위% 재계산, 8월 전대실모 위치도 DB 332명 분포로 채움.
  같은 시험은 지점 합산이 정확도를 올린다(형 확인) — 단 **회차 간 추이**는 여전히 지점 구성이
  다르면 주의.
- **`student_baseline` 백필 완료 — 164쌍 · 27명** (테이블 DDL 은 맥 실행, 메모 133).
  앱 학생 ↔ official_scores 이름 대조, 전량 3,157행 기준 재매칭.
  - ⚠️ **PostgREST 는 limit 없이도 1,000행에서 자른다** — 첫 판 매칭이 1,000행만 보고 돌았다.
    전량 조회는 offset 페이지네이션 필수 (실측).
  - 보류 2명: **김주영**(명단이 김주영/김주영1 로 갈리는데 회차가 안 겹쳐 동일인 판별 불가) ·
    **김동현**(앱 미적분 ↔ 신촌 명단은 미적분→확통 전환 이력 — 동일인 개연성 높지만 형 확인 대기).
    **박정민은 동명 2인**(신촌 04반 미적분 vs I4 확통)을 앱 선택과목으로 판별해 신촌만 넣음
    (`matched_by='이름+선택과목'`).
- **형 지시(맥 경유): "반 구분은 의미 없다 — 학원 구분만 잘 되도록 저장하라"** →
  `official_scores.branch` 칸 추가(맥 마이그레이션, 일산%·신촌% 백필). ⚠️ **OMEGA_5~9 I반의
  학원은 비워 둠** — 정황(강희재·박정민: I4 ↔ 일산 04반 점수·과목 일치)은 "I반=일산"을
  가리키지만 형 확정 전엔 백필하지 않는다.

**2026-08-19** (클라우드 창)
- **SSR 렌더러 1단계** (`deltaskill-ops/scripts/render_items.mjs`) — 문항 JSON→렌더 모델
  {stem_html·figures·choices_html·solution_html}→KaTeX HTML. **실패=게이트(exit 1)** ·
  그림 삼각 게이트 내장. JD+M08 92문항 실패 0, JD 원본을 넣으면 조립 누락 4건을 정확히
  잡는다(맥 assemble 수정의 회귀 시험용). 산출·미리보기는 통로 메모 119. 입력 두 형식
  (DSP/JD 꼴) 정규화 — 기출은 한컴→LaTeX 변환기(다음 조각) 후 같은 입구로.
- **대성 8월(DSP-27-M08) 도착·전환 완료** — 형 업로드 스캔본(문제지 24쪽·해설지 수학부 12쪽,
  합본 19~30/88 추출)을 Mathpix OCR 로 전환. 46문항 분할(공통22·확통8·미적8·기하8, 배점합
  74+26=100, 빈 선택지 0) · 해설 46/46 결합 · **그림 게이트(조립 단계, 언급↔링크) 0건** ·
  크롭 5장 확보(cdn 만료 전). 원본은 통로 메모 111, 산출은 112.
  ⚠️→✅ **정답 확정·등재 완료** — 빠른답 표는 실물이 없다(형 확인: 표지 없는 합본).
  대신 **공식 해설이 정본**: 풀이 최종값(OCR 유도, 객관식은 선택지 유일일치 강제) ↔ 해설
  스캔 원본 **육안 판독**의 2원 대조 46/46 불일치 0 으로 확정(미적28 은 OCR 꼬리가 잘려
  직접 계산→원본에서 2√2e² 확인). exams 행 추가(마이그레이션 파일+PostgREST — PAT 죽어
  Management API 불가). 파일럿 등재 38문항(기하 제외)·CATS 대성 그룹·my 회차명, 자동
  대조 게이트 38/38·배점합 100·스모크 JS 오류 0 후 배포. 확정본은 통로 메모 113.
  남은 것: 3점 steps·원장 규격(분석창) · 8월 공식 등급컷 공개 시 적재.
- **4점 16문항 steps 동시 등재 + 워크플로 표준화** (운영자 확정: "새 회차 배포에 문항분석이
  같이 올라가야 학생이 어느 과정에서 틀렸는지 잡힌다") — 공식 해설 기반 분해→델타맵 등재→배포,
  게이트 단답 대조 193문항 불일치 0·스모크 16문항 주입 확인. 새모의고사 스킬 5단계에
  "4점 steps 동시 등재(해설 없으면 24시간 내 보강)" 를 표준으로 박음.
- **정식 렌더 = KaTeX SSR 확정** (`deltaskill-ops/docs/정식렌더-경로B-0819.md`) — JD27_08
  전 회차 1,517블록이 전처리 1건(`\text` 속 `_`)만으로 **실패 0**(간이 matplotlib 는 우회 필요).
  사전 렌더를 배포 게이트로 쓴다. 남은 큰 조각은 한컴 수식 스크립트→LaTeX 변환기(기출 갈래).
  문항 렌더 HTML·그림은 문항 원문이라 **DB·Storage 만, 정적 배포 금지**(전달은 토큰 뒤).
- **그림 누락 검증 = 세 신호 삼각 대조** (`scripts/audit_figures.py`) — 본문 언급(A)·원본
  개체(B)·산출물 칸(C). 첫 전수 감사: HWP 8,516문항 중 그림 필요(A∪B) **2,110**, 언급 있는데
  개체 없음 **212문항/147단위**(재편집본 — 원본 PDF 크롭 대상). ⚠️ 언급도 링크도 없는 그림
  문항(JD 확통 27)은 사후 감사로 못 잡는다 — **게이트는 원판이 손에 있는 조립 단계에** 건다(메모 110).
- **왕복 검증 도구(4층 방어의 ③, 마지막 미구현 층)** — 운영자 제안(디지털 파일→문제 재생성→원본
  대조)의 실행형 `deltaskill-ops/scripts/verify_roundtrip.py`. 문항 JSON 을 문자 재고로 되돌려
  원본 PDF 와 항목별 부분집합(JSON⊆PDF) 대조 — 본문 오염(수식 오타·숫자 뒤바뀜·남의 문항 혼입)을
  잡는다. 자가검사 7건 · items_0819 전량 어휘 실측(미지 토큰 0.006%). 실행은 맥 소유(메모 108).
  ⚠️ 한계 명시: 배열 뒤바뀜·선택지 맞바꿈·스캔본·그림은 못 본다 — 상세 `docs/왕복검증-설계-0819.md`.
  경로 B(수식→LaTeX 렌더 조판 왕복 — 스캔본 검증 + 서비스 문항 표시 겸용)는 다음 설계.
- **재조판 전 회차 테스트 (JD27_08)** — 운영자 방향 확인: **저작권자(출제 학원)가 고객** —
  그들 문항을 재조판해 그들 학생에게 파는 구조라, 사설 재조판 테스트는 판매 준비다.
  46문항 문항+선택지+해설의 수식 조각 **1,792개 렌더 실패 0** (분해 규칙 3개 보완 후:
  cases 환경 줄 펴기 · \text 분리로 깨진 \left/\right 짝 벗기기 · mathtext 미지원 기호 치환).
  13·15번 데모 페이지는 형에게 직접 전달(내부 시연용 — 정적 배포 금지 원칙 그대로).
  ⚠️ matplotlib 렌더는 간이다 — 경로 B 본체는 KaTeX(cases·aligned 원생 지원)로 짓는다.
  ⚠️→✅ `figure_asset` 전부 null 은 **조립 누락으로 확정** (형 실측: 원문에 그림 있음).
  그림 문항 5개(공통 4·20, 확통 27, 미적 26, 기하 29) — Mathpix 원판엔 좌표까지 있었다.
  **복구 방법 확립**: 이 PDF 는 그림이 래스터 띠 조각이라 `get_image_info()` 조각 **합집합
  상자** 크롭이면 된다(5/5 복구, 배율 추정 불필요 — mathpix 좌표 역산은 배율이 어긋난다).
  파이프라인 수정은 맥 몫(메모 109) — 게이트에 **"'그림과 같이' 언급 + figure_asset null"
  검사**를 넣어 이번 꼴을 기계로 잡는다.

**2026-08-19** (맥 창 · 상세는 `deltaskill-ops/docs/레지스트리-지역구획-0819.md` · `기출정답대조-전량-0818.md`)
- **그림 파이프라인** (상세 `deltaskill-ops/docs/그림파이프라인-0819.md`) — 갈래가 둘이라 도구도 둘이다.
  PDF/Mathpix 길은 `assemble_exam_v2.py` 수정(본문 분리·삼각 게이트·스트립 합집합),
  HWP 길은 **`crop_figures.py` 신규**(원본 PDF 에서 잘라 채운다).
  - 감사: HWP 8,516문항 중 **212문항/147단위**가 "그림과 같이"라면서 그림이 없다 →
    **175 크롭 · 미확정 37.** 분석 창 0차 3단위의 7문항은 **7/7 크롭 + 눈으로 원본 대조**.
  - ⚠️⚠️ **그림은 틀려도 티가 안 난다 — 기계 검사가 전부 통과한 채로 남의 그림이 붙었다.**
    닻을 '첫 일치'로 잡았더니 `그림과 같이` 같은 흔한 말이 걸려 `EDU-2021-M10` 28번(삼각형)에
    18번(두 곡선) 그림이 붙었다. **PNG 를 열어 지문과 견준 것 하나로 잡았다.**
    → **유일한 한글 닻만** 쓰고, 없으면 **인쇄된 번호 + 지문 겹침**으로 고르되 1등이 2등보다
    뚜렷할 때만 받는다. **남의 그림을 붙이는 것이 빈 칸보다 나쁘다** — 학생은 그 그림을 믿고 푼다.
  - ⚠️ 2단 조판에서 **'닻과 가로로 겹치는 개체'로 물으면 안 된다** — 닻은 왼쪽 여백의 번호(x 62~79)인데
    그림은 들여쓴다(x 116~237). 겹침을 요구했더니 **3/4 를 놓쳤다.** 같은 '단'인지로 본다.
  - ⚠️ 그림 게이트는 **원판이 손에 있는 조립 단계**에 건다. 언급(A)만으로는 **3/5** 만 걸린다
    (`확통 27`·`기하 29` 는 '그림' 언급이 아예 없다) — 원판 개체 신호가 없으면 2문항이 조용히 샌다.
    ⚠️ 그 개체 수를 **모르면 그 신호를 쓰지 않는다** — 모르는 것을 '없다'로 읽으면 통과시키는 것과 같다.
  - ⚠️ 크롭 이미지는 **리포에 넣지 않는다**(기출 문항 이미지·저작권). `{work}/items/figures/`.
- **왕복 검증 전량 완주** (`verify_roundtrip.py --registry`, 4층 방어의 ③ 본문 오염) —
  **259단위 · 일치 255 · 검토필요 4.** 상세는 `deltaskill-ops/docs/왕복검증-전량-0819.md`.
  - ⚠️ **첫 판은 335문항이 걸렸는데 셋 다 검사기 결함이었다.** 허용오차는 한 번도 안 건드렸다 —
    걸림이 준 것은 **검사기를 고쳐서**다(335 → 96 → 4).
  - ⚠️⚠️ **한국 시험지 PDF 는 수식을 사용자 정의 영역(PUA) 폰트로 조판한다** — `z` 가 `z` 로
    안 나온다. 2002·2008·2012·2021·2024 **다섯 시대 전부 PUA/알파벳 비율 0.74~1.83 으로
    예외가 아니라 표준**이다. `CLAUDE.md` 가 적어 둔 "사제 서브셋 폰트"가 이것이다.
    → **텍스트로 수식을 검증하는 길은 이 코퍼스에서 막혀 있다.** 판정불가로 버리지 않고
    **수식만 빼고** 견주되 판정에 `(수식제외)` 를 **적어서** 내보낸다. ⚠️ 조용히 좁히면 눈감기다.
  - ⚠️ 그 밖의 계통 오차 셋: 그림 마커 `⟦PIC:BIN0001⟧` 가 재고에 새어 **'초과 정확히 6'**
    (P·I·C·B·I·N) · `EQ` 정규식에 **`re.S` 가 없어** 여러 줄 수식(`cases`·`eqalign`)을 통째로
    놓침 · **현행은 과목별로 문제지가 따로**라 확통 문항을 미적분 시험지와 견줌(정답표와 같은 모양).
  - **남은 4건은 전부 1번 문항에 표지 문구가 딸려 들어간 것**이다(수식 오염 아님) —
    분해기가 표지를 1번으로 빨아들였다. ⚠️ 문항수·배점합·미주수 검산은 **전부 통과**하는데
    1번 지문만 부푼 꼴이라, `split_hwp_items` 의 '번호묻힘' 검사가 막으려던 사고의 약한 형태다.
- **★★ 정답 대조 완주 — 259/259 일치 · 판정불가 0 · 문항 5,547 · 불일치 0.**
  마지막 1단위는 형이 평가원에서 받아 온 **공식 정답표**(`{DS_DATA}/정답표/`)로 메웠다(30/30).
  - ⚠️⚠️ **공식 정답표는 우리 문서와 꼴이 아주 달라 기존 규칙으론 한 개도 안 잡힌다** —
    「문항번호·정답·배점」 세 칸이 **4열 접이식**으로 되풀이되고 **정답이 원문자가 아니라 맨숫자**다.
    그리고 **30번 답이 소수(`26.25`)** 라 정수만 읽으면 `26`·`25` 로 쪼개져 **뒤가 통째로 한 칸씩 밀린다.**
    → **세 칸 묶음으로만** 읽고 **1~30 빠짐없이 + 배점합 80/100**(80점 시대 1996~2003 시행)일 때만 받는다.
  - ⚠️ 파일→단위 연결은 이름으로 하되 **학년도 = 시행년 + 1** 을 지킨다(`2002학년도…자연` → `SNG-2001-M11-자연`).
    **계열이 이름에 없으면 걸지 않는다** — 어느 시험 것인지 모르는 표를 들이대면 멀쩡한 분해를 뒤집는다.
  - 순서는 **우리 문서가 먼저**, 공식표는 못 읽었을 때의 보루다.
  - ⚠️ 정직하게 남길 것: 흩뿌린 정답과 HWP 미주는 **같은 해설의 두 사본**이라 완전 독립 대조는 아니다.
    다만 막으려는 것이 **HWP 재타이핑 오타**이고 그건 이 대조가 정확히 잡는다.
- (그 앞 판) 정답 대조 259단위 중 일치 258 · 판정불가 1 · 문항 5,517 · 불일치 0.
  - ⚠️⚠️ **'정답표없음 77' 은 자료가 없는 게 아니라 우리가 못 읽던 것이었다** — 하나도
    내려받지 않고 **77 → 0**. 옛 기출의 `문제및해설` PDF 는 정답을 **표로 모으지 않고**
    해설마다 `18) 24` 꼴로 **한 줄씩 흩뿌린다.** 대조기는 "한 쪽에 짝 20개"를 요구해서
    쪽마다 한두 개뿐인 이 꼴을 통째로 놓쳤다.
    ⚠️ **레지스트리의 `endnote_answers` 가 그 수를 이미 세고 있었다** — 가진 정보를 안 쓰고
    '없다'고 하던 셈이다. **없다고 말하기 전에 우리가 가진 것부터 다시 볼 것.**
    ⚠️ 줄 전체가 `n) 답` 일 때만 받는다(해설 본문의 `(3) 따라서…` 를 주우면 남의 숫자를 답으로 믿는다).
    표가 있으면 이 길로 오지 않는다 — 미주형은 폴백이다.
  - 남은 1단위(`SNG-2001-M11-자연`)는 미주가 17개로 문턱(20) 미달. 빠진 것은 `18.18) 다음은`
    처럼 **번호에 붙어 한 줄에** 있다. ⚠️ **한 단위를 살리려 정규식을 느슨하게 풀지 않았다.**
- **대조기 현행 46문항 본체 — 판정불가 31 → 0.** (그 판) **259단위 · 일치 180 · 문항 3,725 · 불일치 0**
  (v1 118/2,396 → v7 180/3,725, 판마다 단위별로 견줘 **깨진 것 0**).
  - ★ **3벌 표를 파싱할 필요가 없었다** — 현행(2021~)은 발행처가 **과목별로 파일을 나눠** 낸다
    (`…수학문제및해설(미적분).pdf`·`(확통)`·`(기하)`). 그 파일엔 **공통 1~22 + 그 과목 23~30** 만
    있어 **번호열이 단일**이라 옛 시대와 같은 방식으로 읽힌다. 30단위가 이 길로 풀렸다.
    ⚠️ **공통은 세 파일 어디에나 있다 — 서로 어긋나면 그 표를 믿으면 안 된다**(`공통답_파일간충돌`
    판정을 뒀다. 실데이터 충돌 0). ⚠️ 합본 이름(`(미적분+확통+기하)`)은 **가르지 않는다.**
  - ★ 진짜 3벌 구획은 **1단위뿐**이었다(`EDU-2023-M05`, 경기교육청 통합 해설). `[확률과 통계]`·
    `[미적분]`·`[기하]` **구획 머리말**로 가른다. ⚠️ **선택 셋이 전부 23~30 이라 구획을 안 가르면
    마지막 과목이 앞 과목을 덮는다.** 공통은 **머리말 없는 쪽에서만** 줍는다(선택 쪽의 1~22 는 해설 본문).
  - ⚠️ **현행 단답은 대조하지 못한다** — 표기가 `16 5 17 3` 처럼 **괄호가 없어** 번호끼리 붙은 것과
    구분이 안 된다. 못 본 것을 `표없음` 으로 남긴다 — **조용히 채우지 않는다.**
  - 남은 판정불가 79는 **거의 전부 '정답표없음'(77)** 이다. 표출처의심·현행46문항 칸은 비었다.
- **기출 레지스트리 지역 구획** (`exam_registry.py` 소유권 맥으로 이관, 메모 92 ③) —
  레지스트리 246 → **281단위** · 대조 일치 118 → **149** · 문항 **3,080** · 불일치 0 · **표출처의심 8 → 0**.
  - ⚠️⚠️ **한 달에 지역별 별개 시험이 여럿 있다** — 10월 폴더의 `대전`·`경남`·`전북` 이 고3 학평과
    한 단위로 뭉쳐 있어 **남의 시험 정답표와 견주고 있었다.**
  - ⚠️⚠️ **그렇다고 지역 이름으로 가르면 안 된다.** 전국연합은 시도교육청이 **돌아가며 주관**하고
    (3월 서울·5월 경기·7월 인천·10월 서울) 파일명에 주관청이 붙는다 — 이름으로 가르면
    **전국 학평 한 회차가 지역 수만큼 조각난다**(도구코드 138종 사고와 같은 모양).
    **가르는 자는 이름이 아니라 정답표다**: 41쌍을 읽어 견줘 대전·경남·전북·대구는 무지역본과
    **0~6/21**(자체 시행 → 가른다), 서울은 **21/21**(주관청 표기 → 안 가른다)로 확정했다.
    `SELF_HELD_REGIONS` 에 **새 지역을 넣기 전에 반드시 정답표로 확인할 것.**
  - ⚠️⚠️ **수능을 사칭하던 단위 넷을 찾았다.** 11월 지역 시험이 `mo==11 → SNG` 규칙에 걸려
    수능이 돼 있었다 — `SNG-2018-M11-가/나형` 은 실제로 **대구** HWP 에서 나왔고(대구 PDF 21/21 ·
    진짜 수능 6/21), `SNG-2010-M11-가/나형` 은 **대전**이었다. 지금은 지역 시험이면 월과 무관하게 `EDU`.
    ⚠️ **그런데도 대조기는 '일치'라고 보고했다** — 제 소스의 정답표와 견주니 자기끼리는 맞는다.
    **일치가 곧 옳음이 아니다.** 분해가 소스에 충실해도 **단위의 정체가 틀리면** 통째로 다른 시험이다.
  - ⚠️ **맥에서 처음 돌려 둘이 터졌다(둘 다 경로 표기)** — ① **NFD 자소분리**로 `re.match(r"(\d{4})년")`
    이 한 건도 안 맞아 **1,842개 전부 폴더파싱 실패 · 시험단위 0개**가 됐고 그 판이 `units.jsonl` 을
    **0줄로 덮었다**(백업이 있어 살았다 — **레지스트리 재생성 전에 산출을 백업할 것**).
    ② `hwp_summary.csv` 의 **Windows 역슬래시 키**가 안 맞아 HWP 미주해설이 **237 → 0**.
  - ⚠️ 곁다리: 고1·고2 9단위가 새로 잡혔다 — 옛 PC 는 `재분류\` 로 옮겼는데 **드라이브 사본엔 그대로다.**
    **드라이브와 옛 PC 가 같은 상태가 아니라는 증거** — 이사 해시 대조 때 함께 볼 것.

**2026-08-19 새벽** (클라우드 창)
- **진단질의 첫 연결 — 원장 → 델타맵 `dq` → 파일럿 오답 카드.** 핵심 루프의 '진단'이 처음
  학생 화면에 나간다. 원장 A2 64문항의 `diagnostic` 을 구조화 파싱(64/64)해 델타맵에 `dq` 로
  넣고(위 규격 절), 파일럿 quiz·note 오답 카드에 자가확인 UI(보기 3개 탭 → 정오 + 이유)를
  붙였다. 대상은 파일럿↔원장 대응이 검증된 대성 4회차뿐(추측 연결 금지 — rx 와 같은 원칙).
  배포 게이트 186문항 불일치 0 · node --check · 브라우저 스모크(주입·섞기·라벨 번역·잠금·
  note 모드·JS 오류 0) 통과. 진단 답은 저장하지 않는다(백엔드 무변경) — 붙이려면 스키마 논의 선행.
- **대성 등급컷 앱 반영** — cuts.json 과목별 확장(위 등급컷 확장 절 ✅) + `my` estGrade/examCuts
  과목 인자 + 공식컷 각주(†) + EXAM_NAMES 대성 4회차(이력 표에 코드가 그대로 찍히고 있었다).
  게이트: 프론트↔DB 규칙 격자 1,818건 불일치 0 · grade_of 실측 14/14 · 스모크 JS 오류 0.
- 분업(분석창) 일정: **목·금 가동, 자료 준비는 후순위** (형 지시). 키트 v1 전달 보류,
  맥 코퍼스 재생성 후 v2 로 교체 예정(메모 97·98).

**2026-08-18 심야** (클라우드 창)
- **분업 재편 — 분석 창 합류** (운영자 지시). 직원 클로드 맥스가 **③ 문항 분석 전담**,
  클라우드는 **기획·작업·병합 게이트**, 맥 로컬은 수집·검증, GPT/Gemini 는 보조로 격하.
  시작 키트(기출 7,816문항 텍스트 + 원장 608 + 도구코드 78 + E형식)를 통로 메모 91 로 올림 —
  0차 보정 표본 18문항(원장 D팩 겹침, 해당 행 가림) → 앵커 → 현행 대량 순.
  분석 창은 DB·리포를 안 만지고, 산출물은 merge_gpt 게이트를 그대로 지난다.
  상세: `deltaskill-ops/docs/분업재편-분석창-0818.md`. 분업 파일 왕복은 **song@goodipat.com
  드라이브**가 기본 저장처(맥 창 경유·지정 공유만, 링크 공유 금지 — 메모 93).
- **게이트 준비 완료** — `merge_gpt.py` 가 `작업:"분석"` 을 받는다(원장 행 상태로
  대조/적재/등재대기 세 길, PostgREST 폴백). 이 과정에서 `steps_parse.py` 의
  **본문 속 화살표 오인**을 잡았다 — `(A→P 경우)` 꼴이 단계 경계로 오인돼 **D팩 66행 중
  13행이 대조 불능**이었다(수정 후 66/66, 608 회귀 깨짐 0). G 재평정 대조도 이 수정의
  덕을 본다 — 다시 돌리면 대조 가능 문항이 는다.
- **★ 미해결 #1 종결 — 원장 분석분 steps 608/608.** 형이 올린 대성 3월 원본(MO 파일)으로
  남은 16문항(전부 4점 킬러)을 **직접 분해**했다: 원장 규격(steps ★·관문·진단질의) 16건 적재,
  `steps=is.null` 가드로 기존 산출물 보호. **전 문항을 코드·해석으로 재검산**한 뒤에만 적었다
  (확통28=524·미적28=71·공통14=49 등 전부 정답표와 일치). 델타맵에 이미 있던 13문항 분석과
  들어맞는 것도 확인.
- **전대실모 8월(JD27_08) ③단계 분석 완료** — 4점 16문항의 steps 를 **공식 해설 기반**으로
  분해해 델타맵에 등재(3점은 관례상 생략). 대성 M03 의 델타맵 빈자리 3문항도 원장 추출로 채움.
  배포 게이트 단답 대조 **172→186문항 · 불일치 0.** 이로써 JD27_08 은 채점+단계 UI까지 돈다.
- ⚠️ 진단질의·관문은 원장 A2 기존 48건의 형식을 그대로 따랐다(3지선다 → 정답 + 오답 이유).
  좌표(h·w 등)는 **일부러 비워 둠** — 평정은 앵커 문항 기준이 서야 하는 별개 작업이다.

**2026-08-18 밤** (클라우드 창)
- **8월 전대실모(JD27_08) 등재·배포** — 로컬 창의 검증본(`all_items_v2`, 정답 46/46 정답표 일치)에서
  38문항(기하 제외)을 자동 대조 후 삽입. 분류(area·unit·topic·tc)는 문항을 직접 읽고 앱 tc 어휘
  46종 안에서만 부여. steps 는 비워 둠(③델타맵 분석 산출물 자리 — 지어내지 않는다). CATS 자리표시자
  `{soon}` → `{ex:21}`. 브라우저 스모크(22/8/8·양 트랙 100점) 통과.
- **처방 첫 연결 — `rx.json` + 마이페이지 연습 처방 카드.** GPT K 의 처방 519는 **파일럿 문항이
  아니라 원장 '원형 문항'에 붙어 있다**(A2 대성엔 0건). 서비스 루프 그대로 **틀린 문항 → 도구코드 →
  그 도구의 원형 기출(h_norm 쉬운 것부터 3개)** 사다리를 원장에서 추출해 `rx.json`(15KB)으로 내보냈다.
  ⚠️ 파일럿↔원장 회차 대응이 검증된 건 **대성 4회차뿐**이라 그 64문항만 잇는다(추측 연결 금지).
  ⚠️ 델타맵 `uses` 는 전부 한글 스킬명 — 도구코드가 없어 다리로 못 쓴다(코드 다리는 원장 A2 행).
  ⚠️ rx.json 에 문항 원문은 없다 — 자작 처방·출처 라벨만이라 정적 배포 가능(deltamap 과 같은 지위).
- **★ 정답 전수 대조 완주 — 불일치 0** (맥 v2 실행, 27초). 2,354문항이 원본 PDF 정답표와
  전부 일치, **진짜 오타 확정 0건.** 어제의 '불일치 275'는 전부 검증기 결함이었다(7차 선택 구획 ·
  엉뚱한 표). ⚠️ 검증기가 틀리면 멀쩡한 데이터가 오염돼 보인다 — 불일치를 고치기 전에 **검증기부터
  의심**한 것이 맞았다.
  - ⚠️ **10월 폴더에 대전 별도 시험이 서울 학평과 섞여 있다** — `표출처의심` 7단위의 정체
    (`2012_10월_대전_모의평가` 등). 레지스트리가 월만 보고 한 단위로 합쳤다. 확정표에서 가른다.
  - 정답표 PDF 없는 수능은 외부 기출(KCSAT)로 폴백 — `대조.csv` 에 표 출처 파일 기록.
  - ⚠️ **이 절의 2,354문항·의심 7단위는 대조기 v1 수치다 — 낡았다.** 같은 날 밤 맥에서 v3 까지
    고쳐 **일치 127단위 · 문항 2,617 · 불일치 0** 이 현재값이다(바로 아래 맥 창 절이 정본).
- **대성 16문항(DSP-27-M03) 직접 분해는 재료 대기** — 지문이 클라우드에 없다. 문제지·해설지 PDF 를
  통로로 요청(메모 80). 오면 원장 규격(steps·gate·diagnostic)으로 분해해 `merge_gpt` 게이트로 넣는다.

**2026-08-18 밤** (맥 창 · 상세는 `deltaskill-ops/docs/기출정답대조-전량-0818.md`)
- **마이그레이션 2건 실행** — `exams` 106 → **116**(OMG-27-R05~R10 · SDA-27-SV01~04) · `item_sources.source_name` 추가 · `session_notes.who` 에 **`mac`** 허용.
  - ⚠️ **8/17 원안은 실행하자마자 죽는다** — `insert into exams (code, src)` 두 칸뿐인데 **`title` 이 NOT NULL**(23502). `source_type`·`kind` 는 기본값이 있어 안 걸린다. **새 회차를 넣는 SQL 은 `title`·`hakyeondo`·`session_key` 까지 채울 것.**
  - ⚠️ **`exams.hakyeondo` 표기가 섞여 있다** — 106행 중 104행이 네 자리(2027)인데 `MEG-26-J07`·`OMG-26-R08` 두 행만 두 자리(26)다. 학년도로 집계할 때 이 둘이 샌다.
- **HWP 정답 ↔ 인쇄본 PDF 전량 대조 — 대조기 v3 까지** (`verify_hwp_answers.py --registry gichul`)
  — 236단위 · **일치 127 · 판정불가 109 · 문항 일치 2,617 · 불일치 0.** (v1 118/118/2,396 에서
  세 곳을 고쳐 올렸고 **기준선 대비 깨진 단위 0**을 단위별 대조로 확인했다.)
  - ⚠️⚠️ **한 단위에 지역별 별개 시험이 섞여 있다** — `EDU-2017-M10-가형` 하나에 **경남·고3·전북**,
    `EDU-2012-M10-가형` 에 **대전·고3**. 10월엔 지역 시험이 여럿 치러지는데 레지스트리가
    연·월·과목만 보고 뭉갰다. 그러면 **남의 시험 정답표와 견주게 된다.** 대조기는
    **분해에 쓴 HWP 와 같은 이름의 PDF 를 먼저** 보게 해 5단위를 살렸지만, **근본 수정은
    레지스트리에서 지역을 가르는 것**이다(`exam_registry.py`, 로컬 창).
  - ⚠️ **`japan`·`china-s` 글꼴은 한글이 통째로 증발한다**(`• 1교시 언어 영역 •` → `• 1   •`).
    자가검사가 이것 때문에 헛돌았다 — 영역 닻을 시험한다면서 **닻 글자가 없는 PDF** 를 만들고 있었다.
    합성 PDF 에 한글을 넣을 땐 **`korea`** 를 쓴다(①~⑤ 도 같이 산다).
  - 남은 판정불가 109: 정답표없음 74 · 현행46문항(외부없음) 31 · 표출처의심 3 · 타과목표만 1.
  - ⚠️⚠️ **전과목 통합 해설지는 첫 표가 수학이 아니다.** `2012_10월_대전_모의평가_정답및해설.pdf`(36쪽·국어~탐구 합본)의 **1쪽이 「1교시 언어 영역」 정답표**인데, `read_key` 가 "짝 20개 이상인 첫 쪽"을 집어 **수학 문항을 언어 정답표와 견줬다**(불일치 24/30). 파일명이 `…수리(가형).pdf` 여도 안쪽이 합본이면 소용없다 → **영역 닻(`수리`·`수학 영역`)이 v2 의 숙제다.**
  - ✅ 다만 **"반 넘게 어긋나면 표출처의심으로 강등"** 가드(같은 날 넣은 것)가 **실제로 막았다** — 없었으면 멀쩡한 분해 8단위가 통째로 '불일치'로 뒤집혔다. 8단위 전부 10월 학평이다.
  - 판정불가 내역: 정답표없음 75 · 현행46문항(v2) 35 · 표출처의심 8. **판정불가는 '틀렸다'가 아니다.**
  - ✅ **외부 기출(KCSAT-ML) 트랙 대응 완료** — 외부는 과목별(`-common`/`-calc`/`-prob`/`-geom`),
    우리는 `SNG-2021-M11` 한 단위다. 접미를 벗겨 합치되 **트랙 칸을 나눠 담는다** — 선택은 세 과목이
    전부 23~30 이라 뭉뚱그리면 **남의 과목 답을 그 과목 것이라 믿는다.** 현행 4단위가 살아났다.
    ⚠️ 우리 items 표기는 **`확통`**이지 `확률과통계`가 아니다. **기하는 대조도 하지 않는다**(등재 금지).
  - ⚠️ **맥에서 zip 은 부모에 푼다** — `~/DeltaSkillData/02_normalized/02_normalized/` 로 한 겹 겹쳐 있어 외부 폴백이 안 걸리던 것이다. 파일이 없던 게 아니라 **자리가 한 칸 깊었다**(sha256 동일 확인 후 겹친 쪽만 삭제).

**2026-08-18 저녁** (클라우드 창 · **새 창은 `deltaskill-ops/docs/새창-시작하기-0818.md` 부터**)
- **맥북이 작업 창이 됐다** — venv·`ds` 별칭·`.env`·통로·드라이브까지 연결. 자가검사 통과.
  ⚠️ **맥 함정 일곱을 실제로 다 밟았다**(전부 `docs/새PC-이어받기-0818.md` §3):
  `unzip` 이 한글을 못 푼다 · `-d` 로 폴더 겹침 · NFD 자소분리 · PEP 668 ·
  **`.env` 에 같은 키 두 줄이면 먼저 나온 옛 값이 이긴다**(`_env.py` 가 이제 경고한다) ·
  `sb_publishable_` 키는 **에러 없이 빈 결과**를 준다 · 환경변수가 `.env` 를 이긴다.
- **구글드라이브가 원본 저장소가 됐다** — `내 드라이브/자료이전/` 에 세 폴더(`기출문제`·
  `연월별사설 모의고사`·`연도별 회차별 모의고사`). `.env` 의 `DS_DESKTOP` 이 여기를 가리킨다.
  여태 걸려 있던 **"형 PC 원본에 백업이 없다"** 가 이걸로 풀린다.
  ⚠️ 다만 **드라이브 파일이 옛 PC 것과 같은지는 아직 안 봤다** — 해시 대조 전엔 옛 PC 를 지우지 않는다.
- **★ HWP 문항 경계는 번호가 아니라 미주다** (`scripts/split_hwp_items.py`, 자가검사 19건).
  옛 기출 HWP 는 문항 번호를 **HWP 자동 번호**로 매겨 **문단 텍스트에 `1.` 이 아예 없다**
  (2001학년도 수능 실측: 번호 후보 0 · 배점 30 · 미주 30). 대신 지문 끝 `⟦EN:n⟧` 이 닻이고
  그 미주에 **정답·출제의도·해설**이 달려 있다 — 번호보다 나은 닻이다.
  ⚠️ **두 길(미주/번호)을 다 재고 나은 쪽을 쓴다** — 한 길만 박으면 그 표기를 안 쓰는 문서가
  통째로 빠진다(`steps` 세 갈래 중 하나만 파싱해 기계 C·D 가 빠질 뻔한 것과 같은 모양).
  ⚠️ **검산 셋을 전부 통과하는 밀림**을 자가검사로 재현했다 — 유의사항의 `1. 문제지에 성명과…`
  가 1번으로 잡히면 **진짜 1번이 그 안으로 빨려들어** 배점·미주까지 딸려 와 문항수 30 ✓
  배점합 100 ✓ 미주수 30 ✓ 이 된다. 그래서 **넷째 검사(번호묻힘)** 를 넣었다.
  ⚠️ 레지스트리 `rel` 은 **Windows 역슬래시**다 — 맥에서 그대로 이으면 파일명의 일부가 돼
  20/20 이 '파싱본 없음' 이 됐다. `_key()` 가 `\`→`/` + NFC 로 통일한다.
  ✅ **전량 실행 완료(클라우드에서 3라운드)** — 237단위 중 **214 통과 · 7,816문항 · 정답 98%.**
  실측으로 잡은 것: 단답 답·배점이 **수식 개체**(`⟦EQ:11⟧`·`[⟦EQ:3⟧점]`) · 7차(2004~2011 시행)는
  **공통25+선택5** 구조 · 만점 80점 시대(1996~2003 시행) · 배점 표기 없는 재편집본은 **검산불가로 구분** ·
  한 파일 찍기 대신 **후보 전수 시도**. 남은 23단위는 확정표(배점 오타류 + 2025 신형식 7개).
  ⚠️ **다음: 원본 PDF 정답표 전수 대조**(`verify_hwp_answers.py`, 맥) — 운영자 지시 "하나라도
  오류 금지"의 실행형은 **미검증 필드는 서비스에 안 내보낸다**다. 4층 방어(구조검산·정답2원·왕복재고·표본).
  ⚠️ **드라이브 스트리밍 파일은 여는 순간 몇 분씩 멈춘다** — `signal.alarm` 은 **C 코드 안에서
  안 터져서**(fz_open) Ctrl+C 도 안 먹었다. 시간제한은 **프로세스 격리**로만 진짜가 된다.
  ⚠️ 대량 읽기 전에 드라이브 폴더를 **오프라인 사용**으로 내려받아 두는 게 맞다(스트리밍과 싸우지 않는다).
  ⚠️ **HWP 길과 PDF 길(`assemble_exam_v2.py`)의 산출 스키마가 다르다** — 원장에 넣기 전에 맞춘다.

**2026-08-18** (로컬 창 · 상세는 `deltaskill-ops/docs/디지털자산화-실행계획-0818.md` · `외부디지털자료-활용-0818.md`)
- **시험 단위 레지스트리** `deltaskill-ops/scripts/exam_registry.py` — 바탕화면 `기출문제` 1,826파일 → **242 시험단위**(고1·고2 16파일은 `재분류\기출문제_고1고2\`로 이동), 확정표 10건, 산출 `C:\deltaskill\registry\gichul\`.
- **HWP 로컬 파서** `deltaskill-ops/scripts/hwp_local.py` — 한컴 API 없이 766개 전부 파싱(71초·비용 0). 수식 스크립트 제자리 삽입 `⟦EQ:…⟧`, **미주 번호=문항 번호**로 정답·출제의도·해설 확보. 242 단위 중 HWP 미주해설 237. ⚠️ 원문자 `➀➁➂`(딩뱃)는 `①~⑤`로 정규화해야 정답이 잡힌다. ⚠️ 주말 `math_assets` 문항 JSON은 소스 선택 오류(경남파이널→수능 공통, 평가원→EDU)로 **초안** — 재생성 대상. `hancom_raw/`만 재사용.
- **외부 디지털 기출** `C:\DeltaSkillData\01_raw` 11종 중 기출 3종(KCSAT-ML 664·Korean_SAT_MATH 120·HRM8K 210) 정규화 → `external_ksat_items.jsonl`. **KCSAT-ML 정답도 ~2% 틀린다**(3건 확인) — 정본이 아니라 제3의 눈. 다수결 금지, 인쇄 PDF 정답표가 결정.
- **`item_stats` 에 KCSAT-ML 전국 cohort 정답률 322행 적재**(`source='KCSAT-ML'`, 위 표 참고). 원장 '통합' 대비 나형 −14~−23p·현행 −3~−6p — 모집단이 다르니 섞어 밴드를 매기지 말 것.

**2026-08-13**
- **정답 대조 자동화** — 앱 정답표 ↔ 원장(`ledger_items`) 대조를 스크립트화(`deltaskill-ops/scripts/verify_answers.py`). 대성 4회차 **64문항 전부 일치, 불일치 0**(단답 28 + 객관식 36 — 객관식은 이번이 처음). `answer_verified` 7 → 64.
- **수식 OCR 채택: Mathpix** — 우리 크롭 3장 실측에서 **3/3 · confidence 1**(tesseract 1/3, pix2tex 1/3). 문제지를 문항 단위로 잘라 선택지 ①~⑤ 값을 읽어 원장 `ans_value` 를 채웠다(대성 22 → **58/64**). 3월은 채운 값을 원본에서 **다시 읽어 9/9 재일치**(왕복 검증). 상세·비용·인증: `deltaskill-ops/docs/정답대조-자동화-0813.md`.
  - ⚠️ 자동화 과정에서 **조용히 틀린 값을 채우는 함정 셋**을 잡았다 — ① 문항 전체를 OCR 하면 **문항 번호가 ① 자리로 들어가 전체가 한 칸 밀린다** ② `\sqrt{10}` 에서 √ 가 증발해 `10` 이 된다 ③ 스캔본은 분할이 밀려 **남의 문항 값을 그 문항 것이라 믿는다**(밀린 값도 등차수열처럼 그럴듯하다). 대책은 각각 번호 연속구간 검사 · 화이트리스트 값 환원(모르는 명령은 통째 거부) · **크롭 머리번호 재확인**이다.
- **창 사이 통로 개설** — 코워크·로컬·클라우드 세 창은 **서로 직접 통신하지 못한다**(확인함). git 은 텍스트만 통하고 문항 PDF·크롭은 저작권 규칙상 어느 리포에도 못 넣는다. 그 구멍을 메웠다: 파일은 **비공개 Storage 버킷 `exchange`**, 메모는 **DB `session_notes`**, 도구는 `deltaskill-ops/scripts/xfer.py`. 왕복 검증(한글 파일명 → 바이트 동일 · 공개 접근 400 · 서명 링크) 통과.
- **로컬 창(형 PC) 연결 완료** — 막힌 것이 여섯 번이었고 **전부 Windows 고유**라 이 컨테이너에서는 하나도 재현되지 않는다. 전체 기록: `deltaskill-ops/docs/로컬-연결-기록-0813.md`.
  - ⚠️ **`subprocess.run(text=True)` 는 로케일(cp949)로 디코드한다** — Supabase 응답의 한글에서 죽어 통로가 통째로 안 열렸다. `encoding="utf-8", errors="replace"` 를 **19곳·11개 스크립트 전부**에 박았다. **새 스크립트를 쓸 때마다 붙일 것** — 리눅스에서만 돌고 형 PC에서 죽는데, 테스트로도 안 잡힌다.
  - ⚠️ 그 밖에: `export` 가 안 먹어 **`.env` 파일 방식**으로 통일 · tesseract 가 **PATH 밖에 설치**돼 자동 탐색 추가 · PowerShell **실행 정책**이 `.ps1` 을 막아 `ds.cmd` 래퍼 · **PAT 가 중간에 무효화**돼 "DB 가 죽어도 파일 전송은 산다"로 분리.
  - ⚠️ **첫 창 충돌이 실제로 났다** — 두 창이 같은 파일을 동시에 고쳤다. **코드 수정 전에 통로로 알리고**, 부딪히면 `git diff` 를 사람이 읽은 뒤 `stash`→`pull`→`pop`. `git checkout --` 은 되돌릴 수 없으니 확인 전엔 쓰지 않는다.
- **GPT Pro 로 주말 분업** (2026-08-14) — 꾸러미 68묶음을 만들어 내보냈다. 가르는 기준은
  **"사람이 하나씩 붙여넣을 값이 있는가"** 다. GPT 는 사람 손이 병목(하루 20~30묶음)이라
  **판단이 깊고 건수가 적은 것**만 준다. 단순 반복(파일명 정리·PDF 회차 판정)은 Gemini 몫이다.
  - 맡긴 것: **E** 대성 64문항 단계 분해(미해결 #1) · **G** 519문항 ★ 재평정 ·
    **H** 진단질의 검수 · **K** 단계별 처방(지금 통째로 빈 칸) · C 시험체계 조사 · B 파서 반례.
  - ⚠️ **G 는 분석자·원래 ★ 를 가린 채 내보냈다** — 보이면 끌려가서 재평정이 무의미해진다.
    대조용 원본은 `deltaskill-ops/대조/`(gitignore). 다시 만들려면 `scripts/steps_parse.py`.
  - ⚠️ **묶음 크기는 출력량으로 정한다.** K 를 40문항으로 두면 출력이 17,000토큰이라
    **한 응답에 안 들어간다**(계산으로 잡았다) → 15문항. G 는 1,500토큰이라 40문항.
  - ⚠️ **`steps` 표기가 세 갈래다**(`- A. …` / `A.…` / `A … ★★ → B …`). 하나만 파싱하면
    그 기계가 통째로 빠진다 — 처음에 C(108)·D(66)가 빠졌는데 **하필 C 가 편차를 실측한 기계**라
    감사가 무의미해질 뻔했다. 파서는 `scripts/steps_parse.py`(자가검사 포함).
  - ✅ **결과 병합 경로 완성** — `scripts/merge_gpt.py` (2026-08-14 저녁). 기본 dry-run,
    `--apply` 로 적재. 작업 넷을 각각 다르게 다룬다: **E** 는 대성에 steps·gate·diagnostic 을
    채우고(미해결 #1), **G 는 DB 를 안 건드리고 대조만 한다**(기계별 ★ 편차 보고),
    **H** 는 `다시씀:true` 만, **K** 는 새 칸이 필요해 마이그레이션 SQL 을 파일로 먼저 낸다.
    - 거르는 것: `confidence 낮음`·`skipped` 자동 적재 금지 · `건수 ≠ items` 면 묶음 통째 보류 ·
      `answer_mismatch` 는 **고치지 않고 목록으로 보고** · `tool_code` 는 안 건드린다.
    - ⚠️ **원장의 `subject` 는 `gong`·`hw`·`mi` 짧은 코드다.** 한글(`공통`)로 찾으면 한 건도
      안 맞는다 — E 가 0건이 나와서 잡았다. 병합기는 두 표기를 다 시도한다.
    - ⚠️ E 의 UPDATE 에 `and steps is null` 이 붙어 있다 — **이미 있는 분석 산출물을 조용히
      갈아치우지 않는다.**
    → 다른 클로드 창에 맡길 일감·파일 소유권·함정은 **`deltaskill-ops/docs/클로드-분업-0814.md`**.
    GPT 출력 표본을 `deltaskill-ops/scripts/fixtures/` 에 실데이터로 만들어 뒀다 —
    **월요일을 기다리지 않고 지금 병합 스크립트를 짜고 시험할 수 있다.**
  - ⚠️ **창 소유권을 문서로 못 박았다** — 2026-08-13 에 두 창이 `q.py` 를 동시에 고쳐
    한쪽 방어 코드가 날아갈 뻔했다. 수집 3파일(`ingest_local`·`split_items`·`collect_all`)은
    수집 창 소유이고, 남의 칸을 고치려면 **통로로 먼저 알린다.**
- ⚠️ **모집단이 10배였다 — `Desktop\델타스킬` 은 전체의 9%다** (2026-08-14 전수 조사).
  그 폴더 밖에 PDF **14,050개(109.9GB)** 가 더 있고 D 드라이브를 통째로 놓치고 있었다.
  다만 **'시험지추정 13,634'는 크게 부풀려진 수**다 — 이름만 보고 센 것이라 아래가 섞여 있다:
  논술 606 · **중1·중2 교재**(에이급수학) 224 · 시간표·행정문서(재정반) 758 · Downloads 잡동사니 1,094.
  이것만 빼도 2,785개가 빠진다.
  - ✅ **확장 1순위는 `D:\수학 NEW\4. 모의고사 모음` 2,689** — 파일명이
    `2004-06월-고1-전국연합-수리영역.pdf` 꼴로 가장 정연해 자동 판정 성공률이 제일 높다.
    ⚠️ **고1·고2가 들어 있다** — EDU 를 시행월로 막지 않은 결정이 여기서 값을 한다(막았으면 통째로 막혔다).
  - ⚠️ **중복이 많다.** `killer + 4 작업…` 폴더가 이름만 다른 채 두 벌(각 37개·4.2GB), `…(1).pdf` 다수.
    **해시 중복제거 전에는 실제 규모를 모른다.**
  - ⚠️ **그렇다고 범위를 먼저 넓히지 않는다.** `item_sources` 가 아직 **0행**이고 전량 dry-run 통과가
    **1개**였다. 입력이 부족한 게 아니라 파이프라인이 아직 한 건도 못 쓴다. 넓히면 검토필요만 는다.
    완료 조건을 **"델타스킬 1,326 중 통과분이 실제로 DB에 들어간다"** 로 바꾼다(옛 기준 '텍스트본 943개'는 폐기).
- ⚠️ **zip 은 만들 때도 풀 때도 Python `zipfile` 을 쓴다 — `zip`·`unzip` 명령을 쓰지 말 것.**
  한글 이름이 **양쪽 끝에서 거울상으로** 깨진다. 둘 다 실측했다.
  - **만들 때**(2026-08-14) — Info-ZIP(`zip` 명령)은 UTF-8 이름 플래그(0x800)를 안 붙이고
    Python `zipfile` 은 비ASCII 이름에 자동으로 붙인다. 안 붙으면 **Windows 탐색기·`Expand-Archive`
    가 cp949 로 읽어 폴더명이 깨진다**(`결과제출` → `寃곌낵?쒖텧`).
  - **풀 때**(2026-08-18, 맥북 셋업) — **macOS 의 `unzip` 은 그 플래그를 무시한다.** 붙여 보낸
    zip 인데도 한글 폴더에서 `Illegal byte sequence` 로 튕겨 **766개가 0개 풀렸다**(registry 는
    `write error (disk full?)` 로 반쯤 풀려 더 헷갈린다 — 디스크는 멀쩡했다).
    → `python -c "import zipfile;zipfile.ZipFile('x.zip').extractall('대상')"`.
  - ⚠️ **`-d 이름` 으로 zip 안 폴더명과 같은 이름을 주면 `hwp_local/hwp_local/` 로 겹친다.**
    zip 이 폴더를 품고 있으면 **부모에** 푼다.
- ⚠️ **PDF 는 두 부류가 아니라 셋이다** (2026-08-13 저녁, 서바01 실측으로 발견).
  ① 텍스트본 ② 스캔본 ③ **스캔본에 엉터리 OCR 레이어가 박힌 것**.
  ③은 "글자가 있느냐"고 물으면 ①처럼 보이는데 내용이 쓰레기다 — 서바01은 문항 머리 `1.`이
  `1●`(3.5pt)로, 수식이 `5'τ`·`ˇΓ` 같은 파편으로 들어 있었다.
  **가르는 자(`split_items.text_layer_quality`)**: 최빈 글꼴 점유율 0.20 · 글꼴 크기 45종 ·
  span 평균 2.31자 · 1~2자 span 69% (정상 조판본은 0.45 · 4종 · 5.5자 · 35%). 넷 중 셋이 걸리면 쓰레기.
  - ⚠️ **이걸 못 가르면 `verify_math` 가 쓰레기를 정답지로 삼아 멀쩡한 판독을 전부 '환각'으로
    뒤집는다** — 조용히, 대량으로. 그래서 쓰레기면 원본 대조를 **거부**한다. manifest 의 `text_layer` 가 근거다.
  - 판정은 `manifest.text_layer` 에 남는다. 새 판독·검증 도구는 이 값을 **먼저** 봐야 한다.
- **시험지 규격도 문서가 알려 준다** (`split_items.resolve_layout`) — **2015 개정 전후로 다르다.**
  2001~2016 은 가형/나형·A형/B형으로 갈린 **30문항 단일지**, 2017~ 은 공통 1~22 + 선택 23~30 × 과목수.
  ⚠️ 46문항 한 규격만 기대하면 **옛 기출이 전부 '검토 필요'로 막힌다** — EBS 폴더의 **60%가 옛 기출**이다.
  판정 순서는 ⓐ 문서 글의 근거 → ⓑ 관측 번호열과 정확히 맞는 규격 → ⓒ 길이로 추정.
  - ⚠️ **ⓑ를 ⓐ보다 먼저 쓰면 안 된다** — 그러면 어떤 분할이든 자기에게 맞는 규격을 찾아내
    **개수검사가 항상 통과한다**(순환논증). 근거가 먼저다.
  - ⚠️ 근거로 '홀수형·짝수형'을 쓰지 않는다(두 시대에 다 나온다). '미적분'도 옛 시험지에 단원명으로
    나오므로 **선택과목 이름이 둘 이상** 걸릴 때만 공통+선택으로 본다.
- ⚠️ **분할 개수검사가 OCR 경로에서만 돌던 구멍** — 텍스트 경로면 번호가 뒤죽박죽이어도
  `needs_review=false` 로 `--apply` 를 통과했다(서바01 22문항이 실제로 통과). 경로와 무관하게 건다.
- **기출 출처는 시행월로 갈린다** — 6·9월에 `한국교육과정평가원` 이 있으면 **평가원을 먼저** 본다.
  ⚠️ 예전엔 EDU 를 먼저 봐서 6월 평가원 문서에 '전국연합학력평가'가 한 번만 스쳐도 `EDU-06-M06` 이 됐다.
  수능은 11월, 평가원은 6·9월 — 벗어나면 코드를 만들지 않는다.
  ⚠️ **EDU 는 일부러 월로 막지 않는다** — **고1·고2 학력평가는 6월·9월에도 있다**(고3만 6·9월이 평가원).
  이걸 모르고 EDU 까지 월로 막으면 저학년 회차가 통째로 막힌다.
- **문항 머리 글꼴 문턱은 문서가 스스로 고른다** (`split_items.pick_min_size`).
  ⚠️ 상수 `10.0` 이 **서바 8.5pt·EBS 9.8pt 의 머리를 전부 버려** 텍스트본을 OCR 폴백으로
  떨어뜨렸다(그러면 쪽마다 tesseract 를 불러 **391개에 15시간**이 걸린다 — 고치면 몇 분).
  발행사마다 글꼴이 다르니 **고정값은 반드시 어딘가에서 틀린다.** 후보 문턱을 훑어 번호열이
  가장 시험지답게 나오는 값을 고르고, 동점이면 큰 문턱(가짜가 덜 섞인다)을 택한다.
- **수식 판독 검증기 `verify_math.py`** — Mathpix 는 잘 읽지만 **틀렸을 때 조용하다.** 사람이 전 문항을
  눈으로 볼 수 없으니 기계가 거르게 했다. 축이 둘이고 **잡는 것이 서로 다르다.**
  - ① **원본 대조**(주력·공짜·결정적) — 크롭 자리의 **PDF 텍스트 레이어가 정답지**다. 판독 결과를
    LaTeX→문자로 환원해 **다중집합**으로 견준다. 환각(없는 숫자)·√ 증발·분할 밀림을 잡는다.
    ⚠️ **배열은 못 잡는다** — `\frac{1}{2}` 와 `\frac{2}{1}` 은 문자 재고가 같다. 그건 ②의 몫이다.
  - ② **이중 판독**(`--dual`) — Mathpix ↔ Gemini. 배열 뒤바뀜과 **스캔본**(①이 못 보는 자리)을 맡는다.
  - ⚠️ **선행 조건: `split_items.py` 가 크롭 자리(`rects`)를 남긴다**(같이 추가함). 자리를 안 남기면
    크롭이 원본 어디서 왔는지 아무도 몰라 **검증이 통째로 불가능**하다. 옛 manifest 는 `자리없음`으로 뜬다.
  - ⚠️ **텍스트 레이어를 못 믿는 판이 셋** — 스캔본(레이어 없음) · 사제 서브셋 폰트(사용자영역 쓰레기) ·
    **글자가 조용히 사라지는 폰트**. 셋째를 실측으로 잡았다(PyMuPDF `china-s` 로 넣은 한글이 추출에서
    통째로 증발). 못 걸러내면 **판독이 읽은 한글 전부가 '환각'으로 뒤집힌다.** 셋 다 `판정불가`로 뺀다.
  - ⚠️ **크롭 글자수로 스캔본을 가르지 말 것** — "다음 값은?" 같은 **짧은 문항이 실제로 있다.** 쪽 단위로 가른다.
  - 검증: 자가검사 8건 + 합성 문제지(2단·8문항) 왕복 — 바른 판독 4건 일치, 사고 4건(√증발·적분누락·
    숫자환각·남의 문항) 전부 실패로 잡음, 한글증발 폰트 `판정불가`. 오탐 0.
- **"문항 텍스트를 밖에서 구해 오면 되지 않나" — 조사 끝, 답은 아니다** (실측 근거 아래).
  같은 질문이 반복해서 나오니 결론을 여기 박는다. 다시 조사하지 말 것.
  - **양이 안 맞는다.** 형 PC 1,326개 중 기출은 EBS 391개(**29%**)뿐이고 나머지 **935개(71%)가 사설**
    (대성·서바·오메가·개인 강사)이다. 사설 문항은 **어디에도 텍스트로 없다** — 공개하면 그 회사가 망한다.
    밖에서 대체 가능한 최대치가 29%인데 그 29%가 마침 **이미 텍스트본**이라 우리 파이프라인에서 제일 쉬운 부분이다.
  - **공개 데이터셋은 먼지 수준이다.** HuggingFace 전수 검색 → 후보 둘.
    `Quadyun/Korean_SAT_MATH` = **120문항**(2022~2025 수능 **미적분만**, `has_image` 플래그 있음 = 그림 문항은 불완전),
    `ChuGyouk/CSAT-Math-Test` = 카드에 "Due to license issue, I cannot share this data". 원장이 이미 608문항이다.
  - **HTML로 올려 둔 곳은 있지만 전부 이미지다.** 마크업 실측 — `mathman.co.kr` MathJax 0·KaTeX 0·`<img>` 11 /
    `mathflat.com` MathJax 0·KaTeX 0·`.png` 412 / `mathpowergen.com` 은 기출이 PDF 다운로드 링크.
    한국 기출 사이트가 이미지인 데는 이유가 있다(수식 렌더링·그림·복사 방지). **이미지면 우리가 이미 하는 일과 똑같고**,
    남의 크롭은 회차·문항번호 대응이 불확실한 데다 저작권이 한 겹 더 얹힌다.
  - **문제은행(매쓰플랫·매쓰홀릭·수학비서)은 무료가 아니고, 애초에 우리와 같은 사업이다.**
    매쓰플랫 베이직 **월 198,000원**·프리미엄 월 300,000원, 10일 체험뿐. "선생님이 공짜로 받아 쓴다"는 건
    학원이 구독하고 학습지를 뽑는 것이다. ⚠️ **거기서 문항을 빼 오면 경쟁사 DB 복제다** — 델타스킬이 나중에
    파는 물건이 바로 그건데 그 위에 남의 DB를 깔면 사업이 통째로 위태로워진다. **하지 않는다.**
  - **한글 원본(.hwp)도 접었다 — 운영자 결정.** 기술적으로는 매력이 있었다: PDF 텍스트 레이어는
    **수식이 깨지는데**(분수·근호·적분이 글리프로 흩어진다) 한글 파일은 수식을 **한글 수식 스크립트**
    (`{x^2} over 2`)로 보관해 LaTeX 기계 변환이 된다. 형 PC 전체에 `.hwp` 가 많기도 하다.
    ⚠️ **그런데 그 한글본은 사람이 타이핑한 것이라 오타가 섞인다.** 원장의 정본은 학생이 실제로 본
    **인쇄된 원본 PDF** 여야 한다 — 정답표·배점·문항번호가 거기 맞춰져 있고, 한글본이 한 글자만 달라도
    그 차이가 조용히 원장으로 들어간다. **수식이 살아 있다는 이점보다 정본이 갈리는 위험이 크다.**
    → **원본 PDF 분석으로 확정.** 수식은 크롭 + Mathpix 로 간다(이미 검증된 경로).
    - ⏳ **다만 이 결정은 아직 숫자가 없다** — "오타가 섞인다"는 정성 판단에 기대 있다.
      2026-08-18(화) 에 **한컴 데이터로더 API ↔ Mathpix 비교**를 예약해 뒀다
      (`deltaskill-ops/docs/한컴-mathpix-비교-0818.md`). ⚠️ **축이 둘이고 섞으면 안 된다** —
      ① 판독 정확도만 재면 한컴이 압승하는데 그 결론은 틀리다. 본체는 ② **같은 회차의
      HWP↔PDF 가 실제로 얼마나 다른가**다. 어느 쪽이 나오든 이득이다(결정을 확정하거나 뒤집는다).
- **회차코드 대응표는 만들 수 없음이 확정** — 파일럿 21회차 × 원장 95회차를 이름이 아니라 **정답 일치율**로 전수 대조한 결과, 100%는 대성 4회차뿐이고 나머지는 30~50%(우연 수준)다. `HP26_03`↔`EDU-26-M03` 은 **0/6**. 번호 체계가 어긋난 게 아니라 **아예 다른 회차**다 — 원장은 학년도(시행년+1), 파일럿은 시행년으로 코드를 매겨 한 해 밀려 있고 원장에 파일럿 회차가 없다. **대응할 것이 없다.** 나머지 17회차로 대조를 넓히려면 원본 시험지 수집이 선행이다.

**2026-08-12**
- **같은 번호 계정 갈라짐 사고 해결** (위 절) — 11행 병합 · 부분 UNIQUE 인덱스 · Edge Function `api` v8 배포 · 프론트 복구 경로 4개 · 개인정보 수정란 추가. 검증: 백엔드 12개 호출(신규 등록·재등록 409·유령 0행·프로필 조회/수정·submit/submit-update 회귀), 프론트 브라우저 6개 시나리오 전부 통과(JS 오류 0, 웹폰트 CDN 차단 제외).
- **정답표 채점 오류 5건 정정** — 서바1 공통8·9, 서바2 공통4, 서바3 공통9·미적26이 전부 정답 `⑤`인데 앱에 `③`으로 들어가 **정답을 쓴 학생을 오답 처리**하고 있었다. 방향이 전부 같은 계통 오류(⑤→③ 오판독). 파일 길이 불변(5글자만 교체) 확인 후 반영.
  - ⚠️ **재발 방지**: 사설 팩은 정답표를 원본 이미지에서 재대조하고(OCR 신뢰 금지), steps 주입 후 **단답 정답 ↔ 마지막 step 자동 대조**를 배포 게이트로 둘 것. 이번 오염도 "앱 정답·topic은 맞는데 맵 steps만 한두 칸 밀린" 형태였다.
- **deltamap 재추출본 반영** — 서바 오염 steps 정리·14문항 재추출·기하 104문항 제거·`SN26` 별칭 추가. 24회차 888문항 → **32회차 1,088문항**.
- 백엔드 진단(코드·스키마 직접 확인): `/submit-update`의 `update_failed_delete` 원인 확정 — 구버전 DELETE→INSERT의 DELETE를 `review_items` FK가 차단한 것. 운영자가 8/11 배포한 v3 UPSERT 핸들러로 해결됐고, 배포 후 실사용 수정 저장 4건 성공·유령 응답 0건·고아 review_items 0건 확인.
- 실서버 점검: 전 페이지가 main(b930600)과 일치, apex→www 301 정상.
- 참고: v3 핸들러는 검산 필드(saved/stored/stale_cleanup)를 반환하지 않음 — 프론트(b930600)는 필드가 없으면 경고를 생략하므로 호환. 정리 실패를 알리려면 백엔드에 필드 추가 필요(선택 과제).
- 기관 데이터 조사(위 절 참고) + `official_scores.student_no` 컬럼 추가·백필(623행 전부, 미매칭 0).
- 파일럿2 방향 확정: **데모(`/pilot2/*`)는 가상 데이터로 유지**하고, 실데이터는 `/admin/`(admin-api 확장)에 짓는다. 첫 대상은 **live(단과) · I1·I2·I5 3개 반**(87명, 성적 스펙트럼 80.5/72.6/48.7, I5에 급락 알림이 실제로 켜짐).
- **admin-api v5 배포 완료** — `cohort`·`items` 액션 추가, 기하 제외. 임시 세션으로 실호출 검증까지 마침(반별 집계·추이 12건·급락 6명·미응시 5명, 문항 30개·기하 차단).
- **원장 DB 이관 실행** — Cowork의 `DB이관_v4` 7파일(804 INSERT) 전량 성공. 위 절 참고. RLS는 패키지에 없어 직접 추가.
- **원장 608문항 적재 완료** — `run_all_v3` 분할본 5개(1.27MB·INSERT 1,117개)를 Management API로 순서 실행. 원장 314→608, 도구코드 55→78, 보류 2건, 계보 5,052쌍, 좌표 규격위반 0. 1049호 서비스 질의 7행 반환(h_norm 순). 팩에 없는 RLS·뷰 `security_invoker`는 직후 직접 적용.
  - ⚠️ 실행 경로: 이 컨테이너는 **5432 직결이 막혀 있다**(DNS ok·TCP 차단). psql 불가 → Management API(HTTPS)로 보낸다. 통합본은 전체가 트랜잭션 하나라 분할본을 쓴다(요청 단위 = 트랜잭션 단위).
- **판단 대기 6건 결정 수령·검산** — Cowork의 h 좌표 분석(기계 C만 +0.6 상향)을 이관 SQL 원본 314행에서 독립 재계산해 **수치가 전부 일치**함을 확인(h4 32건 전부 C, 4점 평균 2.59/3.17/2.47, 짝비교 +0.53/+0.69/+0.23). h 규격 1~3 유지·`h_norm` 도입 결정. 미해결 목록을 핵심 루프 기준으로 재정렬(위 절).
- **`/admin/` 반별 현황 화면 완성** — 회차 선택·반 토글·KPI·반별 비교·회차 추이(인라인 SVG)·이번 주 할 일(급락/미응시)·결정 문항. 데모(`?demo=1`)와 실측 픽스처 양쪽으로 브라우저 구동 검증, JS 오류 0.
- 미해결 #2 **결정됨: 기하는 제외**한다(규칙 유지). skills.json·admin-api 양쪽 모두 동일 원칙.
- **대성 더프리미엄 4회차 등재** (`DSP-27-M03`·`M04`·`M05`·`M07`, 152문항) — Cowork 인수인계 시드를 EXAMS에 주입. `verifySeed()` 통과(빈 배열), 배점 100/100 4회차 전부, 브라우저 스모크 테스트에서 객관식·단답 오답이 정확히 채점됨(95점 = 100−2−3). 대성 회차는 자리표시자만 있고 코드가 없었으므로 코드 충돌 없음. **6월은 시드가 없어 목록에서 제거**(운영자 결정) — 대성 그룹은 4회차 전부 입력 가능.
  - 회차 코드 규칙(Cowork 원장): `{SRC}-{학년도2자리}-{시행키}` · 학년도=시행년+1 · SRC는 `SNG`수능/`PYG`평가원/`EDU`학평/`DSP`대성/`OMG`OMEGA 등.
  - ⚠️ 시드의 수학Ⅱ `unit`이 `미분`/`적분`이었는데 **기존 표기 `다항함수의 미분법`/`다항함수의 적분법`으로 맞춰 넣었다**(32건). 기존 데이터가 100% 그 표기라 그대로 넣으면 단원 집계가 갈린다.
  - ⚠️ `area` 표기는 리포 전체가 이미 혼재한다(`미적분`/`미적분(선택)`, `확률과 통계`/`확률과통계`). 시드는 앞쪽 표기를 쓰며 둘 다 실사용 중이라 그대로 뒀다.

**2026-08-11**
- pilot: **채점/오답노트 분리** (위 표), 인트로를 누르는 카드 2개로 교체, 오답 원인 5개로 정리.
- pilot: 저장 실패(`need_pin`) 해결 — my 로그인이 `ds_tk_ph`를 갱신하지 않아 토큰이 폐기되던 문제. 오류 문구도 한글화.
- pilot: 완료 버튼이 전화번호 화면으로 떨어지던 문제 수정(자동 로그인 재적용), 마이페이지에서 온 수정은 `/my/`로 복귀.
- 전 페이지: 붉은 계열을 **단일 코랄**로 통일, 버튼 위계 정리, 모바일 강제 다크 모드 대응 (위 디자인 체계 참고).
- pilot2: 단과 강사 데모 보강(학생 데이터·개인 리포트·주간 운영 흐름), partners에 관리자 화면 데모 3종 홍보 섹션 추가.

**2026-08-10**
- pilot: 단답형 **[입력] 확정 버튼** 추가, **자동 로그인**, 상단 로고 = 홈 링크, "다른 번호로 로그인".
- my: 회원가입 **학원 선택형 버튼** (메가스터디·시대인재·대성·청솔·종로·기타(직접입력)·비재원생 — pilot과 동일 목록 유지할 것), 동의 체크박스 풀폭 버그 수정, 모바일 반응형.
- skills.json: 지QUEL 2회(JQ27_02)·3주(JQ27_03) 등재.

## 미해결 — 우선순위 확정 (2026-08-12)

**가르는 기준은 핵심 루프(오답 제출 → 진단 → 처방)를 막느냐다.**

1. 🔴 **DSP 4회차의 단계 분해가 아직 없다** — 608 적재로 대성 64문항(`machine='A2'`, 호수 2500~2590)이 원장에 들어왔지만 **`steps` 0/64 · `diagnostic` 0/64 · 좌표는 16/64뿐**이다(다른 9개 팩은 전부 100%). 검산 ⑤의 "좌표미부여 48"이 바로 이 구멍이다.
   - 지금 된 것: **도구코드 64/64** → `deltamap.json`에 `uses`로 넣어 마이페이지·관리자의 "요구 스킬" 칸이 채워진다(2026-08-12 반영).
   - 아직 안 되는 것: 파일럿의 **"어디까지 갔나요?" 단계 UI**. `steps` 없이는 못 띄운다. **steps를 이 리포에서 지어내지 말 것** — Cowork의 A2 재분석 산출물을 받아 교체한다.
2. 🟠 **등급컷 — 앱 반영 9회차로 확대 (2026-08-19)** — 기존 5회차(학원추정) + **대성 4회차
   (공식 수능예상, 확통/미적분 과목별)**. DB 에는 대성 12회차가 있으나 DSP-26 8회차는 앱에
   시험이 미등재라 내보내지 않는다. 나머지 회차는 여전히 전국 모평 컷 폴백 — 컷 소스 확보는
   계속(운영자: "대성 더프는 검색하면 등급컷 자료가 다 있다" — 새 회차 등재 때 함께 적재).
3. 🟡 **deltamap 32회차 중 11회차가 파일럿 미등재** (GDK22_0·JQ27_01·02·03·0603·0701·0702·MP25_06·MP25_09·SN25_11·SU27_06 — 2026-08-13 전수 확인). 반대로 **`JD27_07` 은 앱에만 있고 맵에 없다** — 처방(스킬맵)용. 배점·유형·정답·tc가 담긴 EXAMS 데이터를 Cowork에서 받아야 한다. #1과 같은 작업 흐름이라 함께 처리한다.
4. 🟡 **가입 인증이 없다 — 사업자등록 후 일괄 처리로 결정** (2026-08-13). 지금은 전화번호+PIN만으로 가입돼 번호 소유 증명이 없다. 남은 위험은 **레거시 12행**(인증 붙기 전 계정 — `student_auth`에 번호가 없어 먼저 가입하는 사람이 임자)이고, 그중 실제 학생 8명이 **본인이 가입하면** 그 번호는 닫힌다. ⚠️ 대신 가입시키지 말 것 — `/enroll`의 `consent`는 본인 동의다. 붙일 방식·작업 범위·개인정보처리방침 수정 사항은 `_dev/계정병합-인증-0812.md` §6.
5. ⚪ **404 페이지 · sitemap lastmod · GA 설치 범위** — **지금 하지 말 것.** 유입·측정 인프라인데 진단이 아직 안 돌아간다. #1·#2가 끝나 학생이 실제로 진단을 받는 상태가 된 뒤에 붙인다.

**Cowork 쪽 진행분(이 리포 작업 아님)**: 가형원형 파편 20개는 **통합이 아니라 하차** — DB `ledger_items` 314행 적재를 확인했으므로(2026-08-12) 게이트 통과, Cowork 프로젝트에서 제거하고 압축 인덱스만 남긴다.

## 작업 규칙

1. 수정 후 `node --check`로 JS 문법 확인 (script 블록 추출해서).
2. main에 직접 커밋·push (별도 브랜치·PR 불필요 — 운영자 1인 리포).
3. 커밋 메시지는 한국어로 무엇을 왜 바꿨는지 한 줄.
4. 분석 데이터·파이프라인 관련 요청이 오면: 그건 Cowork 세션 담당 — 완제품을 받아 교체하거나 원장 DB에서 추출한다.

### 저장은 요청받지 않아도 한다 (2026-08-13)

클라우드 세션의 작업 폴더는 **세션이 끝나면 사라진다.** 운영자가 "저장해줘"라고 말하기를
기다리지 말 것 — 아래는 **일이 끝나는 즉시** 저장한다. 한 덩어리가 끝날 때마다 커밋한다
(마지막에 몰아서 하지 않는다 — 세션이 중간에 끊기면 그만큼이 사라진다).

| 무엇을 했나 | 어디에 | 언제 |
|---|---|---|
| 홈페이지 파일 수정 | **deltaskillhomepage** main | 문법 확인 직후 (push = 배포) |
| DB 변경 (DDL·데이터 이동·함수) | **deltaskill-ops** `db/migrations/YYYY-MM-DD_설명.sql` | **실행 전에 파일로 먼저** |
| Edge Function 배포 | **deltaskill-ops** `backend/` (소스 + 롤백 번들) | 배포 직후 |
| 재사용할 스크립트 | **deltaskill-ops** `scripts/` | 검증이 끝나면 |
| 결정·사고 기록 | **deltaskillhomepage** `CLAUDE.md` · `_dev/*.md` | 결론이 났을 때 |

### ⚠️ 커밋했다 ≠ 기록했다 (2026-08-13 원칙화)

**새 창은 `git log` 도, 코드 주석도 읽지 않는다.** 자동으로 읽히는 것은 `CLAUDE.md` 뿐이고,
`docs/*.md` 는 누가 가리켜 줘야 열린다. 그래서 **커밋 메시지와 주석에만 남긴 지식은 사라진 것과 같다.**

실제로 그랬다 — 2026-08-13 오후 Windows 함정 여섯을 전부 커밋·주석에 남겼는데
`CLAUDE.md`·`docs/` 에는 하나도 없어서, 새 창이 열리면 같은 벽에 다시 부딪힐 상태였다.

**한 덩어리가 끝나면 세 곳을 같이 갱신한다. 마지막에 몰아서 하지 않는다.**

| 무엇을 알게 됐나 | 어디에 적나 |
|---|---|
| **다음 창이 모르면 다시 데일 것** (함정·사고·환경 제약) | **`CLAUDE.md` 작업 이력** — 한 줄 요약 + ⚠️ 대책, 상세는 `docs/` 링크 |
| 결론이 난 **결정**(무엇을 왜 그렇게 정했나) | `CLAUDE.md` 해당 절을 **고친다**. 낡은 수치·이름을 남겨 두지 않는다 |
| 재현·검증 과정의 **상세** | `deltaskill-ops/docs/주제-날짜.md` |
| 그 파일을 여는 사람만 알면 되는 것 | 코드 주석 |

**판단 한 줄: "새 창이 이걸 모르면 손해를 보나?" → 그렇다면 `CLAUDE.md`.**

- ⚠️ **낡은 수치를 방치하지 않는다.** 고칠 때 옛 숫자를 함께 고친다
  (미등재 회차가 6→11로 바뀐 걸 두 문서가 모르고 있었다).
- ⚠️ **문서가 서로 어긋나면 어느 쪽이 정본인지 명시한다.** 낡은 문서 머리에 경고를 단다
  (`docs/코워크-0812문서-정오표.md` 가 그 예다).

- **📋 진행 중 작전(8/20~9/1): `deltaskill-ops/docs/델타맵-13일작전-0819.md`** — 목표·라인·일일 리듬·
  본부 토큰 원칙이 거기 있다. 분업 체계는 `작전본부-분업체계-0819.md`, 9/2 당일은 `9월모평-작전-0819.md`.
- **📋 새 창은 이것부터: `deltaskill-ops/docs/새창-시작하기-0818.md`** — **진행 중인 작업**·
  창 넷의 분업·맥북/드라이브 배치·저장위치·숫자·막힌 것·다음 한 걸음이 한 장에 있다.
  **이 문서와 CLAUDE.md 둘만 읽으면 시작할 수 있다.** (앞판 `docs/전체현황-0817.md` 는 8/17 기준)
- **두 리포다.** `deltaskillhomepage`(공개) = 홈페이지 코드. `deltaskill-ops`(비공개) = 백엔드
  소스·DB 마이그레이션·스크립트·live2 평문 원본. 공개 리포에 못 넣는 것이 전부 ops로 간다.
  → `github.com/songjae94/deltaskill-ops` · 그 리포의 규칙은 `deltaskill-ops/CLAUDE.md`.
- **`deltamap.json` 을 건드렸으면 배포 전에 게이트를 돌린다.**
  `python3 ../deltaskill-ops/scripts/check_steps_vs_answers.py .`
  앱 정답표와 맵 마지막 step을 단답형에 한해 대조한다(현재 172문항·불일치 0).
  객관식은 기계로 못 잡으니 사설 팩은 **원본 이미지 재대조**가 따로 필요하다.
- ⚠️ **학생 개인정보와 키는 어느 리포에도 넣지 않는다.** 개인정보는 Supabase 한 곳,
  키는 환경변수·시크릿으로만. 커밋 전에 전화번호·이름·`sbp_`·JWT 패턴을 훑는다.
- ⚠️ **새 발명 메커니즘·눈금 수치는 공개 리포(이 파일 포함)에 먼저 적지 않는다** (2026-09-01) —
  이 리포는 공개라 여기 적는 순간 특허의 신규성 시계가 돈다(**출원이 기록보다 먼저다**).
  상세는 ops 비공개 문서로, 여기엔 무딘 요지와 ops 포인터만. 근거: ops `docs/특허출원전략-0901.md` §6·§8.
- 스크래치패드 중간물은 **다 올리지 않는다** — 다시 만들기 어렵거나 재사용할 것만 고른다.
  스크린샷·일회성 검증 스크립트·중간 JSON은 버린다.
- ⚠️ **DB는 파일로 먼저 남기고 실행한다.** 결과는 백업이 지켜 주지만 *이유*는 안 남는다.
  마이그레이션 파일 머리에 **왜 · 선행 조건 · 실행일**을 주석으로 적는다.
